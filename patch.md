# Like a Dragon: Ishin! — ม็อดแปลไทย (ISHTH) · บันทึกการเปลี่ยนแปลง

---

## v1.4 — 8 กันยายน 2026 · แก้ผลข้างเคียงของ v1.3 (บรรทัดห่างผิด · ข้อความล้นกรอบ)

- v1.3 ทำให้จอทหารหน่วยขึ้นไทยได้จริง แต่ก่อผลข้างเคียงทั้งเกม: **บรรทัดห่างขึ้นราว 64%**
  (จอคำเตือนตอนเปิดเกม) และ **ข้อความล้นกรอบ** ในจอสมุดบันทึก/สารานุกรม
- สาเหตุ: Slate คิดความสูงบรรทัดและการตัดบรรทัดจาก **DefaultTypeface** ของ CompositeFont
  ไม่ใช่จากฟอนต์ย่อยที่วาดตัวอักษรจริง — v1.3 เอาฟอนต์ไทยตัวหลัก (ascender 1290/-350 = 1.640 em)
  ไปทับช่องนั้น ทั้งที่ฟอนต์เดิมของเกมสูง 880/-144 ที่ upem 1024 = 1.000 em
  และสองไฟล์ในนั้น (`DF-FutoKaiSho-W9` · `FOT-UDKakugo_LargePr6N-DB`) เป็น DefaultTypeface
  ของ `Font_System` ซึ่งเป็นฟอนต์ของเกือบทุกจอในเกม
- แก้:
  - คืน `DF-FutoKaiSho-W9` กับ `FOT-UDKakugo_LargePr6N-DB` เป็นไฟล์เดิมของเกม (ไม่อยู่ในม็อดแล้ว)
  - อีก 8 ไฟล์เปลี่ยนไปใช้**สำเนาฟอนต์ไทยที่คัดลอก metric แนวตั้งจากฟอนต์ญี่ปุ่นตัวที่มันไปทับ**
    (`scripts/make_default_typeface_fonts.py` → `font/defaults/`) ความสูงบรรทัดจึงเท่าเกมต้นฉบับเป๊ะ
    แต่ยังมีกลิฟไทยให้วาด — จอทหารหน่วย/ส่งทัพ/ช่างตีเหล็ก/คาราโอเกะ ยังเป็นไทยเหมือน v1.3
- ที่ยังค้าง: จอที่ใช้ `Font_CmnGothic` / `Font_CmnMincho` (DefaultTypeface = สองไฟล์ที่คืนไป)
  และ `Font_MacanNum` (แผนผังความสัมพันธ์ — ชื่อขึ้นเป็นกล่อง `?`) ยังแสดงไทยไม่ได้
  ต้องหาทางที่ไม่กระทบความสูงบรรทัดของ `Font_System` ก่อน
- ผลข้างเคียงที่ยอมรับไว้: สำเนาทั้ง 8 มี ascender เตี้ยกว่าตัวหลัก วรรณยุกต์ที่ซ้อนสระบน
  บนจอกลุ่มนั้นอาจโดน crop ขอบบนเหมือนอาการเดิมของ 3 ก.ย.

---

## v1.3 — 8 กันยายน 2026 · ข้อความไทยหายทั้งบรรทัดบนจอทหารหน่วย/มินิเกม

- **อาการ**: จอ "จัดกองกำลัง" และ "เลือกจุดหมาย" (ทหารหน่วย/ส่งทัพ) ขึ้นแถบเมนูเปล่า ไม่มีตัวหนังสือเลย
  ทั้งที่บรรทัดคำอธิบายด้านล่างจอเดียวกันเป็นไทยปกติ และ `???` ของรายการที่ยังไม่ปลดล็อกก็ยังขึ้น
- **ต้นเหตุ (อ่านจากไฟล์ `Font_*.uasset` จริง)**: ฟอนต์ของเกมเป็น CompositeFont ที่เลือกฟอนต์ย่อยตามภาษา
  และฟอนต์ย่อยของชุดภาษาอังกฤษ (EFIGS) มีฟิลด์ `CharacterRanges` กำกับช่วงตัวอักษรไว้ด้วย
  มีแค่ `Font_System` กับ `Font_CmnFude` ที่ช่วงกว้างถึง U+FFFFFF ที่เหลืออีก 13 ตัวหยุดที่ **U+077F**
  (และ `Font_MgKswKaisho` หยุดที่ U+0200) — ตัวอักษรไทยอยู่ที่ U+0E00-U+0E7F จึง**หลุดช่วง**
  ตัวที่หลุดช่วงจะตกไปที่ฟอนต์ญี่ปุ่นซึ่งไม่มีกลิฟไทย ผลคือวาดออกมาเป็นช่องว่าง
  = ทุกจอที่ใช้ฟอนต์พู่กัน/ไคโช/เอ็นไคโช แสดงไทยไม่ได้เลย ทั้งที่คำแปลอยู่ในไฟล์ครบ
- **แก้**: ทับฟอนต์ญี่ปุ่นที่เป็น DefaultTypeface ของฟอนต์กลุ่มนั้นด้วยฟอนต์ไทยเพิ่มอีก 10 ไฟล์
  (รวมเป็น 13 ไฟล์ในม็อด) — แก้ที่ `.uasset` ไม่ได้เพราะอยู่ใน IoStore ทับด้วย pak ไม่ได้ แต่ไฟล์ฟอนต์จริง
  เป็น loose file ใน pakchunk0 จึงทับได้ตรง ๆ
- **ตั้งใจไม่ทับสองตัว**: ฟอนต์เนื้อเพลงคาราโอเกะญี่ปุ่น (`DF_KANTEIRYU_W6`) และฟอนต์ตัวเลขลายพู่กัน
  (`Myfont_fude-Regular`) เพราะทับแล้วจะเสียคันจิในเพลงกับรูปแบบตัวเลขบน HUD
  ถ้าเจอจอไหนที่ยังไม่มีตัวหนังสือ ให้แจ้ง — น่าจะเป็นสองตัวนี้
- ผลข้างเคียงที่รู้อยู่: ข้อความบนจอกลุ่มนี้จะเป็นฟอนต์ Sarabun แทนฟอนต์พู่กัน/มินโจของเกม
  และคันจิที่ยังหลงเหลือบนจอกลุ่มนี้ (ถ้ามี) จะกลายเป็นกล่องว่าง
- ด่านตรวจทั้งชุดผ่านครบ (ข้อความ .msg 53,016 บรรทัด · ARMP 53,256 ช่อง · locres 23,507 คีย์ · roundtrip 1,678 ไฟล์ · ต่าง 0)

---

## v1.2 — 6 กันยายน 2026 · ชื่อศัตรูลูกกระจ๊อกยังขึ้น ?????? — แก้ที่ต้นเหตุจริง

- **ชื่อศัตรูลูกกระจ๊อกบนแถบพลังยังเป็น `??????` ใน v1.1** (ภาพจากผู้ใช้ 6 ก.ย.)
  v1.1 ไปถอดตารางนามสกุลคันจิ (`stay_enemy_name_all`) ออก แต่ตารางนั้นไม่ใช่แหล่งของชื่อบนจอ
  แหล่งจริงคือข้อความ UI ของ Unreal (`Game.locres`) กลุ่ม `enemy_name_template` 874 คีย์ ซึ่งในเกมต้นฉบับ
  ภาษาอังกฤษเป็นนามสกุลโรมาจิ (Tadokoro · Ehara · Tamura …) และ v1.0/v1.1 ทับศัพท์เป็นไทยทั้งหมด
  → ทางแสดงผลของชื่อนี้วาดตัวอักษรไทยเป็น `?` เท่าจำนวนตัวอักษร (ชื่อไทย 6 ตัวอักษรก็ขึ้น `?` 6 ตัว)
- แก้: ล็อกกลุ่มข้อความนี้ให้คงโรมาจิเหมือนเวอร์ชันภาษาอังกฤษ (อ่านออกและผ่านทางแสดงผลแน่)
  และเพิ่มด่านตรวจใหม่ `check_locres_translated.py` ที่แตกไฟล์ที่บิลด์กลับมาเทียบทีละคีย์ว่ากลุ่มที่ล็อก
  ต้องเท่าต้นฉบับ ส่วนกลุ่มอื่นต้องตรงคำแปลใน master
- ตัวเลขของเวอร์ชันนี้: locres 21,920 คีย์ (ลดจาก 22,794 = 874 คีย์ที่ล็อก) · ส่วนอื่นเท่า v1.1
- ยังต้องดูในเกม: ชื่อคู่ต่อสู้ในสังเวียนต่อสู้ (`colosseum_participant_name`) และชื่อคนร้ายที่มีค่าหัว
  (`otazunemono_name`) ถ้าขึ้น `?` เหมือนกันจะล็อกเป็นโรมาจิด้วยวิธีเดียวกัน

---

## v1.1 — 4 กันยายน 2026 · แก้ภาพทิปส์เป็นสี่เหลี่ยมขาว · ชื่อศัตรูลูกกระจ๊อกขึ้นเป็น ??????

- **ภาพประกอบในจอทิปส์/บทสอน (หมวดการต่อสู้ · ผจญภัย · ไล่ล่า) เป็นสี่เหลี่ยมขาว**
  ต้นเหตุ: ตารางหมวดทิปส์ (`tips_category`) มีคอลัมน์ `directory_name` ค่า "Battle" / "Adventure" / "Chase"
  ซึ่งเป็น**ชื่อโฟลเดอร์**ที่เกมเอาไปประกอบพาธของภาพ (`UI/Textures/Tips/Battle/…`) แต่คำเดียวกันก็เป็นชื่อหมวด
  บนจอในคอลัมน์ข้าง ๆ ด้วย ม็อดจึงแปลทั้งสองช่อง → เกมหาโฟลเดอร์ "ต่อสู้" ไม่เจอ ภาพขึ้นขาว
  หมวด System / Minigame / Taishi / Toku / Another ไม่โดนเพราะคำในคอลัมน์นั้นไม่ได้แปล
- แก้: ล็อกคอลัมน์ `directory_name` ให้คงค่าเดิมของเกม (ชื่อหมวดบนจอยังเป็นไทย) และเพิ่มด่านตรวจว่าคอลัมน์ที่ล็อกต้องเท่าต้นฉบับ
- **ชื่อศัตรูลูกกระจ๊อกบนแถบพลังในการต่อสู้ขึ้นเป็น `??????`** (บอสและมินิบอสปกติ)
  ชื่อพวกนี้มาจากตารางนามสกุล (`stay_enemy_name_all`) ซึ่งในเกมต้นฉบับภาษาอังกฤษก็เป็น**คันจิ**อยู่แล้ว
  (ไฟล์ EN กับ JA เท่ากันทุกไบต์) v1.0 ทับศัพท์เป็นไทย 466 ชื่อ แต่ทางแสดงผลของชื่อนี้วาดตัวอักษรไทยเป็น `?`
  ทั้งที่ใช้ฟอนต์ตัวเดียวกับจอที่แสดงไทยได้ปกติ → v1.1 คืนตารางนี้เป็นค่าเดิมของเกม (นามสกุลคันจิเหมือน
  เวอร์ชันอังกฤษ) ไว้ก่อน จนกว่าจะมีทางใส่ชื่ออ่านออกที่ผ่านทางแสดงผลนี้ได้
- **ชื่อผู้พูดในกล่องบทพูดและตัวเลือกตอบเป็นภาษาอังกฤษ** (เช่น "Young Woman" · "Ryoma" · "Let's go.")
  ข้อความพวกนี้อยู่ในตาราง label ของไฟล์บทพูด ซึ่ง v1.0 ไม่ได้แตะเลย (แปลเฉพาะบรรทัดบทพูด)
  v1.1 ใส่คำแปลให้ label ที่เป็นข้อความบนจอและมีคำแปลอยู่แล้ว 365 คำ (2,172 ช่องใน 1,284 ไฟล์ · ชื่อตัวละครหลัก · ตัวเลือกตอบ)
  ส่วน label ที่ยังไม่มีคำแปลอีก ~1,000 คำ (ชาวเมืองทั่วไป · ชื่อ NPC รอง) ยังเป็นอังกฤษ รอแปลรอบถัดไป
- ตัวเลขของเวอร์ชันนี้: ตาราง ARMP 29 ตาราง · 11,761 ช่อง (ลดจาก v1.0 เพราะถอดตารางนามสกุลและคอลัมน์โฟลเดอร์) · ส่วนอื่นเท่าเดิม

---

## v1.0 — 3 กันยายน 2026 · เวอร์ชันแรก

ม็อดแปลภาษาไทยสำหรับ Like a Dragon: Ishin! (รีเมค 2023) เวอร์ชัน Steam (PC)
ติดตั้งเป็นไฟล์ `.pak` ไฟล์เดียวในโฟลเดอร์ `~mods` ของเกม ไม่เขียนทับไฟล์ต้นฉบับ ไม่ต้องใช้ตัวโหลดม็อด

### สิ่งที่แปล

| ชั้นข้อมูล | ที่แทนที่ในเกม |
|---|---:|
| บทพูดและคัตซีน (`.msg`) | 29,561 บรรทัด ใน 1,269 ไฟล์ |
| ตารางระบบและ UI (ARMP) | 12,230 ช่อง ใน 30 ตาราง |
| ข้อความ UI ของ Unreal (`Game.locres`) | 22,794 คีย์ |
| **ประโยคไม่ซ้ำที่แปลเป็นไทย** | **38,471** |
| ประโยคที่คงภาษาอังกฤษโดยตั้งใจ | 3,559 |

ครอบคลุมเนื้อเรื่องหลักทุกบท · เรื่องราวข้างทาง · บทสนทนาในเมือง · เมนูและหน้าจอระบบทั้งหมด ·
ทิปส์และบทสอน (344 หัวข้อ รวมมาจง โชกิ โคอิ-โคอิ โออิโช-คาบุ ซีโล โชฮัง โป๊กเกอร์) ·
ตีเหล็กและตรา · ทหารหน่วย · ไร่และบ้านหลังที่สอง · สติกเกอร์โหมดถ่ายภาพ

คงภาษาอังกฤษโดยตั้งใจ: license / EULA / เครดิตท้ายเกม · รหัสและชื่อแอสเซตที่ปนอยู่ในตารางข้อความ
(แปลแล้วเกมจะหาไฟล์ไม่เจอ) · กล่องข้อความของ Windows

### ฟอนต์

ใส่ฟอนต์ Sarabun ทับ FontFace ละตินของเกมสามตัว (`Kuro-Medium` · `edosz` · `FOT-TelopMinProN-D`)
ซึ่งเป็นฟอนต์ที่ภาษาอังกฤษ/ฝรั่งเศส/อิตาลี/เยอรมัน/สเปนของเกมชี้ไปหา ตัวอักษรญี่ปุ่นในจอที่ปนกันยังแสดงได้ตามเดิม
ฟอนต์ถูกแก้สองอย่างก่อนใส่:

- ปรับค่าความสูงของฟอนต์ให้เกมเว้นที่พอสำหรับวรรณยุกต์และสระบน ไม่ถูกตัดหัว (เช่นไม้โทบนคำว่า "งั้น")
- เพิ่มตารางแปลงตัวอักษรเต็มหลักของญี่ปุ่นที่โผล่ในข้อความอังกฤษของเกม (`？` `！` `。` `、` ช่องว่างเต็มหลัก
  และตัวอักษร/ตัวเลขเต็มหลัก รวม 104 ตัว) ให้ชี้ไปกลิฟปกติ ไม่ขึ้นเป็นกล่องว่าง

### ปัญหาที่พบระหว่างทดสอบในเกมและแก้แล้วก่อนปล่อย

- **ข้อความไทยถูกตัดท้ายประโยค** เช่น "ไม่ได้เจอกันนานเลยนะ ซากาโมโตะซัง" แสดงแค่ 28 ตัวอักษรเท่าประโยคอังกฤษ
  สาเหตุคือไฟล์บทพูดเก็บตำแหน่งตัวอักษรของจุดจบบรรทัดและจังหวะพิมพ์ไว้ในบล็อกคำสั่ง โดยนับจากประโยคอังกฤษ
  ตอนนี้คำนวณตำแหน่งใหม่ตามความยาวข้อความไทยทุกบรรทัด และกวาดยืนยันแล้วว่าไม่มีฟิลด์ลักษณะเดียวกันซ่อนอยู่ในชั้นอื่น
- **ข้อความขาดกลางประโยค กล่องบทพูดว่าง กล้องคัตซีนค้าง** เพราะตัวเขียนไฟล์บทพูดเขียนตารางพอยเตอร์ทับข้อความของตัวเอง
  เมื่อข้อความไทยยาวกว่าเดิม แก้ที่ตัวเขียนแล้ว และมีด่านตรวจเทียบไบต์กับต้นฉบับทุกครั้งก่อนบิลด์
- **จอทิปส์เป็นอังกฤษทั้งที่หัวข้อเป็นไทย** เพราะเนื้อหาทิปส์อยู่ในตารางที่ซ้อนอยู่ในตารางอีกชั้น ดึงเข้าคิวแปลครบแล้ว
- **การ์ดทหารหน่วยที่ยังไม่ปลดล็อกขึ้นเป็นกล่องมีเครื่องหมายคำถาม** เพราะเกมใช้ `？` เต็มหลักที่ Sarabun ไม่มี
  แก้ด้วยตารางแปลงตัวอักษรในฟอนต์ตามหัวข้อฟอนต์ข้างบน

### ข้อจำกัดของเวอร์ชันนี้

- **เสียงตะโกนของ NPC ในเมือง** (ป้ายลอยหัว เช่นคนเรียกรับจ้างส่งของ) และ **ป้าย "Pray" หน้าศาลเจ้า** ยังเป็นอังกฤษ
  ข้อความชุดนี้ราว 70 ประโยคฝังอยู่ในไฟล์วางวัตถุประจำฉาก ยังไม่ได้แกะฟอร์แมต
- ป้ายที่เกมวาดด้วยฟอนต์พู่กันญี่ปุ่น (ชื่อบนการ์ดทหารหน่วย · สแตมป์โหมดถ่ายภาพ · หัวข้อบางจอ) แสดงไทยไม่ได้
  เพราะฟอนต์ชุดนั้นไม่มีช่องละตินให้ทับ
- ยังไม่ผ่านการเล่นจบเกมด้วยม็อดนี้ ช่วงท้ายเกมและจอทิปส์ทั้ง 344 หัวข้อยังไม่มีใครเห็นบนจอจริง
  เจอข้อความเพี้ยน ข้อความขาด หรือเกมค้าง ช่วยแจ้งพร้อมภาพหน้าจอ

### สิ่งที่ไม่ใช่บั๊กของม็อด

- **ปุ่ม ESC ไม่เปิดเมนูหยุดเกม** — เกมต้นฉบับผูกเมนูไว้กับปุ่ม `M` และไม่ยอมให้ตั้ง ESC เป็นปุ่มเมนู
  ม็อดไม่มีไฟล์ตั้งค่าปุ่มอยู่เลย
- **ชื่อการ์ดทหารหน่วยขึ้น `???`** — เป็นการ์ดที่ยังไม่ปลดล็อก เกมต้นฉบับก็แสดงแบบเดียวกัน
- **บอลลูน NPC เป็นจุดไข่ปลา** — บรรทัดของเกมต้นฉบับเป็น `......` อยู่แล้ว ม็อดไม่ได้แตะ

### ความปลอดภัยของไฟล์เกม

ม็อดอยู่นอกรายการไฟล์ของ Steam จึงไม่ถูกลบตอน "ตรวจสอบความสมบูรณ์ของไฟล์เกม" และไฟล์ต้นฉบับไม่เคยถูกแก้
ถอนม็อดได้ด้วยการลบ `~mods\IshinThai_P.pak` ไฟล์เดียว
