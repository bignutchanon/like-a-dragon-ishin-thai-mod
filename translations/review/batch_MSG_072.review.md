# batch_MSG_072 — รีวิว (sprint 16)

ชั้นงาน: บทสนทนา `.msg` · **250 คีย์ · แปลจริง 226 · DNT 24**
เนื้อหาหลัก: บทร้านค้าเร่/นักเลงข้างถนน (0–49) + **ซับสตอรี่ "ลูกโลกที่โรงเรียนวัด"** (50–225)
ซึ่งเป็นบทญี่ปุ่นล้วนที่ทีมอังกฤษแปลตกไว้ → แปลไทยตาม cheatsheet §4.3

## ผลตรวจ
`merge_qc.py --dry-run` **ตก 0 · ขาด 0 · คง EN 24** (= DNT ครบพอดี)
· `check_thai_marks` สะอาด · `check_glossary_locks` ไม่พบคำล็อกที่หายไป
· `check_pronoun_pairs` 0 · `check_cross_batch` 0

**ผู้ตรวจแก้ 1 คีย์** — เลขเต็มความกว้างปนกับอักษรไทย
`ประวัติศาสตร์ยาวนาน ４０００ ปี` → **`4000`**
ตัวเลข: `master_th.json` มี **0 บรรทัด** ที่ผสมอักษรไทยกับเลขเต็มความกว้าง
(เลขเต็มความกว้าง 165 บรรทัดในคลังเป็นสตริงญี่ปุ่น DNT ทั้งหมด) · สแกน 750 คีย์ทั้งคลื่นแล้วเจอจุดเดียว

## คำที่ตั้งรูปใหม่
| ต้นฉบับ | ไทย |
|---|---|
| `清国` | **แผ่นดินชิง** (master มีแต่ `Qing Dynasty` = ราชวงศ์ชิง) |
| `和蘭陀（オランダ）` | **ฮอลันดา** — ตัดวงเล็บอ่านออกตามธรรมเนียมคลัง (JA มี `（）` 1,580 คีย์ · ไทยคงไว้ **0**) |
| `神州` · `「眠れる獅子」` · `勤王志士` | **แผ่นดินศักดิ์สิทธิ์** · **สิงโตหลับ** · **ชิชิผู้จงรักภักดี** |

ยึด master: `地球儀`=ลูกโลก · `寺子屋`=โรงเรียนวัด · `親っさん`=ท่านลุง · `南蛮渡来の反物`=**ผ้าจากยุโรป**
(ยืนยันจาก locres `item_name::…nannbanntorainotannmono` EN = "European Fabric") · `黒船`=เรือดำ
· `出島`=เดจิมะ · `ダンナ`=นายท่าน · เทมเพลตนักเลงยึดรูปของ MSG_024

## เพศผู้พูด
ใส่ "ขอรับ" **7 บรรทัด** ทุกบรรทัด `ref_ja` มี **ワシ ของตัวเอง** + รูป ですます (ชั้น 2)
· ไม่ยืมเพศจากฉาก · ไม่แตะ `gender_lines.json`

⚠ นักแปลเสนอลง `gender_lines.json` ให้บทเด็กหัวโจก (`僕` + です · ~40 คีย์ในฉาก `uid01400014`)
lead **ไม่รับยกชุด** — คีย์ที่มี `僕` ในบรรทัดตัวเองมีหลักฐานชั้น 2 อยู่แล้วไม่ต้องล็อก
ส่วนคีย์อื่นของฉากไม่มีหลักฐานว่าใครพูด

## ⚠ ปัญหาข้ามก้อนที่ก้อนนี้ทำให้เจอ — **ยังไม่แก้**

ซับสตอรี่ "ลูกโลก" เรื่องเดียวกันนี้ **ถูกแบ่งไปอยู่ `batch_MSG_024` ด้วย 186 คีย์
ซึ่งถูกตีเป็น DNT ทั้งหมด** (คงต้นฉบับญี่ปุ่นไว้) → ผู้เล่นจะเห็นเรื่องเดียวกันครึ่งญี่ปุ่นครึ่งไทย

lead ตรวจแล้วว่า:
- คีย์ทั้ง 186 อยู่ในไฟล์ `batch_MSG_024.dnt.json` จริง (ไม่ใช่ความผิดพลาดของผู้แปล)
- เป็นบทของ Ishin จริง (困ったことになった / 地球儀の見方 / じいさん) ไม่ใช่บทตกค้างจากเกมอื่น
- **ต่างจาก MSG_066–071** (1,500 คีย์ DNT) ซึ่งตรวจแล้วเป็นบทโฮสเตสคลับของ Yakuza
  (`桐生さん` · `韓流ブーム` · `サウナ` · `セバスチャン`) เข้าไม่ถึงใน Ishin จริง ๆ → DNT ถูกต้อง

→ **งานค้างที่ต้องทำก่อนปล่อย: รื้อ DNT ของ `MSG_024` แล้วแปล 186 คีย์นั้น**

## จุดที่ตรวจแล้วว่าถูก
- **จุดไข่ปลา 3 จุด** — วัดบรรทัดที่ `ref_ja` มี `……` ได้ **3 จุด 5,750 : 6 จุด 9**
  (ไฟล์ `prior.json` ที่เสนอรูป 6 จุดเป็นหนึ่งใน 9 จุดที่ผิด) · lead ยืนยันรูป 3 จุด
- `อย่างไร` 2 จุดในก้อนนี้ **คงไว้** — เป็นบทเด็กนักเรียนที่พูดสุภาพ (`私`/`ですか`)
  ต่างจากบริวารซานาดะใน MSG_064 ที่ EN ลำลอง
- คำเตือน L 66 จุด — ต้นฉบับเป็นคันจิล้วนสั้นมาก ไทยยาวกว่า 1.8× โดยธรรมชาติ
  จำนวนบรรทัดเท่าต้นฉบับทุกคีย์
