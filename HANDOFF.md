# HANDOFF — ISHTH (Like a Dragon: Ishin! แปลไทย)

> อ่านคู่กับ `CLAUDE.md` (กติกา) · `docs/research.md` (ข้อเท็จจริงจากไฟล์เกม) · `docs/scope.md` (ขนาดงาน)

**อัปเดตล่าสุด: 3 ก.ย. 2026 — sprint 17 (⭐ ทดสอบในเกมครั้งแรก · ไทยขึ้นจริงทั้งเกม · แก้บั๊กตัวเขียนไปสามตัว)**

> **ม็อดขึ้นไทยบนจอจริงแล้ว** — จอไตเติล บทสนทนา บอลลูน NPC เมนู ทิปส์ ทั้งหมดเป็นไทย
> รอบนี้คือรอบแรกที่มีคนเปิดเกมจริง และมันทำให้เจอบั๊กที่ด่านเดิม **รายงานว่าผ่านทั้งที่ไฟล์พัง**
> เรื่องที่ยังไม่จบมีเรื่องเดียว: **ปุ่ม ESC บนคีย์บอร์ดไม่ทำงาน** (ยังไม่รู้ว่าเกี่ยวกับม็อดหรือไม่ · §0.5)

---

## 0. สถานะ ณ ตอนปิด sprint 17 — **อ่านก่อน**

### 0.1 ม็อดที่ติดตั้งอยู่ตอนนี้

```
E:\SteamLibrary\...\LikeaDragonIshin\Content\Paks\~mods\IshinThai_P.pak      = ปล่อยจริง v1.0 (3 ก.ย. 2026 ดึก)
1,303 ไฟล์ · 20.4 MB · เนื้อเดียวกับ Repak7 ยกเว้น 2 ไฟล์ POC label "Young Woman" ที่ไม่ได้ใส่ (uid00021d6c · uid00780043)
```

**ปล่อยแล้ว: GitHub `bignutchanon/like-a-dragon-ishin-thai-mod` release `v1.0`** — `release/LikeADragonIshinThai-v1.0.zip`
สคริปต์ปล่อย: `python scripts/pack_release.py --version vX.Y [--install]` = §0.6 ขั้น repak ทั้งสี่ข้อ + ประกอบ zip
(files/IshinThai_P.pak + packaging/install·uninstall + README.txt + patch.md) · บันทึกเวอร์ชันอยู่ที่ `patch.md` ราก
ตัวติดตั้ง `packaging/install.ps1` เขียนใหม่สำหรับภาคนี้แล้ว (วาง pak ลง ~mods · ลบ IshinThai*_P.pak เก่า · ไม่แตะไฟล์เกม)
ชื่อ pak ที่ผู้ใช้ปลายทางเห็น = `IshinThai_P.pak` คงที่ทุกเวอร์ชัน · ชื่อ RepakN ใช้เฉพาะรอบทดสอบภายใน

⚠ **ชื่อไฟล์เปลี่ยนแล้ว** จาก `LikeADragonIshinThai_P.pak` เป็น `IshinThaiRepak2_P.pak`
ถ้าจะถอดม็อด ลบไฟล์นี้ไฟล์เดียว · ไฟล์เกมต้นฉบับไม่เคยถูกแตะ

### 0.2 ⭐ ตอบคำถามเปิดข้อใหญ่ที่สุดได้แล้ว — **pak ที่เราเขียนเองเกมไม่โหลด**

| ตัวแพ็ก | path hash seed | ผลในเกม |
|---|---|---|
| `tools/pakwrite.py` (ของเราเอง) | `1234ABCD` | **จอไตเติลเป็นอังกฤษล้วน — เกมไม่โหลด pak** |
| `tools/repak/repak.exe` | `00000000` | **ไทยขึ้นทั้งเกม** |

เนื้อ pak ชุดเดียวกันเป๊ะทั้งสองอัน (เทียบไบต์ทุกไฟล์ ต่าง 0) → ตัวแปรเดียวคือตัวแพ็ก
**จนกว่าจะไล่เจอสาเหตุใน `pakwrite.py` ให้แพ็กด้วย repak เท่านั้น** ขั้นตอนอยู่ใน §0.6

### 0.3 ⭐ บั๊กตัวเขียนที่การทดสอบในเกมทำให้เจอ (แก้ครบทั้งสามแล้ว)

| # | บั๊ก | อาการบนจอ | ที่วัดได้ | ที่แก้ |
|---|---|---|---|---|
| 1 | **ตัวเขียน `.msg` ทับข้อความตัวเอง** — ตารางพอยเตอร์ label นอนอยู่**กลาง**บล็อกสตริง โค้ดเขียนพอยเตอร์กลับที่ตำแหน่งเดิมทั้งที่บล็อกขยายแล้ว | ข้อความขาดกลางประโยค · กล่องบทพูดขึ้นชื่อผู้พูดแต่เนื้อว่าง · **กล้องคัตซีนค้างหลังฉาก** (label ฉาก/ท่าทางพังไปด้วย) | เสีย **1,418 บรรทัดใน 1,017 ไฟล์** | `tools/msg.py` `rebuild()` — ยกตารางพอยเตอร์มาทั้งก้อน + ทำแผนที่ออฟเซ็ต `_map()` ให้ทุกฟิลด์ในหัวไฟล์ · **ยกเว้น `header[3]` ที่เป็นจำนวน label ไม่ใช่ออฟเซ็ต** |
| 2 | **ไฟล์ค้างจากบิลด์ทดสอบหลุดเข้าม็อด** — ตัวบิลด์เขียนทับเฉพาะไฟล์ที่มันสร้างรอบนั้น | `battle_bomb_info.bin` มี asset id เป็น `ทดสอบไทย wepct9000` ทั้งตาราง · `.msg` สองไฟล์มีคำว่า "ทดสอบไทย" นำหน้า | ไฟล์ลงวันที่ 2 ก.ย. 06:18 ค้างมา 3 บิลด์ | `scripts/build_text.py` ล้างโฟลเดอร์ stage ของชั้นที่จะบิลด์ทุกครั้ง |
| 3 | **ตาราง ARMP ที่ซ้อนในแถวไม่เคยถูกแปล** — ข้อความจริงของ `tips` อยู่ใน `row["table"]` อีกชั้น | จอ "ทิปส์และสมุดบันทึก" หัวข้อเป็นไทยแต่เนื้อเป็นอังกฤษ | 968 ช่องในชั้นซ้อน (ไม่เคยเข้าคิวแปลเลย) | `build_text._replace_table()` เดินชั้นซ้อน + `scripts/make_worklist_db_nested.py` ดึงเข้าคิว |

### 0.4 ⭐ ด่านใหม่สองตัว — **ต้องรันหลังบิลด์ทุกครั้ง**

ด่านเดิม (`check_msg_roundtrip` · `check_layout_all`) ประกอบไฟล์กลับแบบ**ไม่แทนที่อะไรเลย**
จึงมองไม่เห็นบั๊กที่โผล่เฉพาะตอนสตริงยาวขึ้น — ทั้งสามบั๊กใน §0.3 ผ่านด่านเดิมหมด

| ด่าน | ตรวจอะไร | ผลล่าสุด |
|---|---|---|
| `scripts/check_msg_translated.py` | เปิดไฟล์ `.msg` ที่บิลด์จริง เทียบทุกบรรทัดกับ `master_th.json` + เทียบ **ตาราง label และบล็อกคำสั่ง** กับ vanilla | 1,269 ไฟล์ · 52,569 บรรทัด · **ต่าง 0** |
| `scripts/check_armp_translated.py` | แตก `.bin` ที่บิลด์กลับมาเทียบทีละช่อง — ช่องข้อความต้องตรงคำแปล **ช่องที่ไม่ใช่ข้อความต้องเท่า vanilla เป๊ะ** | 30 ตาราง · 54,695 ช่อง · **ต่าง 0** |

⚠ `check_armp_translated` **ข้ามคอลัมน์ที่ `shift < 0`** (ไม่ได้เก็บไว้ในแถว) โดยตั้งใจ
เคสจริง: `sound_speak_data` คอลัมน์ `*` อ่านกลับได้ 3840 แทน 512 ทั้งที่ไบต์ในแถวตรง vanilla
4,044/4,045 แถว · aux/types/layout ตรงหมด = เป็นค่าที่ตัวอ่านสังเคราะห์เอง ไม่ใช่ข้อมูลพัง
(เคย deny ตารางนี้ไปหนึ่งรอบเพราะเข้าใจผิด แล้วบทพูด NPC หายเป็นอังกฤษ 8,068 ช่อง — **อย่าทำซ้ำ**)

### 0.45 ⭐ ฟอนต์: ทับ `Kuro-Medium` ตัวเดียวไม่พอ (3 ก.ย. 2026 · รอบภาพหน้าจอจากผู้ใช้)

แตก `Font_*.uasset` ครบ 21 ตัวแล้วไล่ว่า culture `en;fr;it;de;es` ชี้ไป FontFace ไหน → ได้ **สามตัว**
`EFIGS/Kuro-Medium` (ทับแล้ว) · `EFIGS/edosz` · `EFIGS/FOT-TelopMinProN-D` (สองตัวหลังไม่เคยทับ)
และตรวจ `cmap` ของ `.ufont` ทั้ง 33 ไฟล์ในเกม — **ไม่มีตัวไหนมีกลิฟไทย** (มีแต่ ฿ ใน DroidSansFallback)
→ จอที่ใช้ `Font_CmnFude` · `Font_MgTaishiFude/Kaisho` · `Font_MgKswKaisho` · `Font_MgPhotoModeStamp`
  จะแสดงไทยไม่ได้ · รายละเอียด/ตาราง: `docs/research.md` §5.1

แก้แล้ว: `build_text.py` → `FONT_GAME_PATHS` (3 รายการ) · แพ็กใหม่เป็น
**`build/IshinThaiRepak3_P.pak`** (1,303 ไฟล์ · 20.4 MB) — เทียบกับ Repak2 แล้ว **ต่างแค่ฟอนต์ 2 ไฟล์
ที่เพิ่มเข้ามา ไฟล์เดิม 1,301 ตรงกันหมด** · เทียบไบต์กับ `stage_pak` ต่าง 0

⚠ ยังเปิดอยู่: `Font_CmnMincho` · `Font_MacanNum` ไม่มี subfont EFIGS เลย ตกไปที่ฟอนต์ญี่ปุ่น
(`DF-FutoKaiSho-W9` · `Myfont_fude-Regular`) ที่ไม่มีไทย — ยังไม่ทับเพราะจะเสียคันจิ ต้องหาก่อนว่าจอไหนใช้

### 0.46 สองอาการที่ผู้ใช้ส่งภาพมา — **ไม่ใช่บั๊กของม็อด** (พิสูจน์แล้ว)

- การ์ดทหารหน่วยขึ้น `???????` = ป้ายชื่อการ์ดที่ยังไม่ปลดล็อก · เกมแท้มี `???`/`?????`/`??????`
  ใน `Game.en.locres` อยู่แล้ว (ฝั่ง JA เป็น `？？？　天狗面の男`)
- บอลลูน NPC เป็นจุดไข่ปลา = บรรทัด `......`/`…………` ของเกมแท้ · **ไม่มีบรรทัดจุดล้วนใน `master_th.json`**
  แปลว่าม็อดไม่เคยแตะบรรทัดพวกนี้

### 0.47 ⭐ รอบทดสอบที่สาม (3 ก.ย. 2026 ค่ำ) — ฟอนต์ crop วรรณยุกต์ · "Pray"/"Young Woman" เป็น label

**1. วรรณยุกต์ที่ซ้อนสระโดน crop ขอบบน** ("งั้น" ไม้โทหาย)
สาเหตุวัดจากไฟล์: Sarabun วางวรรณยุกต์ตัวเล็ก (`uni0E49.small` ฯลฯ) สูงถึง y=1265 แต่ ascender ของฟอนต์
(hhea/typo) = 1068 → Slate คิดความสูงบรรทัดจาก ascender แล้ว clip ส่วนที่โผล่พ้น
แก้: `font/Sarabun-Regular-ishin.ttf` = Sarabun เดิมแก้ metric แนวตั้ง ascender 1068→1290 · descender -232→-350
(fontTools · cmap/กลิฟไม่แตะ) · `paths.SARABUN_TTF` ชี้ไฟล์นี้แล้ว · เทียบ Kuro-Medium ต้นฉบับ 972/-258
⚠ ความสูงบรรทัดโต +26% → ถ้ากล่องไหนข้อความล้นล่าง/บรรทัดห่างผิดปกติ ให้ลดเหลือ 1280/-340 หรือย่อกลิฟแทน

**2. "Pray" (ป้ายปุ่มไหว้ศาลเจ้า) · "Young Woman" (ชื่อผู้พูด) ยังเป็นอังกฤษ**
ไม่ได้อยู่ในบรรทัดบทพูด แต่อยู่ใน **ตาราง label ของ .msg** (`labels: ["dummy","Pray"]` · บรรทัด `en` ว่าง)
pipeline ไม่เคยดึง label เข้าคิวแปล และ `rebuild()` ไม่เคยเขียน label ใหม่
ตอนนี้: `MsgFile.rebuild(repl, label_replacements={label เดิม: ใหม่})` รองรับแล้ว (roundtrip 1,678 ไฟล์ ต่าง 0)
`scripts/make_label_poc.py` แปล label ทดสอบ 2 คำ (`Pray→ไหว้พระ` · `Young Woman→หญิงสาว`) ใน 17 ไฟล์
ลง `build/stage_pak/` ตรง ๆ (ไม่ผ่าน master · ไม่แตะ `build/text/msg`) → ถ้าในเกมขึ้นไทย = label เป็นข้อความบนจอล้วน
ขยายผลได้ · ถ้าปุ่มหาย/ชื่อว่าง = เกมใช้ label เป็นคีย์ด้วย ห้ามแปล
ขนาดงานถ้าขยายผล: label หน้าตาเหมือนข้อความ (ไม่ใช่ `snake_case`/`UPPER`/`dummy`) **687 คำ** · อยู่ใน master แล้ว 157
(ชื่อตัวละคร · ป้ายปุ่ม · ตัวเลือกเมนู เช่น "Let's hear it." "Visit the blacksmith") — ต้องกรอง `ArmsID` · `龍馬` ฯลฯ ออก
⚠ `check_msg_translated.py` เทียบตาราง label กับ vanilla → 17 ไฟล์ POC จะ "ต่าง" โดยตั้งใจ ถ้ารันหลัง POC

**3. การ์ดทหารหน่วย `???????` — ครึ่งหนึ่งเป็นความผิดเรา** (แก้แล้ว · Repak5)
ป้ายชื่อการ์ดที่ยังไม่ปลดล็อกเป็น `？` **เต็มหลัก (U+FF1F)** ซึ่ง Sarabun ไม่มีกลิฟ → เกมวาด `.notdef` ของ Sarabun
ซึ่งหน้าตาเป็น "กล่องมีเครื่องหมายคำถาม" เลยดูเหมือนฟอนต์พัง · กวาดข้อความ EN+ไทยทั้งเกมพบตัวอักษรนอก
Sarabun ที่ไม่ใช่ญี่ปุ่น 83 ตัว (U+FF1F 1,422 ครั้ง · `。` 5,213 · `、` 3,717 · ช่องว่างเต็มหลัก U+3000 1,845 · `！` 839 ·
ตัวอักษร/เลขเต็มหลัก Ａ-Ｚ ０-９ ฯลฯ) → `scripts/make_thai_font.py` ใส่ cmap alias 104 ตัวชี้ไปกลิฟ ASCII/วรรคตอนที่มีอยู่
(＿ U+FF01-FF5E → ASCII · 。→. · 、→, · 「」『』→“” · ー→— · U+3000→space) — ยังไม่ครอบ ★●♪※①② และฮิรางานะ/คันจิ
ฟอนต์ที่ใช้จริงตอนนี้สร้างจากสคริปต์นี้เท่านั้น (`font/Sarabun-Regular-ishin.ttf` = ผลลัพธ์ ห้ามแก้มือ)

**4. ⭐ "text ขาด" ต้นตอจริง — บล็อกคำสั่งของ .msg เก็บตำแหน่งตัวอักษรที่คิดจากประโยคอังกฤษ** (แก้แล้ว · Repak6)
"ไม่ได้เจอกันนานเลยนะ ซากาโมโตะซัง" (33 ตัว) โชว์แค่ 28 ตัว = ความยาว "Been a minute, Sakamoto-san." เป๊ะ
พิสูจน์จากไฟล์: ทุกคำสั่ง 16 ไบต์ในบล็อกคำสั่งเก็บ **ตำแหน่งตัวอักษร** ที่ไบต์ [6:8] (0 = ไม่ใช้) —
EN 28 ตัว → 28 · JA `久しぶりやね、坂本さん。` 12 ตัว/36 ไบต์ → 12 (นับเป็นตัว ไม่ใช่ไบต์ · `
` นับหนึ่ง)
· กวาด 32,806 บรรทัด **ไม่มีชนิดคำสั่งไหนมีค่าเกินจำนวนตัวอักษร** · `01 01` (ปิดบรรทัด) และ `02 09` เท่าความยาว
28,000+ ครั้ง · เกมแสดง/เล่นเสียงถึงตำแหน่งนั้นแล้วหยุด → ไทยที่นับตัวอักษรยาวกว่า EN ถูกตัดท้าย **ทุกบรรทัด**
แก้: `msg.retime_cmds()` — จุดจบ = จำนวนตัวอักษรไทย · จุดกลาง (จังหวะพิมพ์/หยุด/เน้น) สเกลตามสัดส่วน ·
`rebuild()` เรียกให้เองกับบรรทัดที่แทนที่ · `check_msg_translated.py` เทียบบล็อกคำสั่งกับ vanilla+retime แล้ว
(ผล: 1,269 ไฟล์ · 52,569 บรรทัด · ต่าง 0 · roundtrip 1,678 ไฟล์ ต่าง 0) · .msg เปลี่ยน 1,253 ไฟล์เทียบ Repak5
⚠ สมมติฐานที่ยังไม่ยืนยันในเกม: ค่ากลางบรรทัดของ (02,07)/(02,08)/(02,16)/(02,17)/(02,19) ถูกสเกลด้วย
(ไม่เคยมีค่าเกินความยาว จึงถือเป็นตำแหน่ง) ถ้าพบจังหวะพิมพ์/เสียงเพี้ยนให้ยกเว้นชนิดพวกนี้ใน `retime_cmds`
✅ **กวาดหาฟิลด์ชนิดเดียวกันในชั้นอื่นแล้ว — ไม่มี** (`scripts/sweep_len_fields.py` · 3 ก.ย. 2026 ดึก)
เทียบ EN กับ JA ไฟล์เดียวกัน: ฟิลด์ที่ผูกความยาวข้อความต้องต่างระหว่างสองภาษาและเท่ากับ/สเกลตามจำนวนตัวอักษร
· .msg 1,678 ไฟล์ · 54,318 บรรทัด · ทุกฟิลด์ 2 ไบต์ในคำสั่ง 16 ไบต์ → มีเฉพาะ offset 6 ที่เท่ากับจำนวนตัวอักษร (retime จัดการแล้ว)
  ฟิลด์อื่นที่ต่าง EN/JA ((02,09) off 2 = หน่วง 10/20 · (02,00) off 2/4/10) ไม่แปรตามความยาว
· ARMP 30 ตารางที่แปล (รวมชั้นซ้อน) → คอลัมน์ที่ไม่ใช่ข้อความ EN = JA ทุกช่อง ยกเว้น `*` ของ sound_speak_data (§0.4)
· locres → มีแค่ length prefix ที่ตัวเขียนคำนวณใหม่อยู่แล้ว

**5. "Pray" ไม่ได้มาจาก .msg — มาจาก `wdr_en/pac/pac_STID_ST_*.bin` (ไฟล์วางวัตถุประจำฉาก) · ยังแปลไม่ได้** (Repak7)
ทดสอบแปล label "Pray" ใน .msg 15 ไฟล์แล้วจอไม่เปลี่ยน (ผู้ใช้ปิด-เปิดเกมใหม่แล้ว) → กวาด pak0 ทั้งหมด พบสตริง
`Pray ` ฝังใน `pac_STID_ST_{TOSA,KYOTO,GION,RINDOU,RYOMA_IE}.bin` ที่ตำแหน่งเดียวกับ `参拝` ในไฟล์ JA
(สตริงแสดงผล ไม่ใช่คีย์) · ในไฟล์เดียวกันยังมี**เสียงตะโกน NPC ในฉาก** ฝังอยู่ด้วย
("Calling all couriers!" · "Dull sword keeping you down?" · "Up for a palanquin ride?" ฯลฯ) ≈ 70 สตริงใน 168 ไฟล์
ฟอร์แมต: สตริงยาวไม่คงที่ (EN/JA ขนาดไฟล์ต่างกัน 40/168 ไฟล์) + ตารางออฟเซ็ต/ขนาดในส่วนหัว
(TOSA: EN 77,440 / JA 77,476 ไบต์ · จุดต่างแรก @0x353 เป็นคู่ offset+size ขยับ 4) → ต้องแกะฟอร์แมตก่อนแปล
**ยังไม่ทำ** — เป็นชั้นใหม่ (pac) ให้ผู้ใช้ตัดสินว่าคุ้มไหม · ถอด "Pray" ออกจาก `make_label_poc.py` แล้ว (label ไม่ใช่แหล่งแสดงผล)

**6. การ์ดทหารหน่วย `???????` (กล่องขาวมี ?)** — **ไม่ใช่ .notdef ของ Sarabun** (เรนเดอร์จริงแล้ว .notdef ของ Sarabun เป็น
กรอบสี่เหลี่ยมโปร่ง ไม่มี ?) และไม่ใช่ .notdef ของฟอนต์ต้นฉบับตัวไหน (Kuro ว่าง · TelopMin กากบาท · DF/Ksw "?") ·
มี texture `UI/Minigame/Taishi/TaishiCard/Textures/T_UI_TaishiCard_NameBrush` = แผ่นพู่กันขาวรองชื่อทีละตัว
→ น่าจะเป็นดีไซน์ของเกม (ชื่อยังไม่ปลดล็อก = "?" บนแผ่นขาว) · ยืนยันได้ทางเดียวคือดูจอนี้ในเกมเปล่า

### 0.5 ~~ปุ่ม ESC บนคีย์บอร์ดไม่ทำงาน~~ → **ไม่ใช่บั๊กม็อด — เกมไม่ได้ผูก ESC กับเมนูหยุดเกมตั้งแต่ต้น** (3 ก.ย. 2026)

ผลไล่ตรวจ:
- ฝั่งไฟล์ม็อด: locres 23,507 entry · hash namespace/key ตรง vanilla ทุกตัว · สตริงชื่อปุ่ม (`Escape` · `Start` ·
  `Back` · `Menu`) ใน locres **คงอังกฤษ** · ตาราง ARMP 30 ตัวใน pak ไม่มีคอลัมน์ชื่อคีย์ (`option.bin` ไม่มีคำว่า
  esc/keyboard เลย) · `input_key_texture_map` / `ui_texture_input_device_remapper` **ไม่อยู่ใน pak**
  → ไม่มีชั้นไหนของม็อดที่แตะการผูกปุ่มได้
- ฝั่งเกม: ปุ่มเปิดเมนูหยุดเกมบนคีย์บอร์ดค่าเริ่มต้นคือ **`M`** (ไม่ใช่ ESC) และผู้เล่นใน Steam รายงานตรงกันว่า
  ESC **ตั้งเป็นปุ่มเมนูไม่ได้** — กดตั้งแล้วเกมตีความเป็น "ยกเลิก" ปิดหน้าตั้งค่าทันที บางคนเลี่ยงไปใช้ `` ` ``
  (ภาคอื่นของ RGG ผูก ESC ได้ ภาคนี้ไม่ได้ · คนละพฤติกรรมกับที่ผู้ใช้คุ้น)

ที่ยังไม่ได้ทำ (ถ้าอยากยืนยัน 100%): ถอด pak แล้วกด ESC ในเกมเปล่า — คาดว่าไม่ขึ้นเหมือนเดิม

<details><summary>บันทึกเดิมของ §0.5 (ก่อนไล่ตรวจ)</summary>


อาการที่ผู้ใช้รายงาน: กด ESC แล้วไม่ขึ้นเมนูหยุดเกม สกิปคัตซีนไม่ได้ · **แต่จอยใช้ได้ปกติ**

สิ่งที่ยืนยันไปแล้ว: เนื้อ pak มีแค่ `.msg` 1,269 · ตาราง ARMP 30 (เป็นตารางข้อความล้วน) ·
`Game.locres` 1 · ฟอนต์ 1 — **ไม่มีไฟล์ตั้งค่าปุ่ม ไม่มี `.ini` ไม่มี uasset ของ PlayerController**
และ `option.bin` (ป้ายข้อความในเมนูตั้งค่า) ตรวจแล้วไบต์ที่ไม่ใช่ข้อความตรง vanilla ทุกช่อง

**ขั้นตอนตัดตัวแปรที่ตกลงกับผู้ใช้ไว้ (ยังไม่ได้ทำ)**
1. ถอด `IshinThaiRepak2_P.pak` ออกจาก `~mods/` แล้วให้ผู้ใช้กด ESC ในเกมเปล่าหนึ่งครั้ง
   - กดได้ = เป็นม็อดจริง → ไล่ทีละชั้นด้วย `build_text.py --layers locres` → `+msg` → `+armp`
   - กดไม่ได้เหมือนเดิม = ไม่ใช่ม็อด (โหมดอินพุตของเกม/Steam Input/โอเวอร์เลย์)
2. ทางที่ยังไม่ได้ลองฝั่งผู้ใช้: กดปุ่มคีย์บอร์ดอื่นก่อนหนึ่งครั้งให้เกมสลับโหมดอินพุต ·
   ปิด Steam Input · ถอดจอย

</details>

### 0.6 ขั้นตอนบิลด์ที่ใช้อยู่ตอนนี้ (ทำตามนี้ทุกครั้ง)

```
python scripts/merge_qc.py --dry-run            ต้อง ตก 0
python scripts/check_thai_marks.py · check_bond_names.py · check_gender_lines.py · test_qc_tools.py
python scripts/merge_qc.py                      เขียน master
python scripts/build_text.py                    (ล้าง stage ให้เองแล้ว)
python scripts/check_msg_translated.py          ต้อง ต่าง 0   <- ด่านใหม่
python scripts/check_armp_translated.py         ต้อง ต่าง 0   <- ด่านใหม่
python scripts/check_msg_roundtrip.py · check_pak_roundtrip.py · check_layout_all.py   ต้อง ต่าง 0

# แพ็กด้วย repak (pakwrite.py เกมไม่โหลด — §0.2)
#   1) แตก build/LikeADragonIshinThai_P.pak ลง build/stage_pak/ ตาม path ในเกม
#   2) tools/repak/repak.exe pack --mount-point ../../../ --version V11 build/stage_pak build/IshinThaiRepak2_P.pak
#   3) เทียบไบต์ทุกไฟล์กับ stage_pak ต้อง ต่าง 0
#   4) copy เข้า ~mods/ ของเกม
```

### 0.7 ตัวเลขคลังคำแปล ณ ตอนนี้

```
python scripts/merge_qc.py    ผ่าน 42,030 · คง EN 3,559 · ตก 0 · master_th 42,030 คู่
batch 169 ก้อน (168 + batch_086 ที่เพิ่งเปิดใหม่) · ตรวจแล้ว 168 · ไฟล์รีวิวครบ 168
แทนที่จริงตอนบิลด์: msg 29,561 บรรทัด · armp 12,230 ช่อง · locres 22,794 คีย์
```

**งานแปลที่เพิ่มในสprint นี้ — `batch_086` 429 สตริง (73,255 ตัวอักษร)**
ทิปส์ 344 · สติกเกอร์โหมดถ่ายภาพ 72 · ตัวเลือกในเมนู 13
ครอบคลุมบทสอนทั้งหมด: การต่อสู้/ท่าทั้งสี่ · ตีเหล็กและตรา · ทหารหน่วย · ไร่และบ้านหลังที่สอง
· มาจอง (ยาคุครบชุด) · โชกิ · โคอิ-โคอิ · โออิโช-คาบุ · ซีโล · โชฮัง · โป๊กเกอร์สามแบบ
⚠ **ยังไม่มีไฟล์รีวิวของ `batch_086`** (lead แปลเอง ยังไม่มีผู้ตรวจ)

### 0.8 ⚠ กฎใหม่ที่ต้องจำ — คอลัมน์ชนิด 13 ปนไอดีแอสเซตอยู่

`make_worklist_db_nested.py` ใช้ **บัญชีขาว** (`tips` · `photo_stamp` · `option`) ไม่ใช่บัญชีดำ
เพราะคอลัมน์สตริงชนิดเดียวกันมีทั้งข้อความบนจอและ **ไอดี/พาธของแอสเซต**
(`c_cm_ryoma` · `WEPCT2700` · `item/Accessory` · `Wanderer/ACT/` · `T_UI_Tips_glossary01`)
แปลไอดีพวกนี้เมื่อไหร่ = เกมหาแอสเซตไม่เจอ (เกิดจริงมาแล้วกับ `battle_bomb_info`)
ตัวกรองชั้นสอง: ต้องมีช่องว่างอย่างน้อยหนึ่งช่อง และไม่ขึ้นต้นด้วย `<%` และไม่มี `/`

---

## 🔜 ทำอะไรต่อ

### 1. ~~ปิดเรื่อง ESC ให้จบ~~ — ปิดแล้ว (§0.5) · ทางเลือก: ผู้ใช้ทดสอบเกมเปล่ายืนยันอีกที

### 2. รอผลทดสอบ Repak7 (§0.47): บรรทัด "ซากาโมโตะซัง" ครบไหม · จังหวะพิมพ์/เสียงเพี้ยนไหม · ไม้โทบน "งั้น" · ชื่อ "หญิงสาว" ฉากเปิด · **ถอด pak แล้วดูจอการ์ดทหารหน่วยในเกมเปล่า** (ถ้าเหมือนกัน = ดีไซน์เกม ปิดเรื่อง)
- ตัดสินใจ: จะทำชั้น pac (เสียงตะโกน NPC ≈ 70 สตริง + ป้าย Pray) ไหม — ต้องแกะฟอร์แมตก่อน
- ผ่าน → ทำชั้น label จริง: ดึง label ข้อความ 687 คำเข้า worklist (กรองไอดี) · `build_text.build_msg` ส่ง `label_replacements` · ขยาย `check_msg_translated` ให้รู้จัก label ที่แปล
- ไม่ผ่าน → ถอด POC ออกจาก `make_label_poc.py` แล้วบันทึกว่า label ห้ามแปล

### 3. ทดสอบในเกมรอบต่อไป — จุดที่ยังไม่มีใครเห็นด้วยตา
- จอทิปส์ทั้ง 344 หัวข้อ (เพิ่งแปลรอบนี้ ยังไม่เคยเห็นบนจอ)
- บทช่วงท้ายเกม (MSG_073–083) · การ์ดเรื่องย่อ PS Vita · บ้านหลังที่สอง
- จุดเสี่ยงบรรทัดยาว **302 สตริง** (`scripts/ui_length_risk.py`) — สารานุกรมศัพท์ 67 · บอลลูน NPC 40 · มาจง 32
- รายการเต็ม: `docs/INGAME_TEST_CHECKLIST.md`

### 3. งานคุณภาพที่มีหลักฐานพร้อม ยังไม่ลงมือ
- **เพศผู้พูดของ NPC ที่พิสูจน์ได้แล้ว** ~120 คีย์ (`gender_evidence.py --propose`)
  MSG_078 37 · MSG_080 26 · MSG_081 20 · MSG_083 20 · MSG_079 18
  ⚠ อาการิ (`uid016e01ab`) `bond_gender`=female แต่ `scene_gender`=male (ยืมจากบท 俺 ของไซโต) — ห้ามใช้ชั้น scene
- **`scripts/find_msg_twin.py`** ที่เสนอไว้ยังไม่ได้ทำ — หาไฟล์ฉาก JA ที่ซ้ำไบต์ต่อไบต์กับฉากที่ RGG แปล EN ไว้
- ไล่หาสาเหตุว่าทำไม `pakwrite.py` ถึงไม่ถูกโหลด (เทียบดัชนีกับ pak ที่ repak เขียน)
- เขียนไฟล์รีวิวให้ `batch_086`

### 4. กติกาที่ยังใช้อยู่
- **ห้าม lead พิมพ์รูปคำล็อก/ชื่อตัวละครจากความจำ** — copy จาก `name_locks.json` · `place_locks.json` · `batch_NNN.prior.json`
- **กติกา "ก้อนเลขน้อยกว่าเป็นเจ้าของรูป" ไม่ใช่คำตัดสินสุดท้าย** — นับทั้งคลังก่อนกวาดเสมอ
- **ป้ายผู้พูด (`labels`) เชื่อไม่ได้รายบรรทัด** — ต้องอ่านเป็นเทิร์นสนทนา
- **`tr=0` ใช้เป็นเกณฑ์ DNT ไม่ได้** — บทเปิดเรื่องหลักก็ `tr=0`
- **ด่านที่ decode/encode ด้วยบั๊กเดียวกันจะรายงานว่าผ่านทั้งที่ไฟล์พัง** — บทเรียนนี้เกิดซ้ำอีกครั้งใน sprint 17
  ทุกครั้งที่เพิ่มตัวเขียนใหม่ ต้องมีด่านที่เทียบ **ผลลัพธ์ที่แปลแล้ว** กับ vanilla ไม่ใช่แค่ roundtrip เปล่า

---

## 🆕 sprint 16 (บันทึกเก่า — สถานะปัจจุบันอยู่ §0 ด้านบน)

**แปลครบทุกก้อน 168/168 · merge ครบแล้วทุกคลื่น** (MSG_061–065·072 · MSG_073–078 · MSG_079–083)
· รื้อ DNT รวม 318 คีย์ · `merge_qc` ทั้งคลัง **ผ่าน 41,601 · ตก 0**
(ส่วนด้านล่างนี้เป็นบันทึกระหว่าง sprint — สถานะปัจจุบันอยู่ §0 ด้านบน)

### 1. ⭐ field `voice` ในไฟล์ `.msg` = ผู้พูดรายบรรทัด 4,113 แถว

สายงานเดิมใช้อยู่แล้ว (`build_speaker_gender.py`) แต่มีคิวเสียงที่ยังไม่เคยจับคู่เพศ **1,371 แถว**
จับคู่เพิ่มสามตัวที่พิสูจน์ได้ (`translations/characters.json`):

| คิว | ตัวละคร | หลักฐาน |
|---|---|---|
| `haruka` 284 แถว | ฮารุกะ (หญิง) | EN ของเกมใช้ she/her **17 แถว** — `(Is Haruka tired? **She** must've been on **her** feet all day...)` |
| `yujyo` 28 | อันนะ (หญิง) | `bond_gender.json` ป้ายสายสัมพันธ์ `uid016c008c#036` = "interact with **her**" · `ref_ja` = 「**遊女**・杏南」との絆 |
| `iku` 32 | อิคุมัตสึ (หญิง) | `characters.json` มี gender=female อยู่แล้วแต่ `cue_id` ว่าง · บทใช้สำเนียงหญิงเกียวโต どす |

สตริงที่รู้ว่าผู้พูดเป็นหญิง **815 → 934** · ⚠ **วัดอัตราการใส่คำลงท้ายทั้งคลังแล้ว**:
ชายสุภาพ 208 ใส่ : 186 ไม่ใส่ · หญิงสุภาพ 67 : 116 — ฝั่งหญิงใส่น้อยกว่าจริงแต่**ไม่ใช่ความผิดเชิงระบบ**
จึงไม่กวาดยกชุด ให้เติมเฉพาะบรรทัดที่ระดับภาษาเรียกร้อง

### 2. ⚠ กติกา "ก้อนเลขน้อยกว่าเป็นเจ้าของรูป" **ไม่ใช่คำตัดสินสุดท้าย**

มีไว้กันเดดล็อกตอนแปลเท่านั้น — **lead ต้องนับทั้งคลังก่อนกวาดเสมอ**
sprint นี้ MSG_058 (เจ้าของฉาก) เป็นฝ่ายผิดรูปทั้งสองคำ (`お頭` · `依頼書`)
ถ้าเชื่อกติกาเจ้าของจะกวาดผิดทางทั้งคลื่น (glossary §1.9.24)

### 3. ⚠ **ป้ายผู้พูด (`labels`) ในไฟล์ `.msg` เชื่อไม่ได้รายบรรทัด**

`uid010c14d7` ป้าย `Menacing Man` ติดอยู่กับ**บทของแม่**ด้วย · ถ้ายึดป้ายอย่างเดียวจะได้ 77 คีย์
ที่ "พิสูจน์ว่าเป็นชาย" ทั้งที่บทของแม่ปนอยู่ → **ต้องอ่านเป็นเทิร์นสนทนา**
เช่นเดียวกัน MSG_079 พบ `id:haraguchi` ติดกับบทของ**โอกิตะ** 5 บรรทัด (คิวเสียงสองอันขัดกันเองในสตริงเดียว)

### 4. เครื่องมือใหม่

- **`scripts/gender_evidence.py`** — ตรวจหลักฐานเพศระดับฉากให้คีย์ที่ agent เสนอ
  · `--propose` ไล่หาคีย์ที่ล็อกได้ · ใช้แล้ว**ปฏิเสธข้อเสนอไป 4 รายการ** ที่หลักฐานไม่ผ่าน
  (เคสตัวอย่าง: MSG_063 อ้างว่ามี `俺` ในเทิร์นสลับ — เปิดไฟล์ฉากแล้ว `俺` บรรทัดนั้นเป็นบทของ**ไซโต**)
- **`make_prior_hints.mine_names()`** — ขุดชื่อที่ไม่เคยเป็นคีย์สั้นใน master (แก้อาการบรีฟตกคำล็อกชื่อคน
  ที่บันทึกมาสองครั้ง) · **ต้องมีด่านกรองสี่ชั้น** ไม่งั้นได้ค่าพัง:
  ความแม่น ≥ 0.8 · ห้าม `์` ที่ตำแหน่งสอง (`Ernest` → `ร์เนสต์`) · ยาว ≤ 2.5× ของโรมาจิ
  (`Tatsu` → `ยูเมโนทัตสึคุดากิ`) · **ทิ้งกรณีกำกวม** (`Bradley` → `วิลเลียม` เพราะโผล่คู่กันเสมอ)
- **`work/brief/ANNOUNCE.md`** — ประกาศที่แปะลงบรีฟทุกก้อนอัตโนมัติ **ต้องเขียนใหม่ทุกคลื่น**

### 5. บั๊กตัวตรวจที่แก้รอบนี้ (วัดก่อนแก้ทุกตัว)

| ตัวตรวจ | อาการ | ที่วัดได้ | วิธีแก้ |
|---|---|---|---|
| `JA_FEMALE_RE` | `わね` จับรูปปฏิเสธห้วนของชาย (`払わねぇ` · `構わねぇ`) | 6 แถวบวกปลอม · `わね` ที่ไม่นำหน้าด้วยคันจิ 76 แถวหญิงจริง | เติม `(?<![一-鿿])` · **ห้ามใช้กฎเดียวกันกับ `だわ`** (気分だわ ถูก) |
| `RE_CHAO` | **"ศาลเจ้า"** ถูกนับเป็นสรรพนาม "เจ้า" | **78 จาก 82 บรรทัดผิด** | เติม `(?<!ศาล)` + `เจ้า`+ชื่อสัตว์ |
| `LOAN_FALSE_HITS` | `ระดับ**อสูร**`→"บอส" (8) · `ท่าน**เคสุ**เกะ`→"เคส" (3) · `โอเคฮาซามะ` · `ความภักดี**ลด**ลง`→"ดีล" | — | เติมทั้งสี่ |
| `modern_loanwords()` | ตัดบวกปลอม**รอบเดียว** ทำให้สองฝั่งชนกันเป็นคำยืมใหม่ (`สภาพดีทีเดียวล่ะ`→`ดีล`) | — | ตัดซ้ำจนนิ่ง |
| `POLITE_JA` | ไม่รู้จัก **`どす`** (โคปูลาสุภาพเกียวโต) มีแต่ `どすか` | どす 63 แถว · **มองข้าม 33** | เพิ่ม `POLITE_JA_RE = どす(?!こい)` (กัน どすこい 12 แถว) |

`test_qc_tools.py` **114 → 132 เคส ผ่านหมด**

### 6. งานกวาดทั้งคลังของ sprint นี้

| งาน | คีย์ | ที่นับได้ |
|---|---:|---|
| `闘技場` = **สังเวียน** (`sweep_arena.py`) | 33 | 64 : 48 · คง `แต้มสนามประลอง` ตาม §1.9.10 |
| คำยืมสมัยใหม่ที่หลุด (`sweep_loanwords.py`) | 9 | โอเค 5 · แคร์ 1 · เช็ก 3 |
| `คุณตา`→`ตาเฒ่า` · `คุณยาย`→`แม่เฒ่า` | 9 | 28 : 7 |
| `อุระยามะ`→`อุรายามะ` · `อามอน`→`อาโมน` · `ฮายาคาเซะ`→`ฮายากาเซะ` · `เออร์เนสต์`→`อาร์เนสต์` · `มัดข้าว`→`กระสอบข้าว` | 5 | 34:1 · 11:2 · 8:1 · 6:1 · 6:1 |
| `ก๊กโจโซะเท็งงุ`/`คณะนินจาผู้ทรงธรรม` (batch_022) | 5 | 22:2 · 23:2 |
| สัตว์ในเทมเพลตสายสัมพันธ์ `เขา`→`มัน` · `เอาละ`→`เอาล่ะ` · `พี่น้องตระกูลโมโมกาวะ` | 6 | 4:2 · 384:1 · ด่าน `check_bond_names` จับได้ |

### 7. คู่ที่ต่างกันแล้ว **ถูกทั้งคู่** — ห้ามไล่แก้ให้เหมือนกัน

`ท่านโทจิโร`(様) ↔ `โทจิโรซัง`(さん) · `ท่านอาจารย์กินริว`(吟柳先生) ↔ `อาจารย์กินริว`(吟柳 ป้ายภารกิจ)
· `ท่านไซโต ฮาจิเมะ`(EN `Mr.`) ↔ `ไซโต ฮาจิเมะซัง`(EN `-san`) · `คนบ้าดาบ`(ป้าย NPC) ↔ `ชายผู้หลงใหลในดาบ`(ชื่อซับสตอรี่)
· `ยูเมโนทัตสึคุดากิ`(บทพูด) ↔ `มังกรตนสุดท้าย`(ชื่อไอเทม) · `คณะไปรษณีย์เสือดำ`(黒豹飛脚団) ↔ `คนเดินสาร`(飛脚)
· `นักบวชชินโต`(`Shinto Priest`) ↔ `นักบวชศาลเจ้า`(`Priest's Shop`) · `ชิโร`(四郎) ↔ `ชิโระ`(田代/大城)
· `โจโซ` ↔ `โชโซ`(長宗我部 · 尚三 — คนละชื่อ) · โจโซใช้ `ข้า`(ワシ) ใน MSG_058 แต่ `กู`(俺) ใน MSG_059
  เพราะ `ref_ja` เปลี่ยนเองตามท้องเรื่อง (ถูกกำยานกลืน)

---

## 🆕 sprint 15 — สิ่งที่เปลี่ยนไป (อ่านก่อนเริ่มคลื่นถัดไป)

คลื่น **MSG_055–060** (1,500 คีย์) ปิดครบ · master 35,901 → **37,401 คู่ (89.9%)**
ผู้ตรวจสองคนแก้รวม **41 จุด** (055: 11 · 056: 1 · 057: 0 · 058: 6 · 059: 4 · 060: 19)

### 1. ⭐ บิลด์ + ติดตั้งม็อดใหม่แล้ว — ตัวเก่าช้ากว่า master สามคลื่น

`build/LikeADragonIshinThai_P.pak` **1,022 ไฟล์ · 18.8 MB** copy เข้า `~mods/` ของเกมแล้ว
ด่านไบต์ดิบผ่านครบทั้งก่อนและหลังบิลด์ · เขียน **`docs/INGAME_TEST_CHECKLIST.md`** ให้ผู้ใช้แล้ว
โดยวัดจุดเสี่ยงจากไฟล์จริง ไม่ใช่เดา: **199 สตริงชั้น UI ที่บรรทัดไทยยาวเกิน 1.8× ของ EN**
(กระจุกที่บอลลูน NPC 43 · มาจง 30 · ทักษะการ์ดกองทัพ 12) · `สุ~ซุ~จัง!` จุดเดียวในคลัง

### 2. ⚠ บทเรียนสำคัญ: กติกา "ก้อนเลขน้อยกว่าเป็นเจ้าของรูป" **ไม่ใช่คำตัดสินสุดท้าย**

กติกานั้นมีไว้กันเดดล็อกตอนแปล — **lead ต้องนับทั้งคลังก่อนกวาดเสมอ**
รอบนี้ MSG_058 (เจ้าของฉาก) เป็นฝ่าย**ผิดรูปทั้งสองคำ**:
`お頭` = หัวหน้า (ก้อนอื่น 12 : 0 · และ `ท่านหัวหน้า` สงวนให้ 局長 คนโด 65 คีย์)
· `依頼書` = ใบคำขอ (8 : 7 : 1) — ถ้าเชื่อกติกาเจ้าของ จะกวาดผิดทางทั้งคลื่น (glossary §1.9.24)

### 3. ⭐ ตัวขุดชื่อจาก master — แก้อาการ "บรีฟตกคำล็อกชื่อคน" ที่บันทึกมาสองครั้ง

ต้นเหตุ: `make_prior_hints.py` สร้าง lexicon จาก**คีย์สั้น**ใน master เท่านั้น
ชื่อที่ไม่เคยเป็นคีย์เดี่ยวจึงไม่เข้า lexicon เลย (`Higashihara...` ตกเพราะลงท้ายด้วยจุด)

`mine_names()` ใหม่: เอาทุกคู่ใน master ที่ EN มีชื่อนั้น แล้วหาสตริงไทยที่ยาวที่สุดซึ่งปรากฏใน **ทุก** ค่า
⚠ **ต้องมีด่านกรองความแม่น** — รอบแรกที่ยังไม่มี ได้ผลบวกปลอม `Oharu` → "น้องสาว"
· `Land` → "กที่" · `Bradley` → "วิลเลียม" · เกณฑ์ที่ใช้: คู่ที่มีชื่อนั้น ต่อ คู่ทั้งหมดในคลัง
ที่มีสตริงไทยนั้น ต้อง **≥ 0.8** · บรีฟแสดงเป็นหัวข้อ **2.2 `names_from_master`**

### 4. ⭐ `work/brief/ANNOUNCE.md` — ช่องประกาศที่แปะลงบรีฟทุกก้อนอัตโนมัติ

ใช้แจ้ง "คำที่เพิ่งกวาดทั้งคลัง" กับคำตัดสินใหม่ที่ยังไม่เข้า cheatsheet · lead เขียนที่เดียว
ไม่ต้องพิมพ์ซ้ำหกรอบ · **ต้องเขียนใหม่ทุกคลื่น** ฉบับปัจจุบันเป็นของคลื่น 055–060

### 5. บั๊กตัวตรวจที่คลื่นนี้ทำให้เจอ

| ตัวตรวจ | อาการ | ที่วัดได้ | วิธีแก้ |
|---|---|---|---|
| **`JA_FEMALE_RE`** | `わね` ไปจับ**รูปปฏิเสธห้วนของชาย** (`払わねぇ` · `構わねぇ` · `思わねぇ` · `従わねぇ`) → `ja_gender()` ตอบ female ให้บทนักเลง ถ้าใครใส่ "จ๊ะ" ตามหลักฐานปลอมนี้ ด่าน G จะปล่อยผ่าน | **6 แถวเป็นบวกปลอมทั้งหมด** · `わね` ที่ไม่ได้นำหน้าด้วยคันจิ 76 แถวเป็นหญิงจริง | เติม `(?<![一-鿿])` แยกก้านกริยาออกจากวิภัตติ |
| (ข้อควรระวัง) | **ห้ามเอากฎเดียวกันไปใช้กับ `だわ`** | `だわ` ที่นำหน้าด้วยคันจิ 4 แถวเป็นหญิงจริงทั้งหมด (気分だわ · 心配だわ · 反則だわ) เพราะ だ ตามหลังคำนามคันจิได้ | — |

`test_qc_tools.py` **114 → 122 เคส ผ่านหมด**

### 6. `gender_lines.json` โตจาก 181 → **223 คีย์ใช้ได้** (+42)

| กลุ่ม | คีย์ | หลักฐานแกน |
|---|---:|---|
| อุจิทาโร (`uid010c14d7`) | 6 | ผู้พูดใช้ 俺 ในเทิร์นสลับของบทสนทนาเดียวกัน 5 แถว · แม่เรียกเขาว่า 息子/内太郎 |
| ชายที่ล้มซ้ำที่เดิม (`uid010c1657`) | 8 | ผู้พูดใช้ 僕 ห้าแถว · อีกฝ่ายคือเรียวมะใช้ 俺 · ทั้งไฟล์ ชาย 8 · **หญิง 0** |
| กลุ่มห้าคน (`uid010c1609`) | 3 | 僕達 + 俺達 ทั้งฉาก · ทั้งไฟล์ ชาย 11 · **หญิง 0** |
| ศิษย์สำนักกินริว (`uid0116084d`) | 18 | บทของคณะเดียวกันใช้ 俺 · ทั้งไฟล์ ชาย 15 · **หญิง 0** |
| ศิษย์คู่ซ้อม · ลูกน้องแบรดลีย์ · โจโซ · อากุมิ | 7 | ทุกไฟล์ ชาย > 0 · **หญิง 0** |

**เกณฑ์ที่ใช้ทุกคีย์**: EN โผล่ในไฟล์ฉากนั้นไฟล์เดียว · ฉากมีเครื่องหมายเพศชาย
· **ฉากไม่มีเครื่องหมายเพศหญิงเลยสักแถว**

⚠ **ที่ปฏิเสธ**: นินจาเขียว — ไฟล์ `uid01160706` เป็นฉากผสม (ชาย 5 · **หญิง 22** เพราะโอมัตสึพูดเยอะ)
ใช้เกณฑ์ "ฉากเพศเดียว" ไม่ได้ · และไม่มีคีย์ไหนที่ EN โผล่เฉพาะไฟล์นั้น

### 7. ⚠ **ป้ายผู้พูดในไฟล์ `.msg` เชื่อไม่ได้รายบรรทัด**

ในไฟล์ `uid010c14d7` ป้าย `Menacing Man` ติดอยู่กับ**บทของแม่**ด้วย (`#014` · `#122`)
ถ้ายึดป้ายอย่างเดียวจะได้ 77 คีย์ที่ "พิสูจน์ว่าเป็นชาย" ทั้งที่บทของแม่ปนอยู่
→ **ต้องอ่านเป็นเทิร์นสนทนา ไม่ใช่เชื่อป้าย**

### 8. ที่ผู้ตรวจถอนออก (ตรงข้ามกับการเติม)

**ถอน `ขอรับ` ของศิษย์สำนักโคมากิ 9 บรรทัดใน MSG_060** — ตัวละครนี้ไม่มีเครื่องหมายเพศ
ในบรรทัดตัวเองเลยสักแถว (`uid0016086f` ชาย 0 · หญิง 0) · EN ไม่เคยใช้ he/him/his กับเขา
· และ **master แปลบทของเขาไว้กลางเพศครบ 12 บรรทัดแล้ว** — เก็บไว้จะชนกับของที่ ship ไปแล้ว

### 9. งานกวาดทั้งคลังของ sprint นี้ (รวม 60 คีย์)

| งาน | คีย์ | สคริปต์ |
|---|---:|---|
| `"สู้ ๆ"` คำร่วมสมัย | 12 | `sweep_suusuu.py` |
| รูปคำยกย่องของไซโต | 20 | `sweep_saito_honorific.py` |
| คำที่ชนกันในคลื่น (お頭 · 依頼書 · Lucky Cat · เทมเพลตสายสัมพันธ์) | 18 | `sweep_wave_055_060.py` |
| `ก๊กโจโซะเท็งงุ`/`คณะนินจาผู้ทรงธรรม` ใน batch_022 | 5 | (กวาดตรง) |
| สัตว์ในเทมเพลตสายสัมพันธ์ `เขา` → `มัน` (MSG_030) · `เอาละ` → `เอาล่ะ` (060) | 5 | (กวาดตรง) |

---

## 🆕 sprint 14 — สิ่งที่เปลี่ยนไป (อ่านก่อนเริ่มคลื่นถัดไป)

คลื่น **MSG_049–054** (1,500 คีย์) ปิดครบ · master 34,401 → **35,901 คู่ (86.3%)**

### 1. ⭐ ด่านใหม่ `scripts/check_thai_marks.py` — จับกลิฟไทยที่จอแสดงผลไม่ได้

ผู้ตรวจเจอบรรทัด `ตายซะ อากุริอิิ!!!` ที่มี **สระ ิ สองตัวบนพยัญชนะเดียว** — บนจอเป็นกลิฟทับกัน
อ่านไม่ออก **แต่ผ่านด่านทุกตัวที่มีอยู่** เพราะไม่มีตัวไหนดูลำดับตัวประกอบเลย

ตรวจห้าแบบ: สระบนซ้อนสระบน · สระล่างซ้อนสระล่าง · วรรณยุกต์ซ้อนวรรณยุกต์
· วรรณยุกต์มาก่อนสระ · ตัวประกอบลอย (ไม่มีพยัญชนะนำ)
⚠ **วรรณยุกต์ตามหลังสระบนเป็นรูปที่ถูกต้อง** (เชื่อ = ช + ื + ่) — รอบแรกเขียน regex ผิด
ได้ผลบวกปลอม 20,185 จุด ต้องแก้ lookbehind ให้รับสระบน/ล่างด้วย

รันทั้งคลังแล้ว **ถูกต้องทุกบรรทัด** → **เพิ่มเป็นด่านบังคับข้อ 1.5 ก่อนแจกคลื่น**

### 2. บั๊กตัวตรวจที่คลื่นนี้ทำให้เจอ

| ตัวตรวจ | อาการ | ที่วัดได้ | วิธีแก้ |
|---|---|---|---|
| **`TAG_RE`** | `~[^~\s]{0,40}~` จับ**คำไทยลากเสียง** ("สุ~ซุ~จัง") เป็นแท็กเกิน → ด่าน T ตีตกบรรทัดที่ถูก | **เกมนี้ไม่มีแท็กทรง `~...~` เลยสักแถว** (54,318 แถว = 0) | จำกัดเนื้อในแท็กเป็น ASCII |
| `RE_THAN` | **"สองเท่าน่ะ"** (เท่า + น่ะ) ถูกจับเป็นสรรพนาม "ท่าน" เพราะ น มีวรรณยุกต์ของพยางค์ถัดไปเกาะ | ตระกูลเดียวกับ ท่านั่ง · ท่านี้ | เติมวรรณยุกต์ทุกตัวเข้า lookahead |
| `POLITE_JA` | ไม่รู้จัก `ござら`(ござらん) · `御免` · `すいやせん` · `っしゃろ` · `かたじけない` | 御免 19 · かたじけない 4 · すいやせん 3 · っしゃろ 2 · ござら 1 | เพิ่มทั้งห้า (`すんまへん` 21 แถวเข้าเกณฑ์อยู่แล้วผ่าน `まへん`) |
| `check_cross_batch` | **มองไม่เห็นเครื่องหมายพูดติดอ่าง** เพราะเทียบเป็นคำ ไม่ใช่เครื่องหมาย | ทั้งคลัง ยัติภังค์ 285 : จุลภาค 13 — 13 จุดอยู่ในก้อน MSG_054 ก้อนเดียว | ผู้ตรวจนับเอง · ยังไม่ได้ทำเป็นด่าน |

### 3. คำตัดสินสำคัญ (glossary **§1.9.22**)

**บทของทอม (ชาวต่างชาติ) — ถอดเสียงคำญี่ปุ่นที่เขาพูดเพี้ยน ไม่แปลความ**
EN จงใจสะกดผิด (`Arigado gozaimasoo` · `Sayanarra` · `Koodasai` · `desoo`) **ถ้าแปลความ มุกหายหมด**
เพราะในฉบับไทยบทของคนอื่นก็เป็นไทยเหมือนกัน ผู้เล่นจึงไม่เห็นการสลับภาษาเลย
· `Sayanarra`(เพี้ยน) = **ซาโยนาร่า** ≠ `Sayonara`(ถูก) = **ซาโยนาระ** — สองคีย์นี้อยู่ติดกัน เป็นมุกจับคู่
· กวาด `batch_MSG_011` 3 บรรทัดที่เคยแปลความแล้ว
⚠ `sammerai`/`busheedo` **ไม่ดัดสะกด** เพราะ master ตั้ง `The Way of the Sammerai` = "วิถีซามูไรฝรั่ง"

**`Oguri` → อากุริ** — EN พิมพ์ผิดหนึ่งคีย์ ขณะที่คีย์อื่นในซับสตอรี่เดียวกันเขียน `Aguri`
(เคสที่สามของตระกูลนี้ ต่อจาก Saigo/Saito และ `the last player` ใน §1.9.20)

**腰 = สะโพก ทั้งซับสตอรี่ร้านอุด้ง** — EN สลับใช้ hips/back เอง แต่ต้นฉบับเป็นคำเดียว
และมุกคือเล่นคำกับ "โคชิ" ของเส้นอุด้ง · กวาด `batch_MSG_012` อีก 7 คีย์ (เอว → สะโพก)

**คำล็อกใหม่ 8 คำ**: อิเคซุงิ · โอยุกิ · คันดะ · ทังกะ · รุ่นพี่ · โยอิ-โยอิ · ลุงไซโต · ตาเฒ่า
**กวาดรูปที่ชนกับคำล็อก**: ซูซุ→สุซุ · อิเคซึงิ→อิเคซุงิ · คาซึกิจิ→คาซุกิจิ · โอสุซึ→โอสุซุ · ยูกิ→ยุกิ

### 4. `gender_lines.json` โตจาก 79 → **181 คีย์ใช้ได้**

ผู้ตรวจสองคนพิสูจน์จากไฟล์เกมแล้วเสนอมา 102 คีย์ · lead ตรวจซ้ำเองว่า**ทุกคีย์อยู่ในไฟล์ฉากเดียว**
และมีอยู่จริงในชั้น `.msg` ครบ 100% ก่อนรับ

| กลุ่ม | คีย์ | หลักฐานแกน |
|---|---:|---|
| นัตสึมิ โซซากุ (นักเขียน) | 70 | บทรำพึงของเรียวมะในซับสตอรี่เดียวกันเรียกเขาว่า "**he**/**his**" |
| ท่านนักบวช (ฉากสมุดบันทึกหักมุม) | 10 | `uid010c1422#020` "(was I actually writing to the **priest** the whole time!?)" |
| ไซกิ (พ่อของโอยุกิ) | 7+1 | ลูกสาวเรียก 「お父ちゃん」 แล้วเขาตอบ 「お雪！」 ในไฟล์เดียวกัน |
| เจ้าของซ่อง · นักซูโม่ · พ่อค้า · พ่อของเด็ก · ตาเฒ่า · โจชิ | 15 | ป้ายผู้พูดชั้น 1 · でごわす · 俺達商人 · うちの息子 · "My wife" · 拙者/でござる |

⚠ **ที่ปฏิเสธ**: บทมิโกะทั้งหมด (`uid010c1422`) — ป้ายผู้พูดคือ `Shrine Maiden` แต่ `ref_ja`
「はい、なんでしょう」 **ไม่มีเครื่องหมายเพศของตัวเอง** เพศที่ได้มาเป็นชั้น 3 ยืมจากเรียวมะ
ผู้ตรวจถอน "ขอรับ" ออกแล้ว — **และจำเป็นต่อเนื้อเรื่องด้วย** เพราะมุกของซับสตอรี่คือเจ้าของสมุดบันทึก
กลายเป็นท่านนักบวชผู้ชาย ถ้าใส่คำลงท้ายหญิงให้ใครในฉากนี้ มุกพังทันที

### 5. ⚠ ความผิดพลาดของ lead ที่ผู้ตรวจจับได้ (บันทึกไว้ให้คลื่นหน้าไม่ทำซ้ำ)

1. **บรีฟอ้างไฟล์ฉากผิด** — lead สั่งให้เติม "ขอรับ" ให้เจ้าของร้านอุด้งโดยอ้าง `uid000c140e`
   แต่บทในก้อน MSG_052 อยู่ที่ `uid010c1461`/`uid010c1463` · ผู้ตรวจวัดเองแล้วพบว่า
   **บรรทัดที่มีรูป ですます = 0 จาก 30 คีย์** จึงไม่เติมสักบรรทัด — **ถูกต้อง**
2. **กวาดแล้วลืม merge จริง** — หลังกวาดชื่อรอบ §1.9.21 lead รัน `merge_qc --dry-run` แทน `merge_qc`
   master จึงค้างรูปเก่าไว้ · ผู้ตรวจจับได้จากการค้น master
3. **นับผิดสองครั้ง** — บอกว่า `friendo` ตัดคำเรียกทิ้ง 2 บรรทัด (จริง 1) และรับตัวเลข
   "การ์ด 381 his : 0 her" มาโดยไม่นับซ้ำ (จริง 521:17 และมีการ์ดทหารหญิง 42 ใบ)

→ **กติกาเดิมยังใช้ได้ดี: ทุกฝ่ายต้องนับซ้ำก่อนใช้ตัดสิน รวมทั้งตัวเลขที่ lead เป็นคนให้**

### 6. งานกวาดทั้งคลังของ sprint นี้ (รวม 178 คีย์)

| งาน | คีย์ |
|---|---:|
| รูปทับศัพท์ชื่อ (§1.9.21 · เครื่องมือ `check_name_forms.py` + `sweep_name_forms.py`) | 99 |
| คำลงท้าย `...ละ` → `...ล่ะ` · `นี้จ้า` · `อ้าก` · `โอ้ะ` | 53 |
| คำอุทานรอบสอง (เฮ่ย · โอ้ย · เอ๊า · โอโฮ · ไม้ละ · โดสึโคอิ · อาริงาโตะ · โอสุซึ) | 11 |
| 腰 = สะโพก ใน `batch_MSG_012` | 7 |
| ชื่อที่ชนคำล็อก (ซูซุ · อิเคซึงิ · โอกุริ · คาซึกิจิ · ยูกิ) | 8 |

`check_cross_batch` ชั้น 1 (คีย์ EN เดียวกันแปลต่างกัน) = **0 คีย์** · `check_name_forms` เหลือ 5 ชื่อ
ซึ่งตรวจแล้วว่า**ถูกทั้งคู่** (ชิโร 四郎 ≠ ชิโระ 田代/大城 · นะ/นา · น่ะ/น่า เป็นคำไทยปกติ)

---

## 🆕 sprint 13 — สิ่งที่เปลี่ยนไป (อ่านก่อนเริ่มคลื่นถัดไป)

คลื่น **MSG_043–048** (1,500 คีย์) ปิดครบ · master 32,901 → **34,401 คู่ (82.7%)**

### 1. ⭐ บรีฟรายก้อนสร้างด้วยสคริปต์แล้ว — `scripts/make_wave_brief.py`

อาการ "lead พิมพ์คำล็อกลงบรีฟผิดจากความจำ" บันทึกมาแล้ว **หกครั้งติดกัน** (§4.6)
ทุกครั้งนักแปลจับได้เอง แต่เสียรอบถามกลับ — แก้ที่ต้นเหตุแทนการเตือนซ้ำ

สคริปต์อ่าน `batch_NNN.prior.json` (ซึ่งดึงจาก `name_locks.json` · `place_locks.json` · `master_th.json`
อยู่แล้ว) แล้วเขียน `work/brief/batch_NNN.brief.md` ให้ครบ: คำล็อกชื่อคน/สถานที่ · ชื่อเฉพาะที่เคาะแล้ว
· คำเดี่ยวจากบริบทอื่น · **ตารางเจ้าของรูปของฉากที่ใช้ร่วมกับก้อนอื่น** (เลขน้อยกว่าเป็นเจ้าของ)
· ด่านที่ต้องรัน — **ไม่มีคำล็อกคำไหนผ่านมือ lead เลย** · คลื่นนี้บรีฟผิด 0 คำ

### 2. ⚠ ก้อนที่มีไฟล์ `.dnt` — ไฟล์ done ต้องครบตาม `batch_NNN.json` ไม่ใช่ `todo.json`

ด่าน A1 เทียบชุด/ลำดับคีย์กับ **ไฟล์เต็ม 250 คีย์** ส่งแค่คีย์ที่แปลจริง = **ตกทั้งก้อน**
MSG_046 (แปลจริง 149 · DNT 101) เจอเข้าเต็ม ๆ เพราะบรีฟเขียนตกข้อนี้ — นักแปลแก้เองแล้วรายงาน
เขียนลง cheatsheet **§6 ข้อ 3.5** แล้ว · คลื่นถัดไปตรวจก่อนแจกว่าก้อนไหนมี `.dnt`

### 3. บั๊กตัวตรวจที่คลื่นนี้ทำให้เจอ — **เก้าตัว วัดก่อนแก้ทุกตัว**

| ตัวตรวจ | อาการ | ที่วัดได้ | วิธีแก้ |
|---|---|---|---|
| `POLITE_JA` | ไม่รู้จัก **`ござる`** → บท でござる ที่ใส่ "ขอรับ" ตกด่าน J เพราะ `PLAIN_JA` จับ `だろ` ในประโยคปิด | **79 แถว ทุกแถวเป็น でござる ไม่มีบวกปลอม** | เพิ่ม `ござる` |
| `JA_FEMALE_RE` | **`おかしら`** (お頭 = หัวหน้าโจร) ถูกจับเป็น かしら หญิง → ไฟล์บริบทตี `evidence_gender: female` ผิด | **17 แถว ผิดทั้งหมด** | เติม `(?<!お)` |
| `RE_THAN` | **`ท่านหัวหน้า`** · `ท่านรองหัวหน้า` ถูกนับเป็นคำเรียกคู่สนทนา (ตระกูลเดียวกับ `ท่านลุง`) | 53 + 22 แถว | เพิ่มเข้า lookahead |
| `RE_CHAO` | **`เจ้ามือ`** (banker) ถูกนับเป็นสรรพนาม "เจ้า" | **47 จุด ไม่มีจุดไหนเป็นสรรพนามเลย** | เพิ่ม `มือ` เข้า lookahead |
| `RE_CHAN` | **`อิฉัน`** (คำแทนตัวหญิงยุคเก่า) โดนจับ "ฉัน" ข้างใน | — | เติม `(?<!อิ)` |
| `LOAN_FALSE_HITS` | คำล็อก `Pause Menu` = **"เมนูหยุดเกม"** ถูกนับเป็นคำยืม "เกม" | เทมเพลตสายสัมพันธ์ที่ ship แล้วโดนทุกบรรทัด | เพิ่ม `หยุดเกม` |
| `check_glossary_locks` | คำล็อกป้ายจุดหมาย **`To X`** จับแบบซับสตริง → ร้อยแก้ว "ไป**ย่าน**มุคุโรไก" โดนเตือน | 2 จุดในคลื่นเดียว | `sign_pattern()` — กลุ่ม `To X` จับทั้งสตริงเท่านั้น |
| **`check_cross_batch`** | `path.name[6:9]` → `batch_MSG_043.done.json` กลายเป็น `"MSG"` เหมือนกันหมด **ก้อน MSG ทั้งซีรีส์ยุบเป็นถังเดียว** คีย์ก้อนหลังทับก้อนก่อน และ**เทียบข้ามก้อน MSG ด้วยกันไม่ได้เลยตั้งแต่ MSG_001** | ตัวตรวจเห็น 86 ถัง ทั้งที่มี **139 ก้อน** | `path.name[len("batch_"):-len(".done.json")]` |
| `merge_qc` (ผลพลอยได้) | หลังแก้ `cross_batch` ตรวจได้จริงแล้ว: ชั้น 1 (คีย์ EN เดียวกันแปลต่างกัน) = **0** · ชั้น 2 = 102 กลุ่ม | — | รูปสะกดที่ต่างกันในชั้น 2 ยังเป็นงานค้าง (ดู §5) |

`test_qc_tools.py` **104 → 113 เคส ผ่านหมด**

### 4. คำตัดสินสำคัญของคลื่นนี้ (glossary **§1.9.20**)

**บรรทัดที่ EN ผิด — ยึด `ref_ja` (ข้อยกเว้นของ §4.5)** ใช้ได้เฉพาะเมื่อ **EN ขัดกับ EN ของตัวเองในฉากเดียวกัน**
หรือทำให้ผู้เล่นทำตามกติกาเกมผิด:

| คีย์ | EN เขียน | ที่ถูก | หลักฐาน |
|---|---|---|---|
| `uid0107115d#034` · `#097` `…orders given to us directly by the Imperial Court.` | Imperial Court | **รัฐบาลโชกุน** | `ref_ja` = 御公儀 (= 幕府) · บรรทัดถัดไปพูดถึง 討幕派 — แปลตาม EN เนื้อเรื่องกลับข้างทั้งฉาก |
| `uid01021eb0#006` `…show our friend Saito.` | Saito | **ไซโง** | `ref_ja` = 西郷 · **EN ในฉากเดียวกันเขียน "Saigo" เอง 3 บรรทัด** |
| ซีโล 4 คีย์ | `the last player` | **ผู้เล่นคนถัดไป** | 後ろの子 / 一つ後の子 ≠ 最後の子 — EN เขียนทับสองความหมาย |

**ไม้ยมก (ๆ) = เว้นวรรคข้างหน้าเสมอ** — ผู้ตรวจสองคนสั่งกันคนละทางในคลื่นเดียวกัน เพราะต่างยึด
"รูปข้างมากในกลุ่มที่ตัวเองเห็น" · lead วัดทั้งคลัง **เว้นวรรค 2,429 : ติดกัน 560** และรูปเว้นวรรค
เป็นรูปที่ราชบัณฑิตยสภากำหนด → กวาดทั้งคลัง **559 จุด · 474 คีย์ · 44 ไฟล์** (`scripts/sweep_maiyamok.py`)
เทมเพลตข้อความสายสัมพันธ์ที่ ship รูปติดกันไว้ 30 จุด **คือรูปที่ผิด ไม่ใช่บรรทัดฐาน**

**คำอื่นที่กวาด**: `เอาละ` → **เอาล่ะ** (11 จุด · คลังใช้ 538:12 · ไม่แตะ "ไม่เอาละ")
· `Shinpa-chan` → **ชินปะจัง** (2 จุด) · `friendo` → **เพื่อนยาก** รูปเดียว (กวาด "สหาย" ใน batch_070)
· `Bet Settlement` = **เคลียร์เดิมพัน** (ป้ายระบบ ให้ตรงหน้า How to Play) ขณะที่บทพูด = **ชำระเดิมพัน**
· `子` = **ผู้เล่น…** ไม่ใช่ "ลูกมือ" (แก้ §1.9.8 · "ลูกมือ" สงวนให้ `Farmhand`)
· `Thomas Glamour-chan` = **โทมัส กลาเมอร์จัง** · **ห้ามใช้ "คุณชาย"** (คำชนชั้นสูงของไทย) → ใช้ **เจ้าหนุ่ม**

### 5. เพศผู้พูด — รับหนึ่ง ไม่รับสอง

`gender_lines.json` **68 → 79 คีย์ใช้ได้**

| เคส | ผล | เหตุผล |
|---|---|---|
| **โทมัส โกลเวอร์ = ชาย** (10 คีย์) | ✅ รับ | `book_book.bin.json` แถว 21 ของเกมเขียนเอง *"There is a Scottish man… **He** is a weapons merchant… **he** is of stern disposition"* + locres `correlation_person/0172` + msg `uid006f0040#217/#220` (悲しい**男**だな) — lead เปิดไฟล์ยืนยันเอง · เติม "ขอรับ" **3 จาก 10 บรรทัด** เฉพาะที่ `ref_ja` มี です/ます |
| **ทหารชินเซ็นกุมิที่อิเคดายะ = ชาย** (1 คีย์) | ✅ รับ | คิวเสียง `hira1_c06_120_01` (平隊士) · ฮิจิกาตะเอ่ยถึงกลุ่มนี้ในไฟล์ฉากเดียวกัน `uid01022020#003` · คิว `hira*` ทั้งคลัง 7 บรรทัด ชาย 1 หญิง 0 |
| **ทหารหน่วยสิบ** (1 คีย์) | ❌ **ไม่รับ** | ผู้ตรวจอ้างการ์ด "381 his : 0 her" — lead **นับซ้ำทั้งไฟล์ได้ he/him/his 521 · she/her 17** และมีการ์ดทหารหญิง prefix `taishi_l_*` อยู่จริง **42 ใบ** → หน่วยนี้มีสมาชิกหญิงในเกมจริง · คิว `taishi*` ทั้ง 14 บรรทัด `ja_gender` คืน `None` |
| **ฉากสอนทัพหน่วยสาม `uid0107115d`** (43 คีย์) | ❌ คงกลางเพศ | วัดทั้งไฟล์ 126 บรรทัด: `ja_gender` = None ทุกบรรทัด · **ไม่มีช่อง `voice` เลย** · `scene_gender` = None — keigo ไม่บอกเพศ |

⚠ **บทเรียนที่ย้ำอีกครั้ง**: ตัวเลขที่ agent รายงานต้องนับซ้ำก่อนใช้ตัดสิน — คลื่นนี้ผิดสองทาง
ผู้ตรวจนับการ์ดผิด **และ** lead เองนับ `friendo` ที่ตัดคำเรียกทิ้งผิด (บอกสอง จริงหนึ่ง ผู้ตรวจจับได้)

### 6. งานค้างใหม่ที่คลื่นนี้เปิดออกมา

- **`check_cross_batch` ชั้น 2 มี 102 กลุ่ม** ที่เพิ่งมองเห็นเป็นครั้งแรกหลังแก้บั๊กถัง
  ตัวที่น่าจะเป็นรูปผิดจริง: `กนโด` (026) · `เฮ่อ`/`เฮ้อ` · `จุนตาโร`/`ชุนตาโร`/`ชุนทาโร`
  · `โปกเกอร์`/`โป๊กเกอร์` · `โกโร`/`โกโระ` · `โดสึโคอิ`/`โดสุโคอิ` · `ละ`/`ล่ะ` ท้ายประโยคอีกหลายตระกูล
  → **ควรทำเป็นงานกวาดหนึ่งรอบก่อนบิลด์** (ไม่ใช่แทรกกลางคลื่น)
- **จอที่ไทยปนญี่ปุ่น 38 จุด** — คิวคีย์ด้วยข้อความอังกฤษ สตริงญี่ปุ่นที่ RGG ไม่เคยแปล (2,660 สตริง)
  ตัวไหนโผล่ในก้อนที่แปลแล้วก็จะได้ไทยติดมาด้วย · **ยอมรับตามนี้ในเวอร์ชันนี้** (1.4%) รายละเอียด glossary §1.9.20 ช
- **ตารางผู้พูด `id:*`** — ผู้ตรวจพิสูจน์เพิ่มจากไฟล์เกมได้ 8 ตัว เขียนลง `docs/speakers.md` แล้ว
  (`id:majima`=โอกิตะ · `id:saejima`=นางากุระ · `id:date`=นากาโอกะ · `id:akiyama`=คัตสึระ
  · `id:iku`=อิกุมัตสึ · `id:toma`=โกลเวอร์ · `id:ryuji`=ไซโง · `id:katu`=คัตสึ ไคชู)

---

## 🆕 sprint 12 — สิ่งที่เปลี่ยนไป (อ่านก่อนเริ่มคลื่นถัดไป)

### 1. ⭐ ไฟล์บริบทบอกหลักฐานเพศชั้นอื่นแล้ว — `evidence_gender`

**ปัญหาเดิม**: ไฟล์บริบทรู้แค่ป้ายผู้พูด จึงตี `neutral: true` ให้ **85% ของทั้งคลัง**
ทั้งที่ด่าน G รู้จักหลักฐานอีกสามชั้นอยู่แล้ว → นักแปลเขียนกลางเพศไว้ก่อน
แล้วผู้ตรวจต้องไล่เติมคำลงท้ายคืนทีหลัง (คลื่น MSG_031 เติมคืน 25 บรรทัดในก้อนเดียว)

`make_batch_context_ishin.py` เพิ่มช่อง **`evidence_gender`** ให้บรรทัดที่ `neutral: true`
แต่มีหลักฐานรองรับ พร้อมบอกชั้นที่มา (`from` = `lead` > `ja_line` > `scene`)
**ไม่แตะช่อง `neutral`** — ด่าน G ยังตัดสินเหมือนเดิม ที่เพิ่มคือนักแปลเห็นหลักฐานตั้งแต่แรก

**regenerate ครบ 168 ไฟล์แล้ว** · ผลจริง: กลางเพศ 35,340 บรรทัด · ในนั้น **8,926 บรรทัด (25%)**
มีหลักฐานรองรับ — ระดับฉาก 6,847 · เครื่องหมายในบรรทัดเอง 2,011 · คำตัดสินของ lead 68

⚠ **บั๊กที่เจอตอนรัน**: `glob("batch_*.json")` จับไฟล์คู่ที่ sprint 9–10 เพิ่มเข้ามาด้วย
(`.todo` · `.prior` · `.dnt`) รอบแรกจึงเขียนไฟล์บริบท **426 ไฟล์ เกินมา 258 ไฟล์** — ลบทิ้งและแก้ glob แล้ว

### 2. ชั้นหลักฐานเพศ — ชั้นที่ 5 (ป้ายคิวเสียง) พิสูจน์ตัวเองแล้ว

ช่อง `voice` ในไฟล์ฉากเป็น **ป้ายผู้พูดรายบรรทัดที่เชื่อได้** (ต่างจากช่อง `labels` ที่ติดมาหลวม ๆ)
คลื่นนี้ใช้ปิดเคสที่หลักฐานอื่นตันไปสามเคส:

| ผู้พูด | ป้ายคิวเสียง | หลักฐานที่ผูกเข้าไป |
|---|---|---|
| ยามเฝ้าประตูสถานกงสุลซัตสึมะ | `mon_c07_073_*` (門番) | บรรทัด #016 ของเขาเองใช้ `オイが` = สรรพนามชายสำเนียงซัตสึมะ |
| มิซึกิ | `mizuki_c05_080_*` | ต้นฉบับบรรทัดข้าง ๆ เขียน 「女が見てるぞ」 ตรง ๆ |
| โทกูงาวะ โยชิโนบุ · ข้าราชบริพาร 旗本 | `nobu_*` · `hata_*` | ไอเทม `book_book#13#13#text` เล่าฉากเดียวกันจากมุมข้าราชบริพาร ใช้ 拙者 · EN เรียก 上様 ด้วย he/him |

`translations/gender_lines.json` โตเป็น **68 คีย์**

⚠ **`check_gender_lines.py` (ใหม่) เป็นด่านบังคับก่อนแจกคลื่น** — ไฟล์นี้คีย์ด้วย **ข้อความอังกฤษ**
สตริงที่ใช้ซ้ำทั้งเกมจะไปบังคับเพศให้ผู้พูดคนอื่นด้วย เกิดจริงตอน lead ล็อกบทยามซัตสึมะ
แล้วเผลอล็อก **`"What?"` ซึ่งโผล่ 57 ครั้งทั่วเกม** ให้เป็นชาย · ตัวตรวจจับได้ทันที

### 3. บั๊กตัวตรวจที่คลื่นนี้ทำให้เจอ (ทุกตัวมาจากนักแปล/ผู้ตรวจที่รายงานแทนการเลี่ยง)

| ตัวตรวจ | อาการ | วิธีแก้ |
|---|---|---|
| `POLITE_JA` | ไม่รู้จักรูปคันจิ `下さい` (**119 แถวในคลัง**) และ `まして` (78 แถว) | เพิ่มทั้งสอง · วัดก่อนแล้วว่า `ましてや` ที่เป็นบวกลวงมีแค่ 2 แถว |
| `POLITE_JA` | ไม่รู้จักรูปสุภาพสำเนียงซัตสึมะ → ด่าน J เตือนบทยามเฝ้าประตูทั้งฉาก | เพิ่ม `ごわす · ごわし · ごわは · しもす · りもす · じもす · もはん · もした` · **ไม่ใส่ `もす` เปล่า** (62 แถว ส่วนใหญ่เป็นบวกปลอม: 気もす · 何でもす) |
| `JA_MALE_RE` | ไม่รู้จัก `オイ` (สรรพนามชายซัตสึมะ) | ผู้ตรวจเสนอ `オイ(?=[がのはを、。])` — **วัดแล้วกว้างเกิน** เพราะ `オイ、` เป็นคำอุทาน 3 แถว → ใช้ `オイ(?=[がのは])` ได้ 2 แถวถูกทั้งคู่ |
| `MODERN_LOANWORDS` | `ดีล่ะ` · `โกลเวอร์` · **`แต่เดิมทีมัน`** ถูกนับเป็นคำยืม | ขยาย `LOAN_FALSE_HITS` |
| `check_pronoun_pairs` | ไม่มีข้อยกเว้น "เอกสารยาว" ที่ด่าน P มี | ย้าย `is_document()` ลงไป |
| `make_batch_context_ishin` | glob กว้างเกิน เขียนไฟล์บริบทเกินมา 258 ไฟล์ | รับเฉพาะไฟล์ worklist จริง |

`test_qc_tools.py` **100 → 104 เคส ผ่านหมด**

### 4. คำล็อกใหม่ · คำที่กวาดย้อนหลัง (glossary §1.9.19)

**กวาดทั้งคลังรอบนี้รวม ~250 จุด** — คำที่สำคัญที่สุด:

| คำ | รูปที่ล็อก | จำนวน |
|---|---|---|
| `<ชื่อ>-sensei` | **ท่านอาจารย์\<ชื่อ\>** (master เคยมีสองรูปแม้แต่กับคนเดียวกัน) | 27 |
| `Saito-san` ที่แปลว่า "ท่านไซโต" | **ไซโตซัง** | 104 |
| 御所 ในบทพูด · 土蔵 · 奉行所 · 密偵 | **พระราชวังหลวง · โกดัง · จวนขุนนางผู้ว่าการ · สายลับ** | 22 |
| `が出来た X` | **ทำ X ได้แล้ว** (ตรงกับตระกูล `You made X` 43 คีย์) | 39 |
| `Premium X` (特上) · 手作り | **X พิเศษ ติดกัน** · **ทำเอง** (ไม่ใช่ "โฮมเมด") | 15 |
| `-chan` ที่ผู้ใหญ่ล้อกันเอง | **ไอ้หนู\<ชื่อ\>** (ฮาจิเมะ · โทชิ · อิซามิ · โกโร) | 22 |
| `Furutaka` · `Elite Troopers` | **ฟุรุทากะ** · **ทหารหน่วยยอดฝีมือ** | 6 |

**คำล็อกใหม่**: 法度 = กฎเหล็ก · 国士 = ผู้อุทิศตนเพื่อแผ่นดิน · 雑炊 = โซซุย · `lapdog` = ลูกไล่
· `Rokumoncho` = โรคุมนโจ · `iki` = อิคิ · 御館様 = ท่านเจ้านาย · `retainer` (付き人) = ข้ารับใช้
≠ `Bakufu retainers` (幕府の家臣) = ข้าราชบริพารของรัฐบาลโชกุน

**คำพ้องรูปที่ยืนยัน**: `Uncle Hajime` (おじさん) = **ลุงฮาจิเมะ** ≠ `Mister <ชื่อ>` = **พี่\<ชื่อ\>**
(สองรูปนี้ **ไม่ขัดกัน** — คนละสตริง คนละต้นฉบับ · lead เคยอ่านกฎผิดแล้วสั่งนักแปลผิด)
· `geezer` หยาบ = ไอ้แก่ / オッサン = ตาแก่ · `Arms Dealer` ป้ายร้าน = ร้านอาวุธ / บรรยายคน = พ่อค้าอาวุธ
· `Trooper` มีเลขหน่วย = ทหารหน่วย / สมาชิกทั่วไป = ทหาร + ชื่อหน่วย

⚠ **"จัง" ในคำแปลไทยไม่ใช่คำต่อท้ายชื่อเสมอไป** — "อยากฟังเพลงของลุงฮาจิเมะจัง" คือคำวิเศษณ์
การกวาดคำต่อท้ายชื่อต้องกรองด้วย **สตริง EN ที่มี `-chan` จริง** ห้ามกวาดจากคำไทยล้วน

### 5. คำตัดสินเนื้อเรื่องที่ปิดในคลื่นนี้

- **การเปิดเผยชื่อจริงใน MSG_041** — ผู้ตรวจไล่ครบ **250/250 คีย์** สองชั้น (สคริปต์ไขว้ `ref_ja` × EN × TH
  แล้วอ่านด้วยตาทั้งก้อน): **ไม่มีบรรทัดใดที่คำแปลเติมชื่อเข้าไปเองเลย** · `ref_ja` เอ่ย 龍馬 จริง 27 คีย์
  · จุดสารภาพ idx 247–248 ต้นฉบับไม่มีชื่อ คำแปลไม่เติม ✔
- **เรียวมะ → คนโด/ฮิจิกาตะ = T1 (ข้า ↔ ท่าน) ทุกฉาก** รวมฉากหลังเปิดเผยตัวตน — กวาดจุดที่ใช้ "เจ้า" แล้ว
- **เนื้อความยึด EN เสมอ** (cheatsheet §4.5) — MSG_040 idx 233 เคยยึด `ref_ja` (褒めるしかない)
  ขณะที่ EN เขียนใหม่เป็น "recruit the guy" → แก้ตาม EN เพราะผู้เล่นอ่าน EN เป็นฐาน

### 6. เครื่องมือใหม่ของ sprint 11–12

| ไฟล์ | ทำอะไร | ใช้เมื่อไหร่ |
|---|---|---|
| `scripts/scene_audit.py` | ดึงบรรทัดที่ใช้ "เพศระดับฉาก" อย่างเดียวออกมา **พร้อมฉากเต็ม** (JA/EN/TH + ป้ายผู้พูด + เครื่องหมายเพศรายบรรทัด) → `work/audit/scene_MSG_0NN.md` | ตอนตรวจย้อนหลังว่ามีบรรทัดไหน "ยืมเพศ" จากตัวละครอื่น |
| `scripts/bond_gender.py` | ดึงเพศ NPC สายสัมพันธ์จากสรรพนามในป้ายอังกฤษของเกม ("…interact with **him/her**…") → `translations/bond_gender.json` + `docs/reference/bond_npc_gender.md` (**45 ตัว**) | รันใหม่เมื่อ `extracted/parallel/msg.json` เปลี่ยน · เป็นตารางให้คนอ่าน **ไม่ได้เข้าด่าน G อัตโนมัติ** |
| `scripts/check_gender_lines.py` | ตรวจ `gender_lines.json`: ทุกคีย์ต้องมี `why` และต้องเป็นสตริงที่โผล่ในไฟล์ฉากเดียว | **ด่านบังคับข้อ 3.5 ก่อนแจกคลื่นทุกครั้ง** |
| `translations/gender_lines.json` | คำตัดสินเพศ **รายบรรทัด** ของ lead — ชั้นหลักฐานสูงสุดของด่าน G | เมื่อบรรทัดพิสูจน์เพศได้จากไฟล์เกมด้วยหลักฐานที่เครื่องอ่านเองไม่ได้ |

### 6.5 เครื่องมือใหม่ของ sprint 13–14 (เพิ่มจากตารางข้างบน)

| ไฟล์ | ทำอะไร | ใช้เมื่อไหร่ |
|---|---|---|
| `scripts/make_wave_brief.py` | สร้างบรีฟรายก้อน `work/brief/batch_NNN.brief.md` จาก `prior.json` + `name_locks` + `place_locks` + ตารางเจ้าของรูปของฉากร่วม | **ทุกครั้งก่อนแจกคลื่น** — เลิกให้ lead พิมพ์คำล็อกจากความจำ |
| `scripts/check_thai_marks.py` | ตรวจลำดับสระ/วรรณยุกต์ไทยที่**จอแสดงผลไม่ได้** (สระซ้อนสระ · วรรณยุกต์ซ้อน · วรรณยุกต์มาก่อนสระ · ตัวประกอบลอย) | **ด่านบังคับข้อ 1.5** ก่อนแจกคลื่น |
| `scripts/check_name_forms.py` | หา **ชื่อ EN หนึ่งคำที่ถูกทับศัพท์เป็นไทยหลายรูป** — กรองจากฝั่งอังกฤษ จึงแยก "ชื่อเดียวสะกดต่างกัน" ออกจาก "คนละชื่อจริง ๆ" ได้ | หลัง merge ทุกคลื่น |
| `scripts/sweep_name_forms.py` | กวาดรูปทับศัพท์ตามตารางคำตัดสินใน glossary §1.9.21 | เมื่อ `check_name_forms` ชี้ drift ใหม่ |
| `scripts/sweep_maiyamok.py` | กวาดไม้ยมกให้เว้นวรรคข้างหน้าเสมอ | รันซ้ำได้ตลอด (idempotent) |

### 7. ต้นทุน token ที่วัดได้จริง (จากระบบ ไม่ใช่ที่ agent รายงานเอง)

| บทบาท | sprint 11 | sprint 12 |
|---|---|---|
| นักแปลชั้น `.msg` หนึ่งก้อน | 172k – 240k | 213k – 270k |
| ผู้ตรวจที่ถือ 3 ก้อน | 267k – 283k | 293k – 358k |
| ผู้ตรวจย้อนหลัง (400 บรรทัด) | 202k – 209k | — |
| **รวมต่อคลื่น (6 ก้อน + ผู้ตรวจ 2 คน)** | **~1.8M** | **~2.0M** |

งานที่เหลือ **MSG_043–083 = 41 ก้อน ≈ 7 คลื่น ≈ 14M**

⚠ ต้นทุนต่อก้อนสูงขึ้นจาก sprint 10 (137k–284k) เพราะบทเนื้อเรื่องหลักต้องอ่าน `ref_ja` ทุกบรรทัด
และต้องค้น `master_th.json` ก่อนตั้งรูปคำทุกคำ · **ตัวที่ลดต้นทุนได้จริงคือใส่คำล็อกลงบรีฟตรง ๆ**
และ `evidence_gender` ในไฟล์บริบท (sprint 12 §1) ซึ่งตัดรอบ "นักแปลเขียนกลางเพศ → ผู้ตรวจเติมคืน" ออก

---

---

## 🆕 sprint 11 — สิ่งที่เปลี่ยนไป (อ่านก่อนเริ่มคลื่นถัดไป)

### 1. งานค้าง 400 บรรทัด "หลักฐานระดับฉากอย่างเดียว" — **ปิดครบแล้ว**

ผู้ตรวจสองคนอ่านฉากจริงทีละจุด ด้วยเครื่องมือใหม่ `scripts/scene_audit.py` ซึ่งดึงบรรทัดต้องสงสัย
ออกมาพร้อมฉากเต็ม (JA/EN/TH + ป้ายผู้พูด + เครื่องหมายเพศรายบรรทัด) → `work/audit/scene_MSG_0NN.md`

| ชุด | ตรวจ | คงไว้ | ถอน |
|---|---:|---:|---:|
| MSG_009 · 010 · 012 · 013 | 171 | 138 | **33** |
| MSG_015 · 017 · 018 · 019 · 020 · 021 · 022 · 029 · 030 | 229 | 229 | 0 |

ที่ถอนคืออาการยืมเพศแท้ ๆ ทั้งหมด: **แม่เกลียดผัก · ซูมิเระ · จดหมายของโอคินุ** เป็นหญิง
แต่ได้ "ขอรับ" มาจากบรรทัด 俺 ของเรียวมะที่อยู่ในไฟล์ `.msg` เดียวกัน
ส่วน 229 บรรทัดที่คงไว้เป็นฉากสายสัมพันธ์ NPC เดี่ยว (มีแค่ NPC กับเรียวมะ) พิสูจน์ได้ทุกบรรทัด

### 2. ชั้นหลักฐานเพศเพิ่มเป็นห้าชั้น

| ชั้น | หลักฐาน | ที่อยู่ |
|---|---|---|
| 0 | **คำตัดสินรายบรรทัดของ lead** (ชั้นสูงสุด) | `translations/gender_lines.json` — **ทุกคีย์ต้องมี `why` ที่อ้างไฟล์/บรรทัดในเกม ไม่มี = ไม่นับ** · ตอนนี้ 32 คีย์ |
| 1 | ป้ายผู้พูดในไฟล์บริบท | `*.context.json` |
| 2 | เครื่องหมายเพศในบรรทัด `ref_ja` เอง | `merge_qc.ja_gender()` |
| 3 | เพศระดับไฟล์ฉาก | `translations/scene_gender.json` |
| 4 | **สรรพนามบุรุษที่สามในข้อความอังกฤษของเกม** | `translations/bond_gender.json` · `docs/reference/bond_npc_gender.md` (NPC **45 ตัว** — ชาย 35 · หญิง 10) |
| 5 | **ป้ายคิวเสียงที่เป็นคำเรียกบอกเพศ** | ช่อง `voice` ในไฟล์ฉาก (`haha_c01_020_01` → haha = 母) — มีคิวเสียง 4,113/54,318 บรรทัด |

⚠ **ชั้น 4 ไม่ได้เข้าด่าน G อัตโนมัติ — วัดแล้วไม่ผ่านเกณฑ์**
ป้ายสายสัมพันธ์บอกเพศของ**ตัวละคร**ได้แน่ แต่การชี้ว่า *บรรทัดไหน* เป็นบทของเขาไม่แน่:
ป้ายผู้พูดชั้น `.msg` ติดมาหลวม ๆ (แถวเดียวมีหลายป้าย) จับคู่อัตโนมัติแล้ววัดได้ **ตรง 17 · ขัด 6 = 74%**
ต่ำกว่าเกณฑ์ 99.2% ที่ `scene_gender` ใช้มาก → เป็น **ตารางให้คนอ่าน** ถ้าจะใช้กับบรรทัดไหนให้ลง `gender_lines.json`

### 3. บั๊กตัวตรวจที่คลื่นนี้ทำให้เจอ (นักแปล/ผู้ตรวจรายงาน ไม่ได้ดัดคำแปลหลบ)

| ตัวตรวจ | อาการ | วิธีแก้ |
|---|---|---|
| `JA_FEMALE_RE` | ไม่มีคาตากานะ `アタシ` (ฝั่งชายมี `オレ` อยู่แล้ว) → บทของยาเอะที่ใช้ アタシ ทั้งฉากไม่มีหลักฐานเพศ | เพิ่ม `アタシ` · `あたい` · `ワタクシ` |
| `JA_FEMALE_RE` | `かしら` ไม่มีขอบเขต จึงไปตรงกลางคำ `なんだかしらけちまった` (บทนักเลงชาย) | ใส่ขอบเขตท้ายวรรค · **แต่แคบเกินไปจนบท Sexy Madam (`かしらん`) ตก 71 บรรทัด** → ขยายให้รับ `かしらん · かしらね · かしらって` |
| `RE_THAN` | ไม่กัน `ท่านเจ้านาย` (御館様) · `ท่านไทโร` (大老) ซึ่งเป็นคำเรียกบุคคลที่สาม | เพิ่มเข้า lookahead |
| `RE_CHAO` | ไม่กัน `เจ้าแคว้น` · `เจ้าหนุ่ม` | เพิ่มเข้า lookahead |
| `RE_KHORAP` | `ขอรับน้ำใจ` (กริยา + กรรม) ถูกนับเป็นคำลงท้าย | เพิ่ม `น้ำใจ` · `ไมตรี` |
| `MODERN_LOANWORDS` | เทียบแบบซับสตริง → `ดีล่ะ` กลายเป็นคำยืม "ดีล" · `โกลเวอร์` (Thomas Glover) กลายเป็น "เวอร์" | ทำ `tp.modern_loanwords()` ที่ตัดคำพ้องออกก่อนด้วย `LOAN_FALSE_HITS` |
| `POLITE_JA` | ไม่รู้จักรูปสุภาพสำเนียงคันไซ (`でっか · まっせ · まへん · くだはれ · ましたわ · どすか`) → ด่าน J เตือนบรรทัดที่ต้นฉบับสุภาพจริง | เพิ่มเข้าตาราง (**ไม่ใส่ `やす`** เพราะไปตรงกับ `やすい`) |
| `scene_gender` | ให้เพศกับบรรทัดที่ต้นฉบับเป็นข้อความใน `『…』` ล้วน (จดหมายของโอคินุ = เสียงหญิงในฉากของฟูจิเอะ) | `build_scene_gender.py` ตัดคีย์พวกนี้ออก |
| `merge_qc --only` | จับแบบ substring → `--only 022` ลาก `batch_MSG_022` มา merge ด้วย | จับชื่อไฟล์ตรงตัว + ใส่ `--only` ซ้ำได้หลายก้อน |
| `check_pronoun_pairs` | ไม่มีข้อยกเว้น "เอกสารยาว" ที่ด่าน P มี จึงขึ้น "พบปัญหา 2 จุด" ทุกครั้งทั้งที่ merge ผ่าน | ย้าย `is_document()` ลงไป · ตอนนี้สองเครื่องมือรายงานตรงกัน |

`test_qc_tools.py` **85 → 100 เคส ผ่านหมด**

### 4. คำล็อกใหม่ · คำที่กวาดย้อนหลัง (glossary §1.9.18)

**คำล็อกใหม่**: `Hokushin Itto` (北神一刀流) = **สำนักโฮคุชินอิตโต** · `Shindo Munen` (心道無念流) = **สำนักชินโดมุเน็น**
· `retainer` (付き人) = **ข้ารับใช้** · `Rokumoncho` (六文町) = **โรคุมนโจ** (ลง `place_locks.json` แล้ว รวม 144 ป้าย)
· `iki` (粋) = **อิคิ** · 御館様 / `milord` = **ท่านเจ้านาย** · `About Fishing` = ว่าด้วยการตกปลา · `bobber` = ทุ่น

⚠ **เกมสะกดคันจิสำนักดาบต่างจากประวัติศาสตร์โดยตั้งใจ** (天念理心流 ≠ 天然理心流 · 北神一刀流 ≠ 北辰一刀流
· 心道無念流 ≠ 神道無念流) — **ถอดเสียงตามที่เกมเขียน ห้ามแก้ให้ตรงประวัติศาสตร์**

**กวาดทั้งคลัง** (แก้ที่ไฟล์ `done` แล้ว merge ใหม่ ตามกติกาเหล็กข้อ 4):

| คำ | แก้เป็น | จำนวน |
|---|---|---|
| `Saito-san` ที่แปลว่า "ท่านไซโต" | **ไซโตซัง** (รูปข้างมาก 144:102 · เข้าชุดกับ ซาโตซัง / โทกูงาวะซัง) | **104 จุด · 20 ก้อน** |
| ทับศัพท์ชื่อเสียงยาว 三郎 / 七郎 / 秀三 / 重三 / 文十三 / 彦助 | **-ซาบุโร · -ชิจิโร · ชุโซ · จุโซ · มนจุโซ · ฮิโคสุเกะ** | 13 |
| `The Amnesiac` · `Injured Grandma` · `Battered Defender` | ชายผู้จำอะไรไม่ได้ · ยายที่บาดเจ็บ · ผู้พิทักษ์ที่บาดเจ็บ | 6 |
| 竹田 · 小五郎 · `Satow` · `Bathkeeper of Sai` | ทาเคดะ · โคโงโร · ซาโตว์ · เจ้าของโรงอาบน้ำแห่งไซ | 6 |
| `Shinsengumi/Mimawarigumi Trooper` (คนจริง ไม่มีเลขหน่วย) | **ทหารชินเซ็นกุมิ / ทหารมิมาวาริงุมิ** | 2 |

`Trooper` เป็นคำพ้องรูป: ป้ายระบบการ์ดและป้ายที่มีเลขหน่วยยังเป็น **ทหารหน่วย** (คำว่า "หน่วย" ตรงนั้นแปล Division)
ส่วนที่หมายถึงสมาชิกทั่วไปของหน่วยจริงใช้ **ทหาร + ชื่อหน่วย** — เขียนลง cheatsheet §4.1 แล้ว

⚠ **ห้ามไล่แก้ให้เหมือนกันโดยดูรูปโรมาจิอย่างเดียว** — สระเสียงสั้นยังต้องเติม ะ (田代 = **ทาชิโระ** · 助 = **-สุเกะ**)
และนามสกุลที่เขียนใกล้กันเป็นคนละคนจริง (Nakao ≠ Nagaoka · Ogata ≠ Okita · Takei ≠ Takechi · Kano ≠ Kamo)

### 5. คำตัดสินคำลงท้ายบอกเพศของคลื่นนี้

- **ยายที่บาดเจ็บ (MSG_031) = ใส่ "จ้ะ/จ๊ะ"** — ตัวชี้ขาดคือ master เอง บทของยายคนเดียวกันในไฟล์ฉาก
  `uid006e0196` ship คำลงท้ายหญิงไปแล้ว 5 บรรทัด ถ้าปล่อยกลางเพศ ตัวละครเดียวกันจะพูดสองน้ำเสียงในฉากเดียว
- **ผู้พิทักษ์ที่บาดเจ็บ (MSG_031) = คืน "ขอรับ" 14 บรรทัด** แต่ **ไม่ใส่ให้หญิงสาวที่ถูกรังควานกับนักเลง** ในฉากเดียวกัน
- **เจ้าของร้านเหล้า (MSG_033) = ใส่ "เจ้าค่ะ"** — หลักฐานคือโรนินตอบว่า "You bitch!" / 「ババア！」 ที่ `uid01021b90#018`
- **ฮิจิกาตะ / โอโตเซะ / โอเรียว = ใส่ที่ความหนาแน่นต่ำ** — MSG_036 อ้างว่า master ไม่เคยใส่เลย **แต่นับผิด**
  นับใหม่จากไฟล์บริบททุกไฟล์ได้ ฮิจิกาตะ 21/244 · โอโตเซะ 18/309 · โอเรียว 8/285 (3–9%) → **ไม่ต้องกวาดย้อนหลัง**

⚠ **บทเรียนที่ย้ำ**: ตัวเลขที่ agent รายงานเองต้องนับซ้ำก่อนใช้ตัดสิน — ผู้ตรวจจับเรื่องนี้ได้เพราะนับใหม่ทั้ง master

---

## 🆕 sprint 10 — สิ่งที่เปลี่ยนไป (อ่านก่อนเริ่มคลื่นถัดไป)

### 1. ตัดไฟล์ `.msg` ที่ RGG ไม่เคยแปลเป็นอังกฤษออกจากคิว — **2,327 สตริง (11% ของชั้น .msg)**

`extracted/parallel/msg.json` บอกว่ามี **99 ไฟล์ · 3,770 บรรทัด** ที่ทุกแถว `en == ja` และเนื้อในเป็นญี่ปุ่น
สามไฟล์ใหญ่สุด (`uid0134003a-c` · 1,584 บรรทัด) เป็น **บทคุยโฮสเตสของ "桐生" (คิริว)** ตัวละครที่ไม่มีในภาคนี้
อีกไฟล์เป็นไฟล์ทดสอบของทีมพัฒนา (ป้ายผู้พูด `動作確認さん` · `TURNHEAD。` · `質問会話05。`)
ตัวเลข/หลักฐานเต็ม: `docs/research.md` §14

- `scripts/mark_dnt.py` เพิ่มกฎ `dead_msg_files()` (ทุกแถว en==ja **และ** ≥50% ของแถวมีอักษรญี่ปุ่น)
- **MSG_066–071 เป็น DNT 100% ทั้งหกก้อน** → เขียน done แบบ copy ตรง + merge เข้า master แล้ว
- ⚠ ที่คัดออกคือ **ไฟล์ที่ไม่เคยแปลทั้งไฟล์** เท่านั้น · บรรทัดญี่ปุ่นที่ปนในไฟล์ที่แปลแล้ว (174 บรรทัด)
  ยังอยู่ในคิว เพราะบรรทัดอังกฤษรอบ ๆ ต้องแปล

### 2. เครื่องมือใหม่/ที่แก้

| ไฟล์ | สิ่งที่ทำ |
|---|---|
| `scripts/make_todo.py` | **ใหม่** — สร้าง `batch_NNN.todo.json` = worklist ลบคีย์ DNT (พก `ref_ja`/`ref_tm` ไปด้วย) · สร้างครบ 83 ก้อน MSG แล้ว |
| `scripts/mark_dnt.py` | เพิ่ม `dead_msg_files()` · รับชื่อก้อนแบบ `MSG_007` ได้ (เดิม `zfill(3)` ทำให้ทุกก้อน MSG เขียนทับไฟล์ `batch_MSG.dnt.json` อันเดียว) |
| `scripts/assemble_done.py` | รับชื่อก้อนแบบ `MSG_007` ได้ |
| `scripts/merge_qc.py` | (ก) `HONORIFIC_NOT_NAME_EN` — `Cho-han` ไม่ใช่ชื่อคน+คำต่อท้าย (ข) **ด่าน G รับหลักฐานเพศจากต้นฉบับญี่ปุ่น** (ดูข้อ 3) |
| `scripts/check_glossary_locks.py` | (ก) ข้ามแถวตารางที่ช่องซ้ายเป็นโค้ดล้วน (`` `NAME` `` = ชื่อคอลัมน์ ไม่ใช่คำล็อก) (ข) `tags` · `heat` เข้ารายการ `CONTEXT_ONLY` |
| `scripts/test_qc_tools.py` | **75 เคส ผ่านหมด** (เพิ่ม Cho-han + สามเคสของด่าน G แบบใหม่) |
| `scripts/normalize_terms.py` | **ลบทิ้ง** — เป็นไฟล์ของ Lost Judgment ที่หลุดออกมานอก `scripts/ref_lj/` (กฎ 191 ข้อเป็นของ LJ ทั้งหมด · มีกฎ `โชฮัง → โจฮัง` ที่จะทำลายคำล็อกของภาคนี้ถ้าเผลอรัน) · ฉบับอ้างอิงยังอยู่ที่ `scripts/ref_lj/normalize_terms.py` |

### 3. ด่าน G ยอมรับ "เครื่องหมายบอกเพศในต้นฉบับญี่ปุ่น" เป็นหลักฐานแล้ว

**ปัญหาที่เจอ**: ชั้น `.msg` รู้ผู้พูดแค่ 13% ไฟล์บริบทจึงตี `neutral: true` ให้ **17,968 จาก 20,700 บรรทัด**
แต่ในจำนวนนั้น **1,967 บรรทัดมีเครื่องหมายบอกเพศชัดในต้นฉบับ** (ชาย 1,378 · หญิง 589)
เช่นบทของซามูไรที่พูด `拙者/でござる` ทุกบรรทัด — เป็นชายแน่นอน แต่ด่าน G ตีคำว่า "ขอรับ" ตก
ผลคือนักแปลสองก้อน **ตัดคำลงท้ายทิ้ง 17 บรรทัด** ทั้งที่ต้นฉบับสุภาพจริง

**วิธีแก้** (ตรงกับลำดับหลักฐานใน `docs/reference/gender_evidence_ishin.md`: เครื่องหมายในต้นฉบับ = อันดับ 2):
`merge_qc.ja_gender()` อ่าน `拙者・でござる・俺・僕・ワシ・だぜ` = ชาย · `わよ・わね・かしら・あたし・のよ` = หญิง
- คำไทยที่ใช้ **ฝั่งเดียวกับต้นฉบับ** → ผ่าน (เป็นคำเตือน)
- คำไทยที่ **สวนทางกับต้นฉบับ** → ตกเหมือนเดิม (เป็นการเดาผิดจริง)
- ต้นฉบับไม่ชี้เพศ → ตกเหมือนเดิม

ผลจริง: MSG_003 ได้คำลงท้ายคืน 5 บรรทัด (ตัวละครที่พูด 拙者/でござる) · MSG_002 ตรวจซ้ำแล้ว **ไม่เข้าเงื่อนไข**
เพราะ `ref_ja` ใช้ くだされ/ください ซึ่งเป็น keigo ที่ไม่บอกเพศ — การตัดคำลงท้ายออกถูกต้องแล้ว
⏳ ทางที่ดีกว่าในระยะยาว: ให้ `make_batch_context_ishin.py` เขียนเพศจากเครื่องหมายญี่ปุ่นลงไฟล์บริบทเลย
แล้ว regenerate ทั้ง 168 ไฟล์ (ยังไม่ได้ทำ — ทำแล้วนักแปลจะเห็นเพศตั้งแต่แรก ไม่ต้องพึ่งด่านตรวจ)

### 4. คำล็อกใหม่ · คำที่กวาดย้อนหลัง

`glossary.md` **§1.9.17**: ธาตุแสง (閃) · ธาตุสายฟ้า (雷) · `Captain` ตะโกนลอย ๆ = **ท่านหัวหน้าหน่วย**
· magnitude ladder (เล็กน้อย/ปานกลาง/มาก/มหาศาล/มหาศาลสุดขีด) · Rarity = ระดับหายาก

**กวาดย้อนหลังสองชุด** (ทั้งคู่แก้ที่ไฟล์ `done` แล้ว merge ใหม่ ตามกติกาเหล็กข้อ 4 — ห้ามแก้ master ตรง ๆ):

1. `Thunder Rounds` เคยเป็น "กระสุนไฟฟ้า" 2 จุดในก้อน 019 ขณะที่อีก 4 จุดใช้ "กระสุนสายฟ้า"
2. **คำต่อท้ายชื่อ `-taro` (太郎) เขียนปนกันสามแบบ 23 ชื่อใน 6 ก้อน** (022 · 024 · 071 · 072 · 073 · MSG_006)
   — "ตาโระ" · "ตาโร" · "ทาโร" · และ `Hikotaro` มีทั้ง "ฮิโคทาโร" กับ "ฮิโกตาโร"
   → ล็อกเป็น **"ตาโร"** (สระเสียงยาวไม่เติม ะ ตามกฎเดียวกับ โทชิโซ · ไซโต) · `Hikotaro` = **ฮิโคตาโร**
   ⚠ `romaji_to_thai.py` ให้ "ฮิโกตาโระ" ผิดสองจุด (k หลัง i กลายเป็น ก · เติม ะ ท้าย) — เข้ารายการบั๊กใน §1.9.12
   ⚠ ระวัง: คีย์ `Taro` เดี่ยว ๆ ในก้อน 022 = **เผือก** (พืช) ไม่ใช่ชื่อคน — การกวาดต้องไม่แตะคีย์นั้น

### 5. คำตัดสินทะเบียนตัวละครของคลื่นนี้

**โอกิตะ โซจิ (`id:majima`) = T2 (ข้า ↔ เจ้า) ทุกฉาก** — ล็อกลง `PRONOUN_MATRIX.md` §2 แล้ว
ผู้ตรวจจับได้ว่า MSG_001 ใช้ T3 (กู/มึง) ขณะที่ MSG_002 · MSG_003 ใช้ T2 กับตัวละครเดียวกัน
หลักฐานที่ใช้ตัดสิน (`translations/speakers.json`): เขาใช้ わし 44 ครั้ง · 俺 2 ครั้ง = **ระดับเดียวตลอดเกม**
ที่ EN หยาบเป็นบางฉากเป็นการเขียนบทใหม่ของทีมอังกฤษ → **ความหยาบสื่อด้วยคำลงท้าย/คำด่า
ไม่ใช่ด้วยการเปลี่ยนคู่สรรพนาม** · แก้ย้อนหลัง 8 บรรทัดใน MSG_001 แล้ว

### 6. น้ำเสียงหญิงลำลอง — คำลงท้าย "จ๊ะ / จ้ะ" (เคาะ 3 ก.ย. 2026)

ต้นฉบับญี่ปุ่นแยกสองแกน: **ความสุภาพ** (です・ます) กับ **เพศผู้พูด** (わよ · のよ · かしら)
คำไทยเดิมของโปรเจกต์มีแต่ "เจ้าค่ะ" ซึ่งกินทั้งสองแกน บทหญิงที่ต้นฉบับ**ลำลองแต่บอกเพศชัด**จึงไม่มีที่ลง

| ต้นฉบับ | ไทย |
|---|---|
| です・ます + เครื่องหมายหญิง | เจ้าค่ะ / เจ้าคะ |
| ลำลอง + เครื่องหมายหญิง | **จ๊ะ / จ้ะ** |
| ลำลอง ไม่บอกเพศ | ไม่ใส่คำลงท้ายบอกเพศ |

**ไม่ใช่ "จ้า"** — คำนั้นล็อกเป็นคำลงท้าย**กลางเพศ**ของพ่อค้าแม่ค้าไว้แล้ว (glossary §1.9.12)
`thai_pronouns.FEM_CASUAL` + ด่าน G รู้จักแล้ว · เอกสาร: `PRONOUN_MATRIX.md` §4 ข้อ 1.5

ลงจริงแล้ว: MSG_005 4 บรรทัด · MSG_006 12 บรรทัด (ไม่ใส่ 47 บรรทัดกลางฉากตามกฎความหนาแน่น)
· กวาดย้อนหลัง batch_040 สามบรรทัด: พ่อค้าที่ต้นฉบับไม่บอกเพศเคยใช้ "จ้ะ" → เปลี่ยนเป็น **"จ้า"**

**บั๊กของตัวจับเครื่องหมายที่แก้ในรอบนี้** (เจอเพราะตัวตรวจตีคำแปลที่ถูกต้องตก):
- `わし` ไปตรงกลางคำกริยา **交わした** (= ให้สัญญา) → ต้องตามด้วยคำช่วยเท่านั้น
- `のよ` ไปตรงกับ **のような / のように** (= เหมือน) และ **いきのよい** (= สด) → ต้องอยู่ท้ายประโยค
  (รับเครื่องหมายลากเสียง `ー` และสระเล็กท้ายด้วย — `心配したのよー！`)
- `test_qc_tools.py` เพิ่มเป็น **79 เคส ผ่านหมด**

### 7. เพศผู้พูดระดับ "ไฟล์ฉาก" — `scripts/build_scene_gender.py` (ใหม่ 3 ก.ย. 2026)

ด่าน G เดิมรับหลักฐานเฉพาะ **บรรทัดนั้นเอง** ผลคือตัวละครเดียวกันได้คำลงท้ายบ้างไม่ได้บ้าง
สลับกันในฉากเดียว (นักแปลก้อน MSG_012 ต้องถอน "ขอรับ" ของฟูจิเอะออก 72 บรรทัด ทั้งที่ต้นฉบับ
ใช้ 僕/です・ます ตลอด — เหลือแค่ 9 บรรทัดที่บรรทัดนั้นเองมีเครื่องหมาย)

วิธีแก้: บทหนึ่งไฟล์ `.msg` = หนึ่งฉาก · ถ้าทั้งไฟล์มีเครื่องหมายเพศเดียวล้วน **ตั้งแต่ 3 ตัวขึ้นไป**
และไม่มีของอีกเพศปนเลย ให้ถือว่าฉากนั้นเป็นเพศนั้น

**ความแม่นที่วัดจริง** (เทียบกับบรรทัดที่รู้ป้ายผู้พูดแน่นอน 4,476 บรรทัด):

| เกณฑ์ | ไฟล์ที่ชี้ได้ | ชายตรง | หญิงตรง |
|---|---:|---:|---:|
| ≥1 | 408 | 97.4% | 100% |
| **≥3 (ที่ใช้)** | **185** | **99.2%** | **100%** |

ได้เพศเพิ่ม **9,763 สตริง** (ชาย 9,543 · หญิง 220) เขียนไว้ที่ `translations/scene_gender.json`
`merge_qc.py` ด่าน G ใช้เป็นหลักฐานชั้นรองแล้ว (บรรทัดเอง > ฉาก) · รันใหม่เมื่อ `extracted/parallel/msg.json` เปลี่ยน

⏳ **ยังไม่ได้ทำ**: ใส่ "ขอรับ" กลับให้ฟูจิเอะใน MSG_012 (ตอนนี้ระดับฉากรองรับ 249 บรรทัดแล้ว)
และตรวจซ้ำก้อนอื่นที่นักแปลถอนคำลงท้ายทิ้งเพราะกฎเดิม — สั่งผู้ตรวจคลื่นถัดไป

### 8. ⚠ งานตรวจย้อนหลังที่ยังค้าง — 400 บรรทัดที่ใช้ "หลักฐานระดับฉาก" อย่างเดียว

**อาการ**: `scene_gender.json` ตัดสินจากทั้งไฟล์ `.msg` ว่าเป็นเพศเดียวล้วนหรือไม่
ฉากที่มีหลายคนพูดสลับกันจึงอาจ **"ยืมเพศ" จากตัวละครอื่น** มาให้บรรทัดที่ไม่มีหลักฐานของตัวเอง

**เจอจริงในก้อน MSG_025**: ฉากนักข่าวคาวาราบังสัมภาษณ์ไซโต — บทของนักข่าวไม่มีหลักฐานเพศเลย
แต่ผ่านด่าน G ได้เพราะไฟล์นั้นมี 俺/僕 ของ **ไซโตเอง** อยู่ · ผู้ตรวจถอน "ขอรับ" ออก **58 บรรทัด**

**ขนาดของปัญหาที่เหลือ**: ทั้งคลังมี **400 บรรทัด** ที่ใช้คำลงท้ายบอกเพศโดยไม่มีเครื่องหมายในบรรทัดตัวเอง
กระจายมากสุดที่ MSG_013 (83) · MSG_019 (59) · MSG_029 (47) · MSG_009 (45) · MSG_030 (45)

หา list ด้วยสคริปต์สั้น ๆ นี้ (เทียบ `ja_gender()` กับ `scene_gender()` รายบรรทัด):
```python
import json, glob, io, sys
sys.path.insert(0, "scripts"); sys.stdout.reconfigure(encoding="utf-8")
import merge_qc as M, thai_pronouns as tp, paths
for p in sorted(glob.glob("translations/done/batch_MSG_*.done.json")):
    b = p.split("batch_")[1].replace(".done.json", "")
    wl = json.load(io.open(paths.WORKLIST / ("batch_%s.json" % b), encoding="utf-8"))
    ref, ctx = wl.get("ref_ja") or {}, M.load_context("batch_%s.json" % b)
    for en, th in json.load(io.open(p, encoding="utf-8"))["strings"].items():
        info = ctx.get(en) or {}
        if en == th or not M.is_neutral(info) or not tp.POLITE_OLD.search(th):
            continue
        if M.ja_gender(ref.get(en) or info.get("ja") or ""):
            continue                      # มีหลักฐานในบรรทัดเอง = ปลอดภัย
        if M.scene_gender(en):
            print(b, repr(en)[:60], repr(th)[:60])
```

**วิธีตัดสิน**: อ่านฉากนั้นแล้วดูว่าบรรทัดนี้เป็นบทของ**ใคร** ถ้าเป็นตัวละครที่ไม่มีหลักฐานเพศของตัวเอง
ให้ถอดคำลงท้ายออก (เขียนกลางเพศตาม §1.3) · ถ้าเป็นบทของตัวละครที่มีหลักฐาน ให้คงไว้
**ห้ามแก้เป็นชุดด้วย regex** — ต้องอ่านฉากทีละจุด

### 9. เครื่องมือใหม่ของ sprint 10 (นอกจากที่ระบุข้างบน)

| ไฟล์ | ทำอะไร |
|---|---|
| `scripts/batch_overlap.py` | บอกว่าก้อนไหนใช้ไฟล์ฉาก `.msg` ร่วมกับก้อนไหน + ก้อนนั้นแปลไปแล้วหรือยัง — ใช้ก่อนแจกงานทุกคลื่น |
| `scripts/check_bond_names.py` | เทียบชื่อ NPC สายสัมพันธ์สามป้าย (`Bond with X` · `Deepen your bond…` · `You have formed a bond…`) · รันครั้งแรกเจอ **8 ตัวจาก 74 ไม่ตรงกัน** |
| `scripts/build_scene_gender.py` | เพศผู้พูดระดับไฟล์ฉาก (ดู §9) |
| `scripts/make_todo.py` | คิวแปลที่ตัด DNT ออก |

**คำที่กวาดทั้งคลังในคลื่น MSG_019–024** (เจอเพราะนักแปล/ผู้ตรวจรายงานว่าคำใน `master` ปนกันเอง):
`Prize Tickets` = ตั๋วจับสลาก · `Frequently Lost Courier` = **คนเดินสาร**ขี้หลงทาง (ยุคนี้ไม่มีไปรษณีย์)
· `Wood Cutter` = คนตัดฟืน · `Utamaruya` = อุตามารุยะ · `Momokawa` = โมโมกาวะ · `Fukufuku` = ฟุคุฟุคุ
· `Shieikan` = ชิเออิกัน · `informant` = สายข่าว · `Greengrocer` (คน) = เจ้าของร้านผัก
· `palanquin bearer` = คนหามเกี้ยว · เทมเพลตข้อความสายสัมพันธ์เหลือรูปเดียว

### 10. บั๊กตัวตรวจที่เจอในคลื่น MSG_013–018 (แก้ครบแล้ว · `test_qc_tools.py` = 84 เคส)

ทุกตัวเจอเพราะ **นักแปลรายงานแทนที่จะดัดคำแปลหลบ** (กติกา §7.5) — คุ้มค่าที่สุดของโปรเจกต์นี้

| ตัวตรวจ | อาการ | วิธีแก้ |
|---|---|---|
| `RE_THAN` | จับ **"ท่านี้"** (ท่า + นี้ = ท่าต่อสู้) เป็นสรรพนาม "ท่าน" | เพิ่ม lookahead กันสระอี/อิ |
| `RE_KHUN` | จับ **"ขอบพระคุณ"** เป็นสรรพนาม "คุณ" → นักแปลเลี่ยงไปใช้ "ขอบพระทัย" (คำของเจ้าฟ้า ไม่ใช่สามัญชน) | เพิ่ม `(?<!พระ)` แล้วคืนคำเดิม 6 จุด |
| `RE_KHORAP` | `ขอรับ(?![ก-ฮเแโใไ])` กว้างเกิน — **"ขอรับท่าน"** หลุดทั้งด่าน G และ J | เปลี่ยนเป็นบัญชีรูปกริยา (`ใช้·ผิด·รอง·ประทาน·หน้าที่`…) · เจอบรรทัดที่หลุดจริง 2 จุด |
| `JA_MALE_RE` | จับสรรพนาม 俺 ที่อยู่ใน **คำพูดที่ยกมาอ้าง** 「俺の後ろに立つな」ですって | `ja_gender()` ตัดข้อความใน 「」『』 ออกก่อน |
| `POLITE_JA` | คำว่า `いたし` ไปตรงกับ 一緒に**いた**し (いる+し) | เปลี่ยนเป็น `いたしま` |
| `JA_FEMALE_RE` | ไม่รับรูปลากเสียง `のよぉ` · `のよー` | เพิ่มสระเล็ก/เครื่องหมายลากเสียงในชุดท้ายประโยค |

### 11. บทเรียนเรื่องความสม่ำเสมอข้ามก้อนของเนื้อเรื่องเดียวกัน

เนื้อเรื่องย่อย "จดหมายสำเนียงโทสะ" ของฟูจิเอะกินสองก้อน (MSG_012 · MSG_013) แล้ว
**นักแปลสองคนแก้ปัญหาคนละทาง**: ก้อนแรกแปลเป็นภาษาไทยโบราณ · ก้อนหลังคงวลีอังกฤษไว้ 28 บรรทัด
→ lead เคาะให้ใช้แนวไทยโบราณ (ผู้เล่นไทยอ่านอังกฤษไม่ออก มุกถอดรหัสพังทั้งเควสต์)
แล้วล็อกตารางวลีปริศนาลง `glossary.md` §1.9.17

**กติกาใหม่สำหรับ lead**: ก่อนแจกคลื่น ให้ดูว่าเนื้อเรื่องย่อยไหนพาดสองก้อนขึ้นไป
แล้วบอกนักแปลก้อนหลังว่าให้ยึดรูปของก้อนแรก (ดูได้จาก `sources` ที่ซ้ำ uid กัน)

### 12. ต้นทุน token ที่วัดได้จริงของ sprint 10 (จากระบบ ไม่ใช่ที่ agent รายงานเอง)

| บทบาท | ช่วงที่วัดได้ |
|---|---|
| นักแปลชั้น `.msg` หนึ่งก้อน | **137k – 284k** (สูงกว่าชั้นเมนูเพราะต้องอ่าน `ref_ja` ทุกบรรทัด) |
| ผู้ตรวจที่ถือ 3 ก้อน | **120k – 225k** |
| **รวมต่อคลื่น (6 ก้อน + ผู้ตรวจ 2 คน)** | **~1.4M – 1.6M** |

sprint นี้เดิน 5 คลื่น (MSG_001–030 + 081) ≈ **7M**
งานที่เหลือ MSG_031–083 = 53 ก้อน ≈ **9 คลื่น ≈ 13M**

**ทางที่ทำให้ถูกลงจริงในคลื่นหลัง ๆ**: ใส่ตารางกฎเพศ + คำล็อกล่าสุดลง **บรีฟนักแปลโดยตรง**
(แทนที่จะให้ไปเปิดเอกสารเอง) และกรอง `ref_tm` ที่ผิดกติกาออกจากคิวตั้งแต่ต้น —
คลื่น MSG_025–030 เสียรอบแก้ย้อนหลังน้อยกว่าคลื่นแรก ๆ ชัดเจน

---


## 🆕 sprint 9 — สิ่งที่เปลี่ยนไปจากเดิม (อ่านก่อนเริ่มคลื่นถัดไป)

### 1. ตัดสตริงชนิดข้อมูลออกจากคิวแปล — `scripts/mark_dnt.py` (ใหม่)

worklist สร้างด้วยตัวกรองที่ดูแต่ **หน้าตาสตริง** คอลัมน์ที่เก็บ path รูป · id อาวุธ · ธงเนื้อเรื่อง ·
เสียงอ่านคานะ จึงหลุดเข้าคิวแปลไปด้วย (`/WeaponThumbnail/T_UI_Thum_model3` · `TUTORIAL_BTL_掴み_開始`)
**ถ้าแปลของพวกนี้ เกมจะหารูป/ธงไม่เจอ**

`mark_dnt.py` ดู **คอลัมน์ต้นทางจริง** ของทุกครั้งที่สตริงโผล่ (จาก `extracted/parallel/*.json`)
+ กฎ "en เท่ากับ ja ทุกที่ที่โผล่ และหน้าตาเป็น id" → คัดออก **1,412 คีย์ทั้งเกม**
ก้อนที่เป็นข้อมูลล้วน (049 · 055 · 084) เขียนไฟล์ done แบบ copy ตรงได้เลย ไม่ต้องเสียนักแปล

**ขั้นตอนใหม่ของ lead ทุกคลื่น**:
```
python scripts/mark_dnt.py NNN ...            # เขียน batch_NNN.dnt.json
python scripts/mark_dnt.py NNN --fill         # ก้อนที่ DNT 100% -> เขียน done ให้เลย
# แล้วสร้าง batch_NNN.todo.json (คิวที่ตัด DNT ออกแล้ว) ส่งให้นักแปลแทนไฟล์ worklist เดิม
python scripts/assemble_done.py NNN <ไฟล์งานนักแปล>   # ประกอบกลับให้ครบทุกคีย์ตามลำดับ worklist
```

### 2. ปลดล็อกตาราง `tips` — `scripts/armp_graft.py` (ใหม่)

reARMP ไม่รู้จักชนิดคอลัมน์ **30 (บล็อกธง 32 ไบต์) และ 31 (4 ไบต์)** ของ Ishin จึงเขียนกลับเป็นศูนย์
วิธีแก้: เราไม่เคยแก้คอลัมน์พวกนี้เลย → **ก๊อปไบต์จาก vanilla กลับเข้าแถวเดิม** หลังประกอบเสร็จ
(ตรวจก่อนว่า rows/cols/storage และตาราง aux ตรงกันทุกไบต์ ไม่งั้นไม่ปะ)

ผล: `check_armp_rebuild.py` จาก **111/122 เป็น 116/122** · `tips` ผ่านแล้ว → ปลดช่องข้อความ **172 ช่อง**
`build_text.py` กับ `check_armp_rebuild.py` เรียก graft ให้อัตโนมัติแล้ว
ที่ยังตกอีก 6 ตารางเป็นคนละอาการ (ตัวอ่านมองทุกคอลัมน์เป็นชนิด -1) และ **ไม่มีข้อความให้แปล**

### 3. ตัวตรวจที่แก้เพราะตีคำแปลที่ถูกต้องตก (กติกา "ห้ามดัดคำแปลให้ผ่านเครื่องมือ")

| ด่าน/ตัวตรวจ | อาการ | วิธีแก้ |
|---|---|---|
| **G** | นับ "คุณ" เป็นคำบอกเพศ | แยกชุด `MODERN_GENDERED` (ผม/ดิฉัน/ฉัน/ครับ/ค่ะ) — "คุณ" ไม่บอกเพศผู้พูด |
| **G** | บรรทัดที่ `gender: unknown` แต่ `speakers` มีคนเดียวและระบุเพศ | เพิ่ม `is_neutral()` — ผู้พูดคนเดียวที่รู้เพศ = พิสูจน์ได้ ไม่ใช่กลางเพศ |
| **G/M** | ชื่อเต็มสองคำที่ลงท้าย "โคะ/นาคะ" | `is_name_label()` รับชื่อ 2–3 คำ แต่กันวลีที่มีคำสามัญ (Group · Party · Clan) |
| **M** | "คุณหนู" (คำนามเรียกลูกสาวเจ้าบ้าน) | เพิ่ม `หนู` ในรายการยกเว้นของ `RE_KHUN` |
| **H** | `Okami-san` (女将さん) = คำเรียกตำแหน่ง ไม่ใช่ชื่อคน | เพิ่ม `HONORIFIC_TITLE_EN` (okami · sensei · danna · oyaji …) |
| **P** | ชื่อไอเทมที่มีคำว่า "เจ้า" อยู่ในชื่อ ("เบ็ดเจ้าสายน้ำ") | ตัดชื่อในเครื่องหมายคำพูดออกก่อนตรวจสรรพนาม |
| **P** | เอกสารยาว (ตำราประวัติศาสตร์) มีหลายน้ำเสียงในสตริงเดียว | สตริง > 400 ตัวอักษร หรือ ≥ 2 ย่อหน้า = **เตือน ไม่ใช่ตก** |
| **C** | ตารางที่ต้นฉบับเป็นญี่ปุ่นล้วน (นามสกุล · พจนานุกรม) ต้องถอดเป็นไทยทั้งคีย์ | หายทั้งหมด = เตือน · หายบางส่วน = ตก |
| **A2** | ก้อนบทพูดสั้นได้ corr 0.76–0.83 ทั้งที่คีย์ตรงหมด (ตรวจมือแล้ว 6 ก้อน) | เกณฑ์ corr ต่ำใช้ได้ต่อเมื่อ **การเลื่อนไม่ได้แย่กว่ามาก** (`SHIFT_LOSES_BADLY = -0.25`) |

`test_qc_tools.py` ขยายเป็น **71 เคส ผ่านหมด** · `check_alignment.py --self-test` ยังจับเคสเลื่อนจริงได้เหมือนเดิม
`sprint_status.py` เรียนรู้ไฟล์คู่ชนิดใหม่ (`.dnt.json` · `.todo.json`) แล้ว

### 4. คำที่ ship ไปแล้วไม่ตรงกัน — กวาดย้อนหลังทั้งคลัง

| คำ | แก้เป็น | จำนวน |
|---|---|---|
| ทับศัพท์นามสกุล ค/ช/ซู (ก้อน 026 ใช้คนละระบบกับทั้งโปรเจกต์) | ก/จ/สุ ตามผลของ `romaji_to_thai.py` | **67 ชื่อ** |
| `Orb` (魂球) "ออร์บ" / "ลูกแก้ววิญญาณ" | **ลูกแก้ว** | 5 |
| `The Apex` · `Donate` (寄贈) | **บัลลังก์สูงสุด** · **ถวาย** | 3 |
| `Arena Points` · `Hundred-Man Beatdown` (ร่างของ lead ขัดกับของที่ ship) | **แต้มสนามประลอง** · **สังหารร้อยคน** | 10 |
| `Scarecrow/Farm Level N` "ระดับ N" | **"<ระบบ> ขั้นที่ N"** ทั้งตระกูล | 8 |
| kawaraban · Satcho · หมาป่ามิบุ · Soulful Strike · Corporal | คาวาราบัง · ซัตโช · หมาป่าแห่งมิบุ · หมัดวิญญาณ · สิบเอก | 13 |
| ชื่อมือไพ่มาจงที่ทับศัพท์ (ก้อน 078) | **แปลความหมายตาม EN** (ครึ่งสีเดียว · สีเดียวล้วน) | 14 |
| คำเรียกลูกค้าของพ่อค้าแม่ค้า "เจ้า" | **"ท่าน"** ทั้งชั้น (ระดับ T1 งานบริการ) | 29 |
| นามสกุลตระกูล 上- (อุเอ-) | **อูเอ-** (อูเอดะ · อูเอโนะ · อูเอฮาระ) | 10 |

⚠ **บทเรียนที่ย้ำเป็นครั้งที่ห้า**: ร่างคำล็อกของ lead ขัดกับรูปที่ ship ไปแล้วอีกสองคำในคลื่นนี้
(`Arena Points` · `Hundred-Man Beatdown`) และบรีฟเขียนผิดอีกสองคำ (`ซนโนโจอิ` · `มิมาวาริกุมิ`)
นักแปลจับได้เองแล้วยึด master ถูกต้อง → **เช็ก `master_th.json` ก่อนเขียนคำล็อกใหม่ทุกครั้ง**

### 5. คำล็อกใหม่ที่เพิ่มลง glossary

| หมวด | เนื้อหา |
|---|---|
| §1.9.10 | สังเวียนประลอง · ถ้วยรางวัล · อีกชีวิตหนึ่ง · บทพูดชาวเมือง · ตำราประวัติศาสตร์ · พจนานุกรมญี่ปุ่น · ตรา/ตั้งค่า · นามสกุลศัตรู |
| §1.9.11 | ศัพท์ประวัติศาสตร์จากพจนานุกรมในเกม + ชื่อบุคคลจริง (อิเอยาสึ · อิอิ นาโอสึเกะ · เพร์รี …) |
| §1.9.12 | กฎทับศัพท์เพิ่ม (上 = อู- · 鈴 = ซูซู- · 田 ท้ายชื่อ = -ดะ) + **บั๊กของ `romaji_to_thai.py` ที่ต้องแก้มือทุกครั้ง** |
| §1.9.13 | การ์ดไทชิ · ระดับขั้นเกมกระดาน (**級 = คิว · 段 = ชั้น**) · บทเรต M |
| §1.9.14 | 組 = หมู่ (แยกจาก 隊 = หน่วย) · Okami-san = แม่นาง · เด็กเรียกผู้ใหญ่ = "พี่…" |
| §1.9.15–16 | ศัพท์มาจงครบชุด — **ชื่อมือไพ่แปลความหมาย · คำเรียกทับศัพท์** (ตามที่ EN ทำ) |

`PRONOUN_MATRIX.md` §2 แก้รายการของ **โอกาดะ อิโซ**: ไม่ใช่ T1 เสมอไป — ฉากที่เขาแค้นเรียวมะ
ต้นฉบับใช้ ワシ/おまん/じゃき ไม่มี keigo เลย → **T3** · ตัดสินจาก `ref_ja` รายบรรทัดเสมอ

### 6. ต้นทุน token ที่วัดได้จริงในคลื่นนี้ (สูงกว่าที่บันทึกไว้เดิม)

| บทบาท | ช่วงที่วัดได้ |
|---|---|
| นักแปลหนึ่งก้อน | **125k – 265k** (ก้อนตำราประวัติศาสตร์ 050 = 261k · 70 นาที) |
| ผู้ตรวจที่ถือ 3–6 ก้อนพร้อมกัน | **198k – 269k** |

**ผู้ตรวจที่ถือหลายก้อนพร้อมกันคุ้มกว่าคู่ 1:1 ชัดเจน** — จับได้สองชนิดที่นักแปลตรวจเองไม่ได้:
คำล็อกที่ใช้ถูก 26 ครั้งแล้วหลุดครั้งเดียว · ก้อนพี่น้องตั้งคำไม่ตรงกัน (組 = "ทีม" vs "หมู่")

---

## ✅ ฐานความรู้จากการสำรวจไฟล์เกม (sprint 4 — ยังใช้อ้างอิงอยู่)

### 1. คลังคู่ขนาน อังกฤษ↔ญี่ปุ่น ทั้งเกม (ของที่มีค่าที่สุดของรอบนี้)

pak เดียวกันมีข้อความครบ 9 ภาษา — ดึงญี่ปุ่นออกมาจับคู่กับอังกฤษได้ **ทุกบรรทัด**

| ชั้น | วิธีจับคู่ | ผล |
|---|---|---|
| `.msg` | ไฟล์เดียวกัน · ดัชนีแถวเดียวกัน | 54,318 แถว · ตรงกันครบทั้ง 1,678 ไฟล์ |
| ARMP | ตาราง + sub-table + `reARMP_rowIndex` + คอลัมน์ | 33,621 ช่อง · 100% |
| locres | namespace + key | 23,507 แถว · 100% |

อังกฤษของ RGG ตัดสิ่งที่ภาษาไทยต้องใช้ทิ้งหมด — เพศผู้พูด · ระดับความสุภาพ · คำนำหน้าชื่อ ·
สำเนียงคันไซ **ตอนนี้ญี่ปุ่นแนบไปกับทุกบรรทัดใน batch แล้ว (ช่อง `ref_ja` ครบ 41,601 สตริง)**

### 2. แกะ opcode ที่ชี้ผู้พูดได้จริง — `0x03` ชนิดย่อย `0x35`

คำสั่ง `0x03` ไม่ได้แปลว่า "คิวเสียง" อย่างที่เข้าใจกันมาก่อน มันอ้างถึง label ทุกชนิด รวม
**ตัวเลือกในเมนูสนทนา** ที่ค้างกับทุกแถวในบล็อกเดียวกัน ตัวที่แปลว่า "เล่นเสียงบรรทัดนี้" คือ
ชนิดย่อย `0x35` ซึ่งมี **อย่างมากหนึ่งตัวต่อบรรทัดเสมอ** (4,113 บรรทัด)

**เจอเพราะผลผิด**: ตัวสร้างทะเบียนเพศรอบแรกตัดสินฮารุกะกับโอเรียวเป็น "ชาย" ทั้งที่เป็นหญิงทั้งคู่
— เพราะบรรทัดรำพึงของเรียวมะพก label ของฮารุกะติดมาด้วย
รายละเอียด + ตัวเลขที่วัดได้: `docs/research.md` §9

### 3. ทะเบียนตัวละคร — เจอแหล่งที่ดีที่สุดในเกม

`Game.locres` namespace `correlation_person_*` คือ **แผนผังความสัมพันธ์ในเกม** ให้ครบชุด:
ชื่อเต็ม (`Sakamoto Ryoma`) · ชื่อสั้นที่ใช้เป็นป้ายผู้พูด · สังกัด (`Tosa Domain`) ·
**ประวัติย่อที่ใช้ he/she ตรง ๆ** · และฝั่ง ja ให้ **คันจิชื่อจริง** (坂本龍馬)

→ ตัวละครหลัก **35 คน** · เพศ **ชาย 30 · หญิง 3 · พิสูจน์ไม่ได้ 2**
→ ผู้พูดที่ไม่อยู่ในแผนผังอีก 141 ป้าย (NPC/ป้ายกลุ่มคน)

### 4. เพศผู้พูด — ภาคนี้ไม่มีตารางเพศ

ตรวจครบทั้ง 244 ตาราง ARMP แล้ว **ไม่มีคอลัมน์ `sex`/`gender` เลย** (ต่างจาก Dragon Engine
ที่มี `sound_voicer.bin`) · `TextBridge/AuthSpeaker/` เป็นแค่ข้อความชื่อผู้พูด ไม่มีเมทาดาทา

ที่ใช้แทนคือหลักฐานอื่นในไฟล์เกม เรียงตามความน่าเชื่อ: ประวัติในแผนผัง (he/she) >
เครื่องหมายในต้นฉบับญี่ปุ่น > ป้ายผู้พูดที่บอกเพศในตัวเอง > สรรพนามคันไซ (ชั้นรอง)

ผล: **ชาย 44 · หญิง 19 · พิสูจน์ไม่ได้ 106** จาก 169 ป้ายผู้พูด
ที่พิสูจน์ไม่ได้ส่วนใหญ่เป็น **ป้ายกลุ่มคน** (`Employee` 946 บรรทัด) ซึ่งบังคับกลางเพศเสมอ
เพราะคนหลายสิบคนหลายเพศใช้ป้ายเดียวกัน

### 5. worklist + ไฟล์บริบท พร้อมแจก

**168 batch · 41,601 สตริงไม่ซ้ำ** — ทุก batch มีไฟล์คู่ `.context.json` บอกผู้พูด/เพศ/บท

| priority | เนื้อหา | สตริง | รู้เพศผู้พูด |
|---:|---|---:|---:|
| 1 | บทคัตซีนเนื้อเรื่อง | 2,680 | **82%** |
| 2 | เรื่องย่อ/คำอธิบายเนื้อเรื่องและตัวละคร | 1,448 | 0% |
| 3 | เมนู/ระบบ/ไอเทม/ทักษะ/ร้านค้า | 9,366 | 0% |
| 4 | บทพูด NPC เดินถนน | 3,711 | 36% |
| 5 | มินิเกม/การ์ดไทชิ/โหมดถ่ายรูป | 3,696 | 0% |
| 9 | บทสนทนา `.msg` (ซีรีส์ `batch_MSG_*`) | 20,700 | 13% |

ที่ priority 2/3/5 เป็น 0% ไม่ใช่ปัญหา — เป็นเมนูและคำอธิบาย ไม่มีผู้พูดอยู่แล้ว

### 6. เอกสารทีม

| ไฟล์ | สถานะ |
|---|---|
| `docs/reference/TRANSLATOR_BRIEF_ishin.md` | ใหม่ · เขียนเฉพาะภาคนี้ |
| `docs/reference/REVIEWER_BRIEF_ishin.md` | ใหม่ · 7 ด่าน + กับดักเฉพาะภาคนี้ |
| `translations/PRONOUN_MATRIX.md` | เขียนใหม่เป็นยุคบาคุมัตสึ (ข้า/ท่าน · ข้า/เจ้า · กู/มึง) |
| `translations/glossary.md` | §1 มีคำล็อก 37 คำแล้ว (ฝ่าย · ยศ · สถานที่ · สี่ท่าต่อสู้) |
| `translations/name_locks.json` | ใหม่ · คำล็อกชื่อ 35 ชื่อ + ชื่อแฝง — แก้ที่นี่ที่เดียว |
| `translations/glossary_seed.md` | ใหม่ · 3,918 คำจากไฟล์เกม + JA + คำจากภาคก่อน |
| `translations/name_proposals.md` | ใหม่ · 35 ชื่อ ขึ้น LOCKED ครบ (สร้างจาก `name_locks.json`) |
| `docs/characters.md` | ใหม่ · ตาราง + ประวัติย่อทุกคนจากในเกม |
| `docs/reference/gender_evidence_ishin.md` | ใหม่ · หลักฐานเพศรายคน |

### 7. ตัวตรวจคำล็อกแข็งขึ้น (`check_glossary_locks.py`)

แก้สองจุดที่ทำให้ตัวตรวจเดิมใช้กับภาคนี้ไม่ได้:
- **เดิมอ่าน `glossary.md` ทั้งไฟล์** → หยิบตาราง "คำที่ยังรอเคาะ" มาบังคับใช้ด้วย (22 คำผิด ๆ)
  ตอนนี้อ่านเฉพาะหัวข้อระดับ `## ` ที่มีคำว่า LOCKED
- **เพิ่มให้อ่าน `translations/name_locks.json`** — ชื่อ 35 ชื่ออยู่ในไฟล์ JSON ไม่ใช่ตารางใน
  glossary ถ้าไม่อ่านจะบังคับได้แค่ 6 ชื่อที่ยกมาโชว์ใน §1.5

ผล: **บังคับใช้ได้ 97 คำ** · ทดสอบแล้วจับ "ท่านโคนโดะ" ที่ควรเป็น "คนโด" ได้

---

## ✅ คำตัดสินของ lead (2 ก.ย. 2026) — ปิดครบ พร้อมแจก batch

1. **คำลงท้ายยุคเก่า = "ขอรับ / เจ้าค่ะ" ทั้งเกม** (ไม่ใช้ ครับ/ค่ะ ในบทพูดของตัวละครในยุค
   ไม่แยกตามชั้นยศ) พร้อม **กฎคุมความหนาแน่น** เพื่อไม่ให้อ่านเกร็ง — ใส่เฉพาะบรรทัดที่
   ต้นฉบับญี่ปุ่นใช้รูป ですます จริง · บทยาวใส่ที่ประโยคแรกกับประโยคปิดพอ
   → `PRONOUN_MATRIX.md` §4 ข้อ 1
2. **`Domain` = "แคว้น"** (ไม่ทับศัพท์ "ฮัง") · **`Bakufu` = "รัฐบาลโชกุน"** (ไม่ทับศัพท์)
   → `glossary.md` §1.1
3. **ชื่อตัวละคร 35 ชื่อ + ชื่อแฝง ล็อกครบ** → `translations/name_locks.json`
   ที่แก้จากร่างอัตโนมัติ: คนโด (ไม่ใช่ โคนโดะ) · ทาเคจิ ฮันเปตะ · นางากุระ ชินปาจิ ·
   อิโนอุเอะ เก็นซาบุโร · ไซโง · โทกูงาวะ · สระเสียงยาวไม่เติม ะ ท้าย (โทชิโซ · โทโด · อิโต)
   และ **ไซโต ฮาจิเมะ** = ชื่อแฝงของเรียวมะในชินเซ็นกุมิ (โผล่ 1,055 ครั้ง)
4. **สี่ท่าต่อสู้** — ญี่ปุ่นตั้งเป็น 「〜の型」 คำไทยจึงใช้ "ท่า…" ให้เข้ากับยุค:

   | EN | JA | ไทย |
   |---|---|---|
   | Swordsman | 一刀 | **ท่าดาบเดี่ยว** |
   | Gunman | 短銃 | **ท่าปืนสั้น** |
   | Wild Dancer | 乱舞 | **ท่าระบำอลวน** |
   | Brawler | 格闘 | **ท่ามือเปล่า** |

   `Swordsman Rank 7` = **ดาบเดี่ยว ขั้น 7** → `glossary.md` §1.4

---

## 🔄 สถานะคลื่นแปล (2 ก.ย. 2026)

| batch | สถานะ |
|---|---|
| 001–011 | ✅ priority 1 (บทคัตซีนเนื้อเรื่อง) · ตรวจครบ · merge แล้ว |
| 012–017 | ✅ priority 2 (เรื่องย่อ/คำอธิบาย/แผนผังตัวละคร) · ตรวจครบ · merge แล้ว — **รวม master 4,128 คู่** |
| 018–029 | ✅ priority 3 ชุดแรก (ทักษะ · ไอเทม · เมนู · ร้านค้า · ชื่อคน) · **12 ทีมขนาน** · ตรวจครบ · merge แล้ว |
| 030–041 | ✅ priority 3 ต่อ (มินิเกม · พนัน · ตั้งค่า · ป้ายสถานที่) · 12 ทีมขนาน + ผู้ตรวจ 6 คู่ · ตรวจครบ · merge แล้ว |
| 042–055 | ✅ priority 3 ปิดท้าย (สังเวียน · ถ้วยรางวัล · ไร่นา · ชาวเมือง · ตำราประวัติศาสตร์ · พจนานุกรม · นามสกุลศัตรู) · sprint 9 |
| 056–067 | ✅ priority 4 (บทพูด NPC · บทสนทนา `.msg` · ร้านค้า · เสียงตกใจ) · sprint 9 |
| 068–085 | ✅ priority 4–5 (ย่านสำราญ · การ์ดไทชิ 851 ใบ · มินิเกมกระดาน · ฝึกทหาร) · sprint 9 — **เหลือ 081 กำลังแปลรอบสอง** |
| `MSG_001–083` | ⏳ **งานที่เหลือทั้งหมด — 20,700 สตริง · บทสนทนา `.msg` (priority 9)** · แจกไปแล้ว MSG_001–004 |

**รูปแบบทีมที่ใช้อยู่**: หนึ่ง batch = นักแปล 1 + ผู้ตรวจ 1 (sonnet ทั้งคู่) · เดิน 6 ทีมขนานกัน
· lead (เซสชันหลัก) เป็นคนเคาะคำล็อก กวาดคำก่อนส่งตรวจ merge และเขียนไฟล์รีวิว

### ขั้นตอนที่ lead ต้องทำทุกครั้งที่รับงานจากนักแปล

1. ตรวจโครงไฟล์: คีย์ครบ + **ลำดับคีย์ตรง worklist** (ไม่ตรง = ด่าน A1 ตกทั้ง batch)
2. `python scripts/merge_qc.py --dry-run --only NNN` · `check_glossary_locks.py` · `check_translit_drift.py`
3. **กวาดคำที่ทีมตั้งเองไม่ตรงกัน** แล้วล็อกลง `glossary.md` §1 ทันที — ถูกกว่าปล่อยให้ผู้ตรวจไล่
4. ส่งผู้ตรวจพร้อม **รายการคำล็อกใหม่ของวันนั้น** (ผู้ตรวจต้องบังคับใช้ย้อนหลังในไฟล์ที่ตัวเองถือ)
5. รับงานจากผู้ตรวจ → รันด่านซ้ำ → `merge_qc.py --only NNN` → เขียน `translations/review/batch_NNN.review.md`

### คำล็อกที่เคาะเพิ่มระหว่างสองคลื่นนี้ (อยู่ใน `glossary.md` §1 ครบแล้ว)

**ยศ/ตำแหน่ง**: หัวหน้าหน่วยหนึ่ง/สอง/สาม (隊長 — **ใช้ "หน่วย" ไม่ใช่ "กอง"**) · หัวหน้าหน่วย (隊長 ไม่ระบุเลข)
· ฝ่ายตรวจการ (監察) · เลขาธิการใหญ่ (総長) · หัวหน้าพรรค (筆頭) · หัวหน้า (局長 เดี่ยว) · ฝ่าบาท (上様)
· โจชิ · โกชิชั้นสูง · แม่นาง/ท่านผู้หญิง (Ma'am)

**สถานที่/องค์กร**: โอมิยะ · สำนักเค็นโงชิ · อิเคดายะ · อาบุระโคจิ · กิอง · สึบากิโร · ทามะ · ชิเออิกัน
· เท็นเน็นริชิน · สำนักมิโตะเท็งงุ · หมาป่าแห่งมิบุ (壬生狼) · โคมุโซ (虚無僧)

**เนื้อเรื่อง/ประวัติศาสตร์**: การปฏิรูปเมจิ · การคืนอำนาจครั้งใหญ่ (大政奉還) · องค์จักรพรรดิ/จักรพรรดิ
· ราชสำนัก · พระราชวัง (御所) · อีกา (鴉) · อิโซนักฆ่า (人斬り以蔵) · เบี้ย (歩) · ท่านลุง (Pops · 親っさん)

**คำเรียกและคำต่อท้ายชื่อ**
- `-san` = ซัง · `-kun` = คุง · `-chan` = จัง (เขียนติดชื่อ) · `-dono`/`-sama` = "ท่าน" นำหน้าชื่อ
  · `-han` (รูปคันไซของ -san) = **ซัง** เหมือนกัน · `-sensei` = **"อาจารย์"** นำหน้าชื่อ (แปล ไม่ทับศัพท์)
- **ห้ามซ้อนสองชั้น** — บรรทัดที่ใช้ "ท่าน" นำหน้าชื่อแล้ว ไม่ต้องเติม "ซัง" อีก
- ちゃん ที่ผู้ใหญ่ใช้**ล้อ**ผู้ใหญ่ (โอกิตะ → เรียวมะ) = **"ไอ้หนู" นำหน้าชื่อ** ไม่ใช่ "จัง"
- `Brother` (兄弟 พี่น้องร่วมสาบาน) = **"พี่"** เมื่อเรียวมะเป็นผู้พูด · 兄者/兄さん (พี่สายเลือด) = "ท่านพี่"
- **ข้อยกเว้นรายตัวละคร**: ฮิจิกาตะเรียกเรียวมะ/ไซโต ว่า **"ท่าน"** เสมอ (ต้นฉบับใช้ 敬語 ต่อเนื่อง)
- **เรียวมะใช้ "ข้า" ทุกระดับ** สงวน "กระผม" ไว้เฉพาะฉากเข้าเฝ้าเจ้าแคว้น

`translations/name_locks.json` = **41 ชื่อ** (เพิ่ม เซริซาวะ คาโมะ · ฮิรายามะ โกโร · ฮิรามะ จูสุเกะ
· alias `Saigo Yoshinosuke` ที่อังกฤษสะกดต่างจาก `Saigo Kichinosuke` แต่เป็นคนเดียวกัน)

### ด่านตรวจที่เพิ่มจากความผิดพลาดจริงของสองคลื่นนี้

| ด่าน/เครื่องมือ | เกิดจากอะไร |
|---|---|
| **ด่าน H** ใน `merge_qc.py` | คำต่อท้ายชื่อหายไป **19 จุดใน 3 batch** ผู้ตรวจสามคนจับได้รวมกันจุดเดียว |
| `check_translit_drift.py` | "ชิเออิกัน" กับ "ชิเออิกัง" อยู่ไฟล์เดียวกัน · คำที่ยังไม่ได้ล็อกจะเพี้ยนเงียบ ๆ |
| คำล็อกวลีชนะคำเดี่ยว | `Tosa Loyalist Party` ถูกบังคับให้มีคำว่า "ชิชิ" — เตือนเท็จ 5 จุด |
| `check_pronoun_pairs.py` อ่านเฉพาะ `strings` | เดิมสแกนทั้งไฟล์ เลยจับข้อความในช่อง `notes` ของผู้ตรวจว่าผิด |
| `RE_KU` กัน "โทกูงาวะ" | นักแปล batch_007 **เขียนประโยคเลี่ยงชื่อโทกูงาวะทิ้งไปสองบรรทัด** เพราะด่าน P ตีกลับ |
| `check_particle_density.py` | เดิมวัดอะไรไม่ได้เลยกับภาคนี้ (regex ผิดยุค · อ่านช่องผู้พูดผิด · ไม่รู้จักคีย์ `lines`) |
| `RE_THAN` กัน "ท่านลุง" | คำล็อก Pops ถูกนับเป็นคำเรียกคู่สนทนา → นักแปล batch_010 **ตัดประธานทิ้ง**เพื่อหลบ |
| `RE_KHUN` กัน "คุณูปการ" | คำไทยปกติถูกจับเป็นสรรพนาม → นักแปล batch_011 **เปลี่ยนคำ**เพื่อหลบ |
| `MODERN_SPEECH` เพิ่ม ฉัน/ดิฉัน | ผู้ตรวจ batch_008 เจอ "ฉัน" หลุดในบรรทัดกลางเพศ ด่าน G มองไม่เห็น |

**กติกาที่ย้ำจากรอบนี้: ห้ามดัดคำแปลให้ผ่านเครื่องมือ** — ถ้าเครื่องมือผิด ให้แก้เครื่องมือแล้วคืนคำแปลเดิม
เกิดขึ้นจริง **สามครั้งในคลื่นเดียว** (โทกูงาวะ · ท่านลุง · คุณูปการ) ทุกครั้งนักแปลเลือกเลี่ยงคำแทนที่จะรายงาน
→ ใส่คำเตือนข้อนี้ลงบรีฟของทั้งนักแปลและผู้ตรวจแล้ว

### คำตัดสินทะเบียนตัวละครที่เคาะจากคลื่นนี้

- **โอกาดะ อิโซ พูดกับเรียวมะ/ไซโต ด้วย T1 (ข้า ↔ ท่าน) เสมอ** + "ขอรับ" เฉพาะบรรทัดที่ `ref_ja`
  มี ですます — ต้นฉบับให้เขาใช้ 私/あなた/です ทั้งที่เป็นมือสังหาร ความนอบน้อมนี้เป็นบุคลิกตัวละคร
  (ผู้ตรวจ batch_008 จับได้ว่า batch_004 ใช้ "เจ้า" ไม่ตรงกัน → แก้ย้อนหลัง 5 บรรทัดแล้ว)
- **โทกูงาวะ โยชิโนบุ** ใช้ "ข้า" + เรียกคู่สนทนา "เจ้า" · คนอื่นเรียกเขา **"ฝ่าบาท"**
- **ยามาอุจิ โยโด** (เจ้าแคว้น) — คนอื่นเรียก "ท่าน" เสมอแม้ในบทประชด
- **โอโตเซะ** ใช้ "ท่าน" กับเรียวมะ ขณะที่เรียวมะใช้ "เจ้า" กับเธอ — ไม่สมมาตรโดยตั้งใจ

### ต้นทุน token ที่วัดได้จริง (จากระบบ ไม่ใช่ที่ agent รายงานเอง)

| บทบาท | ช่วงที่วัดได้ |
|---|---|
| นักแปลหนึ่ง batch (250 สตริง) | 144k – 204k |
| ผู้ตรวจหนึ่ง batch | 93k – 120k |
| รวมต่อ batch | **~250k – 320k** |
| ทั้งเกม 168 batch (ประมาณการ) | **~45–53M** |

⚠ **agent ประเมิน token ตัวเองต่ำกว่าจริง 30–100% ทุกตัว** — ตัวหนึ่งรายงาน "55–65k" ขณะที่ระบบวัดได้ 195k
ให้ดูตัวเลขจากระบบเท่านั้น

## ✅ นำร่องสายการแปลจริงสำเร็จ — batch_001 ขึ้น master แล้ว (2 ก.ย. 2026)

รอบนี้เดินครบวง: **แจก → แปล → ตรวจ → merge → บิลด์ → ตรวจไบต์** ด้วย batch จริงหนึ่งก้อน

| ขั้น | ผล |
|---|---|
| นักแปล (sonnet) | 250/250 คีย์ · ลำดับคีย์ตรง worklist · ด่านอัตโนมัติตก 0 |
| ผู้ตรวจ (sonnet) | แก้ 4 บรรทัด (คำล็อก `Party Rep` → "หัวหน้าพรรค") · ไม่พบข้อผิดพลาดด้านความหมาย/ระดับภาษา |
| lead merge | `master_th.json` = **250 คู่** · `check_alignment` corr 0.943 |
| บิลด์ | msg 126 ไฟล์ (218 บรรทัด) · ARMP 2 ตาราง (37 ช่อง) · locres 433 คีย์ (สตริงไทยใหม่ 249) |
| ตรวจ pak ที่ได้ | เปิดกลับด้วย `pakfile.py` แล้วเจอไทยครบทั้งสามชั้น · `check_layout_all` ต่าง 0 · `check_pak_roundtrip` ต่าง 0 |

ไฟล์ม็อด: `build/LikeADragonIshinThai_P.pak` (6.4 MB · 130 ไฟล์ · รวมฟอนต์ไทย)
ติดตั้งด้วย `python scripts/build_text.py --install` แล้วให้ **ผู้ใช้** ทดสอบในเกม (กติกาเหล็กข้อ 2)

### ต้นทุน token ที่วัดได้จริง (สำคัญต่อการวางแผน)

| บทบาท | ที่วัดได้ | ที่ agent เดาเอง |
|---|---:|---:|
| นักแปล batch_001 | **194,740** | 55–65k (เดาต่ำไปสามเท่า) |
| ผู้ตรวจ batch_001 | **120,349** | 55–60k |
| รวมต่อ batch | **~315,000** | — |

→ ทั้งเกม 168 batch ≈ **53M token** ถ้าใช้คู่แปล+ตรวจทุกก้อน
→ ทางลด: `docs/reference/BATCH_CHEATSHEET_ishin.md` (ใหม่) รวมทุกกฎไว้ไฟล์เดียว
   นักแปลไม่ต้องเปิดบรีฟ+matrix+glossary+name_locks ทีละไฟล์อีก · คลื่นถัดไปใช้ใบนี้แล้ววัดผลเทียบ

### เครื่องมือที่แก้เพิ่มจากผลตรวจรอบนี้

- `check_glossary_locks.py` — **คำล็อกที่เป็นวลีชนะคำเดี่ยวที่อยู่ข้างใน** (`Tosa Loyalist Party`
  ไม่ถูกบังคับให้มีคำว่า "ชิชิ" อีก) · เตือนเท็จลดจาก 5 เหลือ 1
- `check_particle_density.py` — เดิม **วัดอะไรไม่ได้เลยกับภาคนี้** สามชั้นซ้อน:
  regex เป็น ครับ/ค่ะ ของภาคปัจจุบัน · อ่านช่อง `speaker` เดี่ยวทั้งที่ภาคนี้เก็บเป็นรายการ `speakers`
  (แต่ละตัวเป็น dict) · และไม่รู้ว่าไฟล์บริบทห่อข้อมูลไว้ใต้คีย์ `lines`
  ตอนนี้วัดได้จริงและ **บอกเหตุผลเมื่อไม่มีอะไรให้วัด** แทนที่จะเงียบ
- glossary §1 เพิ่มคำล็อก: หัวหน้าพรรค · ท่านลุง (Pops) · โจชิ · โกชิชั้นสูง · โอมิยะ ·
  สำนักเค็นโงชิ · **เจ้าคะ** (รูปคำถามของ เจ้าค่ะ)
- PRONOUN_MATRIX §2 ล็อกแล้ว: **เรียวมะใช้ "ข้า" ทุกระดับ** สงวน "กระผม" ไว้เฉพาะฉากเข้าเฝ้าเจ้าแคว้น

## ✅ พอร์ตท่อ QC เข้าภาคนี้ (2 ก.ย. 2026 · sprint 5)

`merge_qc.py` ที่ copy มาจาก Lost Judgment **รันไม่ขึ้นเลย** (`ModuleNotFoundError: check_alignment`)
และมีด่านที่ผูกกับฟอนต์ Dragon Engine ซึ่งภาคนี้ไม่มี — รอบนี้พอร์ตให้ใช้กับ Ishin! ได้จริง

| ไฟล์ | สิ่งที่ทำ |
|---|---|
| `scripts/check_alignment.py` | ใหม่ (ยกจาก LJ) · self-test สร้างข้อมูลสังเคราะห์จาก `worklist/batch_001.json` ได้เองเมื่อยังไม่มีไฟล์ done |
| `scripts/thai_pronouns.py` | เพิ่มชุดยุคบาคุมัตสึ: `RE_KHA_SELF` (ข้า) · `RE_THAN` (ท่าน) · `RE_CHAO` (เจ้า) · `RE_KRAPHOM` · `POLITE_OLD` (ขอรับ/เจ้าค่ะ) · `MODERN_SPEECH` · `MODERN_LOANWORDS` |
| `scripts/check_pronoun_pairs.py` | ตารางระดับเป็นบาคุมัตสึ **T1 ข้า/กระผม ↔ ท่าน · T2 ข้า ↔ เจ้า/นาย · T3 กู ↔ มึง** + M1/M2 ของภาคปัจจุบันไว้ใช้กับเมนู |
| `scripts/merge_qc.py` | ตัดด่าน X/S (slotmap/donor) · เพิ่มด่าน **M · G · J · D** |
| `translations/pronoun_exceptions.json` | ใหม่ (เริ่มจากว่าง) |
| `scripts/make_batch_context_ishin.py` | แก้ readme ในไฟล์บริบทให้เป็นคำลงท้ายยุคเก่า แล้ว regenerate ครบ 168 ไฟล์ |

ด่านใหม่ที่เพิ่มเข้ามา:
- **M** — บทพูดในยุค (priority 1/4/9) ห้ามใช้ ผม/คุณ/ครับ/ค่ะ · คำยืมสมัยใหม่ = เตือน
- **G** — บรรทัดที่บริบทระบุ `neutral: true` ห้ามมีคำลงท้าย/สรรพนามบอกเพศ
- **J** — "ขอรับ/เจ้าค่ะ" ต้องมีหลักฐานในต้นฉบับญี่ปุ่น: ญี่ปุ่นห้วนชัด (だぜ・やがる) = **ตก** · แค่ไม่พบ ですます = เตือน
- **D** — ตัวเลขอารบิกที่หายไปจาก TH = เตือน

กับดักที่เจอตอนพอร์ต (บันทึกไว้กันพลาดซ้ำ):
- `"ค่ะ" in text` ใช้ไม่ได้กับภาคนี้ — **"เจ้าค่ะ" มี "ค่ะ" อยู่ข้างใน** จะกลายเป็นจับคำลงท้ายที่ถูกต้องว่าผิด
  ตัวตรวจคำลงท้ายจึงต้องเป็น regex ทั้งหมด (`RE_KHA_MODERN` มี lookbehind กัน "เจ้า")
- `RE_PHOM` ต้องเพิ่ม `(?<!กระ)` ไม่งั้น **"กระผม"** (คำแทนตัว T1 ของภาคนี้) ถูกนับเป็น "ผม" ซ้อน
- `ท่าน` ต้องกัน ท่านั่ง/ท่านอน/ท่าน้ำ และ **"ท่าน + ชื่อที่ล็อกไว้" = คำนำหน้าบุคคลที่สาม ไม่ใช่คำเรียกคู่สนทนา**
  (ไม่งั้น "ท่านฮิจิกาตะสั่งมา เจ้าไปเถอะ" ตกเท็จ)
- `ข้า` ต้องกัน เข้า/ข้าง/ข้าว/ข้าม/ข้าศึก/ข้าราชการ · `เจ้า` ต้องกัน เจ้าของ/เจ้านาย/เจ้าเมือง/เจ้าหน้าที่
- `ขอรับ` ต้องกันรูปกริยา (ขอรับใช้ · ขอรับผิดชอบ · ขอรับรอง) ด้วย lookahead ห้ามตามด้วยพยัญชนะ/สระหน้า

พิสูจน์แล้วด้วยไฟล์ `done` สังเคราะห์ของ batch_001 (250 คีย์): ด่าน N · G · M · P · J · D ยิงตรงเคสที่ตั้งใจ
ทุกตัว · `check_alignment.py --self-test` จับเคสคีย์หล่นได้ทั้งแบบเลื่อนทั้งไฟล์และเลื่อนเฉพาะท้ายไฟล์

## ✅ ท่อบิลด์ครบสามชั้น + ด่านตรวจ ARMP (2 ก.ย. 2026 · sprint 5 ต่อ)

`scripts/build_text.py` ใหม่ — อ่าน `master_th.json` แล้วประกอบข้อความกลับทั้งสามชั้น
แล้วแพ็กเป็น pak ม็อดไฟล์เดียว (ใส่ฟอนต์ไทยไปด้วย เว้นแต่สั่ง `--no-font`)

| ชั้น | ตัวประกอบกลับ | ปลายทางในเกม | สถานะ |
|---|---|---|---|
| `.msg` | `tools/msg.py` `rebuild()` | `…/wdr_en/msg/uid00xxxxxx/<uid>.msg` | ทดสอบแล้ว · roundtrip 1,678/1,678 ไบต์เป๊ะ |
| ARMP | `tools/reARMP_fixed.py` | `…/db.macan/en/<table>.bin` | ทดสอบแล้ว · ผ่านด่าน layout |
| locres | `tools/locres.py` `build_full()` | `…/Localization/Game/en/Game.locres` | ทดสอบแล้ว · เพิ่มสตริงท้ายตารางแล้วชี้ idx ใหม่ |

**locres ต้องเพิ่มสตริงท้ายตารางเสมอ ห้ามเขียนทับที่เดิม** — หลายคีย์ชี้สตริงเดียวกัน
แก้ที่เดียวจะไปโผล่ที่อื่น (วิธีเดียวกับ PoC ที่พิสูจน์บนจอจริงแล้ว) · สคริปต์รวมสตริงไทยที่ซ้ำกัน
ให้ใช้ช่องเดียว ตารางจึงโตเท่าที่จำเป็น

### ด่านใหม่ของชั้น ARMP — และตารางที่ห้ามแตะ

- `scripts/armp_layout_check.py` (ยกจาก LJ) + `scripts/check_layout_all.py` (พอร์ตแล้ว)
  → เทียบ **ไบต์ในแถว** ของ bin ที่บิลด์กับ vanilla
- `scripts/check_armp_rebuild.py` (ใหม่) → ประกอบทุกตารางกลับ **โดยไม่แก้อะไรเลย** แล้วเทียบไบต์ในแถว
  เป็นการพิสูจน์ตัวเขียนล่วงหน้าก่อนมีคำแปลมากพอจะไปแตะตารางนั้นจริง

**ผลรอบแรก: 122 ตาราง · ผ่าน 111 · ตก 11**
สาเหตุที่วัดได้จริง: reARMP **เขียนคอลัมน์ชนิด 30/31 กลับเป็นศูนย์ทั้งก้อน**
(ตรวจ `tips` แถว 1 ออฟเซ็ต 16: vanilla `01000080ffffffff…` · ที่ประกอบกลับได้ `0000…`)

| ตารางที่ตก | มีข้อความให้แปลไหม |
|---|---|
| `tips` | **มี 172 ช่อง · โผล่ใน 13 batch** ← ก้อนเดียวที่กระทบงานแปล |
| อีก 10 ตาราง (`battle_cmdset` · `font2_tag` · `taishi_mission` · `tips_layout` ฯลฯ) | ไม่มีช่องข้อความเลย |

`check_armp_rebuild.py` เขียนรายชื่อลง `build/armp_deny.json` และ **`build_text.py` อ่านไฟล์นั้น
แล้วข้ามให้อัตโนมัติ** พร้อมรายงานว่ามีคำแปลกี่ช่องที่ยังใส่ไม่ได้ — ไม่มีรายชื่อฝังในโค้ดสองที่

⏳ ค้างไว้: หาทางใส่ข้อความของ `tips` (แพตช์ไบต์เฉพาะดัชนีสตริง หรือแก้ตัวเขียนชนิด 30/31)
ยังไม่ด่วน เพราะ 13 batch นั้นยังไม่ถึงคิวแปล

## เครื่องมือที่มีแล้ว (ผ่านด่านตรวจทุกตัว)

### อ่าน/เขียนไฟล์เกม (`tools/`)

| ไฟล์ | หน้าที่ | สถานะ |
|---|---|---|
| `iostore.py` | อ่าน/แตก `.utoc`+`.ucas` | ใช้ได้ · 279,340 ไฟล์ |
| `pakfile.py` | อ่าน/แตก `.pak` v11 | ใช้ได้ · pakchunk0 35,646 ไฟล์ |
| `pakwrite.py` | เขียน `.pak` v11 | roundtrip 6/6 · **ยังไม่ทดสอบในเกม** |
| `msg.py` | อ่าน/ประกอบ `.msg` + หา label ผู้พูด (`0x35`) | roundtrip 1,678/1,678 ไบต์เป๊ะ |
| `reARMP_fixed.py` | ARMP (ยกจาก Lost Judgment) | ใช้ได้ · **ยกเว้น 11 ตารางที่เขียน layout พัง** |
| `locres.py` | `.locres` | roundtrip byte-identical |
| `repak/repak.exe` | ตัวแพ็กของชุมชน (อ้างอิง/สำรอง) | v0.2.3 · ตรวจ sha256 แล้ว |

### สคริปต์สาย data (รันตามลำดับนี้ถ้าต้องสร้างใหม่)

```bash
python scripts/extract_msg.py --lang en          # pak -> .msg -> JSON   (+ --lang ja)
python scripts/extract_db.py --lang en           # ARMP -> JSON          (+ --lang ja)
python scripts/extract_locres.py --lang en       # locres -> JSON        (+ --lang ja)
python scripts/build_parallel.py                 # คลังคู่ขนาน EN<->JA ทั้งสามชั้น
python scripts/build_speaker_gender.py           # ทะเบียนผู้พูด + หลักฐานเพศ
python scripts/build_characters.py               # ทะเบียนตัวละคร + ใบเสนอชื่อไทย
python scripts/make_glossary_seed.py             # คำเฉพาะ 3,918 รายการ
python scripts/make_worklist_ishin.py            # จัด batch (harvest ก่อน re-chunk)
python scripts/make_batch_context_ishin.py       # ไฟล์บริบทคู่ทุก batch
python scripts/make_prior_hints.py NNN ...       # คำที่ก้อนก่อนเคาะแล้ว (รันก่อนแจกทุกคลื่น)
python scripts/make_skill_registry.py            # glossary §1.9.7 จาก master (รันหลัง merge)
```

⚠ `make_worklist.py` และ `make_batch_context.py` (ไม่มี `_ishin`) เป็นของ Lost Judgment **อย่ารัน**
⚠ **ห้าม regenerate `*.context.json` ระหว่างทีมถือ batch อยู่**

### สาย QC (พอร์ตครบแล้วใน sprint 5)

| สคริปต์ | หน้าที่ |
|---|---|
| `merge_qc.py` | **ทางเดียวที่เขียน `master_th.json` ได้** · ด่าน K E N T C P M G J H D L + A1/A2 |
| `check_alignment.py` | ด่าน A — จับคำแปลเลื่อนคีย์ทั้ง batch ด้วยสถิติความยาว |
| `check_glossary_locks.py` | คำล็อกจาก `glossary.md` §1 + `name_locks.json` (วลียาวชนะคำเดี่ยว) |
| `check_pronoun_pairs.py` | สรรพนามข้ามระดับ (T1/T2/T3 บาคุมัตสึ + M1/M2 ของเมนู) |
| `check_translit_drift.py` | คำทับศัพท์ที่สะกดเพี้ยนกันเองโดยยังไม่ได้ล็อก |
| `check_particle_density.py` | ความหนาแน่นคำลงท้าย ขอรับ/เจ้าค่ะ รายตัวละคร |
| `thai_pronouns.py` | **แหล่งเดียวของ regex สรรพนาม/คำลงท้าย** — ห้ามเขียนรูปซ้ำที่อื่น |
| `test_qc_tools.py` | เทสต์ตัวตรวจทั้งชุด **70 เคส** — รันทุกครั้งหลังแก้ regex หรือด่าน |

### สายบิลด์ (ใหม่ sprint 5)

| สคริปต์ | หน้าที่ |
|---|---|
| `build_text.py` | `master_th.json` -> `.msg` + ARMP + locres -> pak ม็อดไฟล์เดียว (`--install` ได้) |
| `check_armp_rebuild.py` | ประกอบทุกตาราง ARMP กลับโดยไม่แก้ แล้วเทียบไบต์ในแถว · เขียน `build/armp_deny.json` |
| `check_layout_all.py` | เทียบ layout ของ bin ที่บิลด์จริงกับ vanilla |
| `armp_layout_check.py` | ตัวเทียบไบต์ในแถว (ยกจาก LJ) |
| `check_msg_roundtrip.py` · `check_pak_roundtrip.py` | ด่านบังคับก่อนบิลด์ (กติกาเหล็กข้อ 6) |

**ผลด่าน ARMP รอบล่าสุด**: 122 ตาราง · ผ่าน 111 · **ตก 11** เพราะ reARMP เขียนคอลัมน์ชนิด 30/31
กลับเป็นศูนย์ทั้งก้อน · ตารางเดียวที่มีข้อความคือ `tips` (172 ช่อง) → `build_text.py` ข้ามให้อัตโนมัติ

## สภาพเกมตอนนี้

```
Content/Paks/~mods/IshinThaiRepak_P.pak     ← ม็อด PoC ตัวเก่า (แพ็กด้วย repak · 1.8 MB)
```

ไฟล์เกมแท้ไม่ถูกแตะเลย

`build/LikeADragonIshinThai_P.pak` (6.4 MB · 130 ไฟล์ · รวมฟอนต์ไทย) **บิลด์แล้วแต่ยังไม่ได้ติดตั้ง**
— เป็นตัวที่มีคำแปลจริงจาก master_th ครบทั้งสามชั้น

⚠ **ถอน PoC ก่อนติดตั้งตัวใหม่** (`python scripts/make_title_poc.py --uninstall`) เพราะสอง pak
มีไฟล์ฟอนต์และ locres ทับกัน

---

## ✅ คลื่นแปลที่สอง — batch 012–017 (2 ก.ย. 2026 · sprint 6)

เดิน 6 ทีมขนานกันเหมือนคลื่นแรก · **1,448 สตริง ปิด priority 2 ทั้งชั้น** · ทุกก้อนผ่านด่านอัตโนมัติ ตก 0
· alignment จับคู่ผิดคีย์ 0 ทั้ง 6 ก้อน

| batch | เนื้อหา | นักแปลแก้เอง | lead กวาด | ผู้ตรวจแก้ |
|---|---|---:|---:|---:|
| 012 | รายการภารกิจ (activity list) | 3 | 5 | 0 |
| 013 | คำพจนานุกรม · ป้ายชื่อผู้พูด | 0 | 10 | 0 (แต่จับ lead ผิด 1) |
| 014 | แผนผังความสัมพันธ์ · ชื่อถนน · UI มินิเกม | 4 | 24 | 0 |
| 015 | ประวัติตัวละคร · เรื่องย่อบทหลัก | 1 | 22 | 0 |
| 016 | เรื่องย่อบทหลัก/substory · ภารกิจหน่วยรบ | 3 | 20 | 1 |
| 017 | substory · ชื่อตอน · คำพจนานุกรม | 4 | 6 | 1 |

### บทเรียนใหญ่ของคลื่นนี้: งานที่ทำขนานกันจะตั้งคำชนกันเงียบ ๆ

ทีมทั้งหกไม่เห็นงานของกันและกัน ผลคือ **คำเดียวกันถูกตั้งคนละอย่าง 8 คำ** ทั้งที่ทุกก้อนผ่านด่านของตัวเองสะอาด
— ด่านรายก้อนมองไม่เห็นเพราะเป็นการตรวจ **ภายใน**ไฟล์เดียว

| คำ | ที่ตั้งกันมา | เคาะเป็น |
|---|---|---|
| barracks (屯所) | ค่ายทหาร (013·016) · กองบัญชาการ (014·015) | **ค่าย** — ชินเซ็นกุมิไม่ใช่กองทัพ · "กองบัญชาการ" เป็นคำสมัยใหม่ |
| Ebisu | เอบิสึยะ (012) · เอบิสุ (016·017) | **เอบิสึ** |
| Golden Sneak | โจรทอง (012) · โจรทองคำ (013) | **โจรทองคำ** |
| Ecchudo (越中堂) | เอ็จจูโด (012) · เอ็คชูโด (013) | **เอ็ตชูโด** |
| Nichibuza (にちぶ座) | นิชิบุซะ (012) · นิจิบุซะ (015·017) | **นิจิบุซะ** |
| Sengoku residence | บ้านพักตระกูล… (013) · คฤหาสน์… (014·017) | **คฤหาสน์เซ็นโงกุ** |
| Bathkeeper of Sai | เจ้าของ… (014) · คนดูแล… (015) | **เจ้าของโรงอาบน้ำแห่งไซ** |
| Sanada Militants (真田家残党) | กองกำลัง… (013) · พลพรรค… (016) | **พลพรรคตระกูลซานาดะ** |

→ เขียนเป็นเครื่องมือถาวรแล้ว: **`scripts/check_cross_batch.py`** — ตรวจสองชั้น
(1) คีย์ EN เดียวกันในหลายก้อนได้ไทยต่างกัน (2) คำไทยที่ต่างกันเล็กน้อยหลัง normalize
(วรรณยุกต์ · จ/ช · ซ/ส · ึ/ุ · ต/ท · ค/ก · บ/ป · ะ ท้าย)
ชั้นที่สองคือชั้นที่จับ 8 คำข้างบนได้ทั้งหมด · `check_translit_drift.py` เดิมรันทีละก้อนจึงมองไม่เห็น

**รันย้อนกลับไปทั้ง 17 ก้อนแล้ว** เจอคำอุทานสะกดคนละแบบในคลื่นแรกอีก 2 คำ
(เฮ่ย → **เฮ้ย** batch_004 · โอโฮ → **โอ้โฮ** batch_009) แก้และ merge ซ้ำแล้ว
เหลือ 2 คู่ที่ตรวจแล้วว่าถูกทั้งคู่ (เอ่อ/เอ้อ = คนละคำอุทาน · อะไรว—/อะไรวะ = บทที่ถูกขัดกลางคำ)
— จดไว้ในตัวสคริปต์เองแล้วกันคนรอบหน้าไล่แก้ผิด

**ขั้นบังคับใหม่ของ lead**: รัน `python scripts/check_cross_batch.py` ก่อนส่งผู้ตรวจทุกคลื่น

### คำล็อกที่ชนกันเองใน glossary (เจอตอนกวาด)

`Mibu Wolves` เขียนไว้สองที่ด้วยคำต่างกัน — §1.1 "หมาป่าแห่งมิบุ" · §1.3 "หมาป่ามิบุ"
master ใช้ "หมาป่าแห่งมิบุ" ไปแล้ว 5 บรรทัด → แก้ §1.3 ให้ตรง **ตัวตรวจคำล็อกไม่เตือนคำล็อกที่ขัดกันเอง**

### ผู้ตรวจจับข้อผิดพลาดของ lead ได้ (ครั้งแรกของโปรเจกต์)

lead สั่งกวาด `Satsuma Samurai` → "ซามูไร**แห่ง**ซัตสึมะ" เพราะเห็น "ซามูไรแห่งโชชู" อยู่บรรทัดใกล้กัน
ผู้ตรวจ batch_013 ชี้ว่าทำให้ป้าย `dictionary_word_list` ที่ JA โครงเดียวกันหมด (`X藩士`·`X藩上士`·`X藩郷士`)
ได้ไทยสองแพตเทิร์นในไฟล์เดียว (ซามูไรแห่งซัตสึมะ ปนกับ โกชิโทสะ · โจชิโทสะ · ซามูไรโทสะ)

**เคาะใหม่ (glossary §1.6.6): ไทยเดินตามรูปประโยคของ EN ไม่ใช่ตาม JA**
`X Samurai/Goshi/Joshi` (ป้ายสั้น) = ซามูไร/โกชิ/โจชิ + ชื่อแคว้นติดกัน · `Samurai of X` (ร้อยแก้ว) = ซามูไร**แห่ง**X

บทเรียน: **คำสั่งกวาดคำต้องดูทั้งกลุ่มป้ายที่ JA โครงเดียวกัน ไม่ใช่บรรทัดที่บังเอิญอยู่ติดกัน**

### หมวดใหม่ใน glossary (§1 โตจาก 97 → 174 คำล็อก)

- **§1.6** ป้ายความสัมพันธ์ (Friend/Friends/Ally/Allied ต้องแยกคำ) · ศัพท์ระบบ · **รูปประโยคมาตรฐานของ activity list**
- **§1.6.5** คำล็อกจาก batch_015/016 (barracks · Haruka · ชุดตระกูลซานาดะ · Ebisu · Rakunai)
- **§1.6.6** กฎป้ายแคว้น+ตำแหน่ง
- **§1.7 คำล็อกที่พ้องรูป — ต้องดูบริบทก่อนใช้** (หมวดที่มีค่าที่สุดของรอบนี้)

| คำ | ใช้คำล็อกเมื่อ | ใช้คำอื่นเมื่อ |
|---|---|---|
| Swordsman | ชื่อท่าต่อสู้ = ท่าดาบเดี่ยว | บรรยายคน = แปลตามความหมายปกติ |
| Brawler | ชื่อท่าต่อสู้ = ท่ามือเปล่า | ฉายาคน = **นักเลงหมัด** |
| Captain | 隊長 = **หัวหน้าหน่วย\<ชื่อ\>** (ห้าม "หัวหน้า\<ชื่อ\>" เพราะชนกับ Chief) | กัปตันเรือดำ = **นายเรือ** |
| Chief | 局長 = **หัวหน้า** เปล่า | — |
| Tennen Rishin | **สำนักเท็นเน็นริชิน** — ห้าม "ท่า…" (สงวนให้สี่ท่าต่อสู้) ห้ามเติม "ริว" | — |
| Tosa Loyalist | 土佐勤王党**員** = **สมาชิก**พรรคจงรักภักดีโทสะ | ป้ายสังกัดนำหน้าชื่อ = พรรคจงรักภักดีโทสะ เปล่า |

**`Haruka` = ฮารุกะ ยืนยันว่าใช้ได้** — เป็นตัวละครของภาคนี้จริง (`ref_ja` เป็นคันจิ 遥)
ไม่ใช่ฮารุกะของซีรีส์หลักที่ CLAUDE.md ห้ามไว้ · ล็อกพร้อมหมายเหตุกันผู้ตรวจรอบหน้าไปแก้ทิ้ง

### ต้นทุน token ที่วัดได้ (จากระบบ)

| บทบาท | ช่วงที่วัดได้ |
|---|---|
| นักแปลหนึ่ง batch | 155k – 236k |
| ผู้ตรวจหนึ่ง batch | 82k – 125k |
| รวมต่อ batch | **~240k – 360k** |

ใบสรุปงาน (`BATCH_CHEATSHEET_ishin.md`) **ไม่ได้ลด token ของนักแปลลงอย่างที่หวัง** — ยังอยู่ช่วงเดิม
แต่ **ผู้ตรวจถูกลงชัดเจน** (82–125k เทียบกับ 93–120k ของคลื่นก่อน โดยที่ก้อนใหญ่กว่า)
เพราะ lead ส่ง **รายการคำล็อกใหม่ของวันนั้นไปในบรีฟผู้ตรวจโดยตรง** ผู้ตรวจไม่ต้องเปิด glossary ทั้งไฟล์

### ชื่อที่เพิ่มลง `name_locks.json` (รวม 43 ชื่อ)

`Sengoku Toranojo` = **เซ็นโงกุ โทราโนโจ** (ō ยาว ไม่เติม ะ) ·
`Yoshida Toshimaro` = **โยชิดะ โทชิมาโระ** (o สั้น เติม ะ · คนละคนกับ โยชิดะ โทโย)

---

## ✅ คลื่นที่สาม — batch 018–029 (2 ก.ย. 2026 · sprint 7)

ผู้ใช้สั่ง **คงสูตรเดิม (นักแปล + ผู้ตรวจครบทุกก้อน) แต่ขยายจาก 6 เป็น 12 ทีม**
· 3,000 สตริง · ทุกก้อนผ่านด่านอัตโนมัติ ตก 0

**ผู้ตรวจใช้ 5 ตัวคุม 12 ก้อน โดยจับคู่ก้อนที่เนื้อหาต่อกันให้ผู้ตรวจคนเดียวกัน**
(018+019 · 020+021 · 022+023 · 024+025 · 026–029) — จงใจ เพราะปัญหาใหญ่ที่สุดของคลื่นนี้
คือ **ชื่อเดียวกันถูกแจกข้ามก้อน** ผู้ตรวจที่ถือสองก้อนพร้อมกันจึงเห็นความไม่ตรงกันที่ด่านมองไม่เห็น

| batch | เนื้อหา | lead กวาด | ผู้ตรวจแก้ |
|---|---|---:|---:|
| 018 | คำอธิบายทักษะ/ปุ่มบังคับ | 16 | **68** |
| 019 | ชื่อทักษะ · `Essence of X` 35 ชื่อ | 16 | 3 |
| 020 | ท่าต่อสู้ · ไทชิ · ไร่นา/ครัว | 0 | 3 |
| 021 | เมนูตั้งค่า · เกมพนัน | 0 | 0 |
| 022 | พจนานุกรม · นักสู้สังเวียน · อาหาร | 2 | 3 |
| 023 | พจนานุกรม · บทพูดชาวเมือง 21 | 1 | **30** |
| 024 | บทพูดศัตรู 150 · ชื่อเฉพาะ 100 | 3 | 17 |
| 025 | นามสกุลเกือบทั้งก้อน · ชื่อไอเทม | 2 | **62** |
| 026 | นามสกุลสุ่ม · ป้ายศัตรู | 4 | 0 |
| 027 | เมนูตีเหล็ก · กล่องยืนยัน | 1 | 2 |
| 028 | เมนูตีเหล็ก · เพลงในเกม 2 เพลง | 12 | 25 |
| 029 | คำอธิบายไอเทม · ร้านค้า | 0 | 0 |

### บทเรียนใหญ่: ด่านคำล็อกมองไม่เห็นสิ่งที่ไม่เคยเขียนลง glossary

**ชื่อท่า 47 จุดใน 018 กับ 28 จุดใน 023 ตั้งชนกันข้ามก้อน แล้วผ่านด่านไปได้ทั้งหมด**
เพราะชื่อท่าเหล่านั้นไม่เคยถูกเขียนลง `glossary.md` — มีแต่ปรากฏใน `.done.json` ของก้อนก่อน
`check_glossary_locks.py` อ่านเฉพาะ glossary จึงไม่มีอะไรให้เทียบ

ตัวอย่างที่ชนกัน: `Swordplay:` = "ท่าดาบ:" / "ดาบร่าย:" · `Finishing Blow` = "ท่าจบ" / "ท่าจบคอมโบ" ·
`Splendid Skill` = **"ท่าเทพ" (สแลงสมัยใหม่!) / "ท่าชั้นเลิศ" / "ท่าพิสดาร"** · `Texas Two-Step` สามคำจากสามก้อน

**แก้แล้วสองชั้น**
1. **`scripts/check_term_consistency.py`** (ใหม่) — เทียบ **ตระกูลชื่อ** ไม่ใช่คำต่อคำ
   หาคีย์ที่ EN เป็น**ชื่อยืนเดี่ยว** (= ชื่อที่โชว์ในเมนูจริง) ใช้เป็นรูปมาตรฐาน
   แล้วไล่ทุกประโยคที่เอ่ยชื่อนั้นว่าใช้รูปมาตรฐานหรือไม่ · จับได้ **30 จุด** ในคลื่นนี้
2. **`glossary.md` §1.9.7 ทะเบียนชื่อท่า** — ดึงชื่อจาก `master_th.json` อัตโนมัติ **126 ชื่อ**
   → คำล็อกที่ตัวตรวจบังคับได้เพิ่มจาก **178 เป็น 299 คำ** · คลื่นหน้าจะถูกจับตั้งแต่ด่านของนักแปลเอง

### บทเรียนรอง: `ref_tm` มีค่าที่ขัดกับ master เอง

`ref_tm` (คำจากภาคก่อน) ให้ `Cho-han` = "โจฮัง" ทั้งที่ master ใช้ "โชฮัง" มาตั้งแต่คลื่น 012/013
· นักแปลสองคนคนละคลื่นจับได้เองทั้งคู่ · ยังเจอ `Resume` = "เรซูเม่" (เอกสารสมัครงาน!) และ
`Plant` = "ต้นไม้" (คำนาม ทั้งที่ EN เป็นกริยาเมนู) → **`ref_tm` เป็นร่าง ไม่ใช่คำตอบ ย้ำในบรีฟแล้ว**

### เครื่องมือที่แก้/เพิ่มรอบนี้

| ไฟล์ | เกิดจากอะไร |
|---|---|
| `check_term_consistency.py` (ใหม่) | ชื่อท่าตั้งชนกันข้ามก้อน 75 จุด |
| `thai_pronouns.py` — `is_name_label()` | ด่าน G ตีกลับ **นามสกุลลงท้าย "คะ"** (ฮาทานาคะ · โนนาคะ · เทซึคะ) 7 บรรทัด · แยกด้วยตัวอักษรก่อนหน้าไม่ได้เพราะ "มาคะ" (ประโยค) กับ "นาคะ" (ชื่อ) ต่างกันแค่สระ → แยกที่ **รูปของทั้งสตริง** แทน |
| `thai_pronouns.py` — `RE_PHOM` · `RE_KHUN` | "รัด**มวยผม**" · "เครื่องประ**ดับผม**" (ผม = เส้นผม) · "**คุณ**ลักษณะ" |
| `romaji_to_thai.py` — คำเตือนใน docstring | **แยกขอบเขตสระติดกันไม่ออก**: Matsuura → "มาตูระ" (ที่ถูก มัตสึอุระ) · Ouchi → "โอจิ" (ที่ถูก โออุจิ) — ผลดูเหมือนถูกแต่ผิด ไม่มีสัญญาณเตือน |
| `test_qc_tools.py` | **50 → 70 เคส** (เพิ่ม 20 เคสจากบั๊กจริงของคลื่นนี้) |

⚠ **`romaji_to_thai.py` เชื่อไม่ได้กับชื่อที่มีสระสองตัวติดกันหรือมี `tsu` กลางคำ**
ผู้ตรวจ batch_025 เกือบแก้ชื่อกลุ่ม -坊 ทั้ง 9 ชื่อตามสคริปต์ แต่ไปอ่าน `notes` ของนักแปลก่อน
แล้วพบว่า**สคริปต์ผิด นักแปลถูก** — เป็นเหตุผลที่กติกาบังคับให้เขียน `notes` ทุกก้อน

### กติกา priority 3 ที่เคาะไว้ **ก่อน**แจก (ต่างจากคลื่นก่อนที่ต้องไล่แก้ทีหลัง)

`Essence of X` = **แก่นแท้แห่ง X** (ยกจาก LJ) · `Rank N` = ขั้น N ≠ `Level N` = ระดับ N ·
อาวุธทั้งชุด (刀 ดาบ · 太刀 ดาบทาจิ · 脇差 ดาบสั้นวากิซาชิ · 小太刀 ดาบสั้นโคดาจิ · 大太刀 ดาบโอดาจิ · 十手 จิตเตะ) ·
อาหาร (ชื่อจานทับศัพท์ · คำขยายแปล · **酒 เดี่ยว = "เหล้า"** · 日本酒 = "สาเก") ·
**ป้ายเมนูเพดาน 1.5×** (คำอธิบาย 1.8×) · สตริงที่ EN เท่ากับ JA = คีย์เอนจิน คงต้นฉบับ

**คำตัดสินที่ต้องอาศัยการเปิดไฟล์เกม**: `yen` (円) = **"เยน"** ไม่ใช่ "เรียว" —
`ref_ja` เป็น 円 จริง และบทมินิเกมโชงิใน `.msg` คิดค่าเล่นเป็น ５００円 โดยที่ EN เท่ากับ JA
(RGG ยกมาจากภาคปัจจุบันแล้วไม่ได้แก้) **ไม่ใช่หน้าที่เราไปดัด**

### ต้นทุน token ที่วัดได้ (จากระบบ)

| บทบาท | ช่วงที่วัดได้ |
|---|---|
| นักแปลหนึ่ง batch | 96k – 193k |
| ผู้ตรวจ (ถือ 2 ก้อน) | 114k – 207k |
| **รวมทั้งคลื่น 12 ก้อน** | **~2.8M** |
| ต่อสตริง | **~930** (คลื่นก่อน 1,220) |

การให้ผู้ตรวจถือสองก้อน **ถูกกว่าและจับได้มากกว่า** ผู้ตรวจก้อนละตัว —
คลื่นก่อนผู้ตรวจ 6 ตัวแก้รวม **2 จุด** ขณะที่คลื่นนี้ผู้ตรวจ 5 ตัวแก้รวม **213 จุด**

### ⚠ ค้างรอผู้ใช้ตัดสิน — เนื้อเพลงใน batch_021

34 คีย์ (`locres:asyura` + `locres:bakamitai`) = เนื้อเพลง **"Asura Komachi"** กับ **"Baka Mitai"**
ท่อนร้อง + ชื่อเพลง + เครดิตนักแต่ง · **ยังเป็นภาษาอังกฤษ** ผ่านด่านในฐานะ "คงต้นฉบับ"

นักแปลเลือกไม่แปลเพราะเป็นเพลงมีลิขสิทธิ์ · lead ตัดสินว่าควรแปล (กติกาคงอังกฤษครอบเฉพาะ
license/EULA/credits) แต่ **ทั้ง SendMessage กลับไปหา agent เดิมและการเปิด agent ใหม่ถูก classifier ปฏิเสธ**
→ ไม่หาทางอ้อม ยกให้ผู้ใช้: ปล่อยเป็นอังกฤษ · ผู้ใช้แปลเองแล้วให้ lead merge · หรือเปิดสิทธิ์แล้วลองใหม่

⚠ **batch_028 แปลเพลงในเกมไปแล้วสองเพลง** (`harapeko` · `ijizakura` ~29 บรรทัด) ซึ่งเป็นเพลง
ที่แต่งขึ้นสำหรับเกม ไม่ใช่เพลงปล่อยขาย — ตอนนี้จึงปฏิบัติกับเพลงสองแบบในคลื่นเดียว

---

## ✅ คลื่นที่สี่ปิดแล้ว — batch 030–041 (2 ก.ย. 2026 · sprint 8)

12 ทีมแปลขนาน + ผู้ตรวจ 6 ตัว (จับคู่ก้อนที่เนื้อหาต่อกัน) · 3,000 สตริง
**`master_th.json` = 10,128 คู่ จาก 41,601 (24.3%)** · priority 3 เหลืออีก 6,366 สตริง

| ขั้น | ผล |
|---|---|
| นักแปล 12 ก้อน | 250/250 ทุกก้อน · ด่าน `merge_qc` ตก 0 ทุกก้อน |
| ผู้ตรวจ 6 คู่ | แก้รวม **14 จุด** (030+039 แก้ 3 · 031+032 แก้ 1 · 033+034 แก้ 1 · 035+036 แก้ 1 · 037+038 แก้ 1 · 040+041 แก้ 7) |
| lead กวาดคำชนข้ามก้อน | **10 จุด** ก่อนและหลังส่งตรวจ |
| ด่านบังคับก่อนบิลด์ | `check_msg_roundtrip` 1,678/1,678 ต่าง 0 · `check_pak_roundtrip` ต่าง 0 · `check_layout_all` 22 ตาราง ต่าง 0 |
| บิลด์ | `build/LikeADragonIshinThai_P.pak` **10.5 MB · 299 ไฟล์** (msg 276 · bin 21 · locres 1 + ฟอนต์) |
| ตรวจไฟล์ที่บิลด์ได้ | เปิดกลับด้วย `tools/pakfile.py` แล้วเจอไทยครบสามชั้น — locres (UTF-16 ตามที่ UE เก็บ) · msg 276/276 · bin ผ่าน layout |
| ติดตั้ง | ลงที่ `Content/Paks/~mods/` แล้ว · ย้าย pak PoC เก่า (`IshinThaiRepak_P.pak`) ออกไปสำรองที่ `build/` เพื่อไม่ให้สอง pak ทับกัน |

### สิ่งที่คลื่นนี้เพิ่มเข้าท่อ (ใช้ต่อได้ทุกคลื่น)

| ของใหม่ | แก้ปัญหาอะไร |
|---|---|
| `translations/place_locks.json` (179 ป้าย) | ป้ายสถานที่กระจายอยู่ 5 ก้อน + จะโผล่ซ้ำในบทพูด NPC คลื่น 044–046 · แบ่ง `places` 142 ป้ายที่ตัวตรวจบังคับ กับ `context_only` 37 ป้ายที่ EN เป็นคำสามัญ |
| `scripts/make_prior_hints.py` | สร้าง `batch_NNN.prior.json` — คำที่ก้อนก่อนเคาะแล้วและโผล่ในก้อนนี้ (จาก master + คำล็อก) · **ผลที่วัดได้: คลื่นนี้ผู้ตรวจแก้ 14 จุด เทียบกับ 213 จุดของคลื่นก่อน** |
| `scripts/check_speech_layer.py` | ด่านใหม่ · จับบทพูดที่ซ่อนในชั้น priority 3 (โปกเกอร์ · ช่างตีเหล็ก · จดหมาย · โรงมาจง) ที่แปลด้วยภาษาปัจจุบัน — ด่าน M เดิมยกเว้นชั้นนี้ไว้ · ระบุแหล่งบทพูด 2,226 สตริงจากไฟล์เกมจริง (locres namespace + ตาราง ARMP) |
| `scripts/make_skill_registry.py` | สร้าง glossary §1.9.7 จาก master อัตโนมัติ (178 ชื่อ) — เดิม HANDOFF สั่งให้ lead อัปเดตด้วยมือทุกคลื่น |
| glossary §1.9.8 · §1.9.9 | กติกาชั้นมินิเกม/พนัน/ตั้งค่า + คำที่เคาะระหว่างคลื่น |

### หนี้เก่าที่ตามเก็บได้ในคลื่นนี้ (คำล็อกเคาะทีหลัง แต่ของเก่า merge ไปแล้ว)

- `Bathkeeper of Sai` — ก้อน 002 ใช้ "คนดูแลบ่อน้ำร้อนแห่งไซ" ทั้งที่ล็อกไว้ตั้งแต่ batch_013 → แก้ + merge ใหม่
- `Sushi Zanmai` — ก้อน 012 สะกด "ซูชิซันไม" (น) ขัดกฎเสียงกลืน ん+ま → "ซัมไม" ทุกจุด
- `清水寺` — ก้อน 012 · 013 · 015 · 016 ใช้ "คิโยมิสึ" (ผลดิบจาก `romaji_to_thai.py`) → "คิโยมิซุ"
- `Satsuma Consulate` · `Magistrate's Office` · `ryotei` — ร่าง `place_locks.json` ของ lead ขัดกับคำล็อกเดิมที่ ship แล้ว → ยึดของเดิม แล้วแก้ทั้งไฟล์ล็อกและก้อนที่ใช้ไปแล้ว

**บทเรียนที่ยืนยันซ้ำ**: ร่างคำล็อกใหม่ต้อง **เช็กกับ `master_th.json` ก่อนเสมอ** — สี่ครั้งในคลื่นเดียวที่ร่างของ lead
ขัดกับรูปที่ ship ไปแล้ว ทางแก้คือให้ `make_prior_hints.py` เอาคำล็อกไปเทียบกับ master ตั้งแต่ตอนสร้างไฟล์

### เครื่องมือที่แก้เพิ่มจากความผิดพลาดจริงของคลื่นนี้

- `thai_pronouns.py` — `RE_KHUN` เพิ่ม `(?<!สรรพ)` กัน **"สรรพคุณ"** (คำอธิบายยา/อาหาร) ตระกูลเดียวกับ คุณลักษณะ/คุณูปการ
- `check_glossary_locks.py` — อ่าน `place_locks.json` เพิ่ม · เพิ่มชุด `CONTEXT_ONLY` สำหรับคำล็อกที่ EN เป็นคำสามัญคำเดียว
  (Gold = ขุนพลทอง เฉพาะกระดานโชกิ · Clear Data ที่เป็นคำพ้องรูป) ไม่งั้นเตือนเท็จทุกก้อน
- `check_speech_layer.py` — คำยืมสมัยใหม่เป็น "เตือน" ไม่ใช่ "ตก" เพราะจับด้วย substring แล้วชนคำไทยปกติ
  (มินิ**เกม** · ยิน**ดีล**อง · **ดีล่ะ**)
- `sprint_status.py` — ไม่นับไฟล์คู่ `.prior.json` เป็นก้อนงาน (เดิมทำให้ "ว่างถัดไป" เพี้ยนเป็น `030.prior`)

### ต้นทุน token ที่วัดได้จริงของคลื่นนี้

| บทบาท | ช่วงที่วัดได้ |
|---|---|
| นักแปลหนึ่งก้อน (250 สตริง) | 148k – 255k (เฉลี่ย ~178k) |
| ผู้ตรวจหนึ่งคู่ (500 สตริง) | 155k – 215k |
| รวมทั้งคลื่น (12 แปล + 6 ตรวจ) | **~3.2M** |

การจับคู่ก้อนให้ผู้ตรวจตัวเดียว (6 ตัวแทน 12) ทำให้ต้นทุนตรวจลดลงครึ่งหนึ่งเมื่อเทียบกับหนึ่งก้อนหนึ่งตัว
และยังจับปัญหาข้ามก้อนได้ตรงจุดกว่า (14 จุดที่แก้ ล้วนเป็นคำที่สองก้อนตั้งต่างกัน)

### คำถามที่ยังไม่ปิดของคลื่นนี้ (ต้องดูบนจอจริง)

1. `Sp` (超 · เมนูโชกิ) — น่าจะย่อจาก "Super" คู่กับ `Take Back` (待った) ตอนนี้ใช้ "สุด"
2. `Field of Vision` (視野角) กับ `Field of View` — คนละสตริงในไฟล์เกม ยังไม่รู้ว่าเป็นค่าตั้งเดียวกันไหม
3. `Font Style` กับ `Set Font` — `ref_ja` เป็น フォント設定 เหมือนกัน แต่คำอธิบายคนละเนื้อหา
4. บทพูดสำเนียง **オラ** 4 บรรทัดในก้อน 037 ใช้ "กู" 1 · "ข้า" 1 — ไฟล์เกมไม่ผูก speaker id
   **พิสูจน์ไม่ได้ว่าเป็นคนเดียวกัน** จึงคงไว้ตามเดิม (กติกา "พิสูจน์ไม่ได้ = ห้ามเดา")

---

## งานถัดไป (เรียงตามลำดับที่ควรทำ)

### 1. เก็บงานที่ค้างจาก sprint 9 (ทำก่อนอย่างอื่น)

เซสชันก่อนถูกปิดกลางคัน agent 8 ตัวหายไป — กู้ได้ 4 ก้อน (080 · 082 · 083 · 085 merge แล้ว)
**ที่แจกใหม่แล้วกำลังเดินอยู่: 081 · MSG_001 · MSG_002 · MSG_003 · MSG_004**
ไฟล์ผลลัพธ์ของนักแปลอยู่ที่ scratchpad ชื่อ `bNNN_part.json` / `bMSG00N_part.json`

```
python scripts/assemble_done.py NNN <scratchpad>/bNNN_part.json
python scripts/merge_qc.py --dry-run --only NNN     # ต้องได้ ตก 0
python scripts/merge_qc.py --only NNN
```
แล้วเขียน `translations/review/batch_NNN.review.md` ทุกก้อน (ไม่งั้น `sprint_status.py` นับว่ายังไม่ตรวจ)

⚠ **บทเรียนจากการที่เซสชันถูกปิด**: ใส่ประโยคนี้ในบรีฟนักแปลทุกใบ —
"เขียนไฟล์ผลลัพธ์ทันทีที่แปลครบ อย่ารอจนจบการตรวจของตัวเอง"
(สี่ก้อนที่กู้ได้คือก้อนที่เขียนไฟล์ไว้ก่อนแล้ว ที่เหลือหายทั้งหมดทั้งที่แปลเสร็จ)

### 2. เดินชั้น `.msg` ให้จบ — งานที่เหลือทั้งหมดอยู่ตรงนี้

**`batch_MSG_001–083` · 20,700 สตริง** = บทสนทนาของเกมทั้งหมด (priority 9)
เป็นชั้นที่ **รู้เพศผู้พูดสูงสุด** (บางก้อนถึง 99/250 บรรทัด) จึงต้องอ่านไฟล์บริบททุกบรรทัด

**สูตรที่พิสูจน์แล้วใน sprint 9** (เร็วกว่าและถูกกว่าคู่ 1:1):
- **นักแปล 6–8 ตัวขนานกัน** ก้อนละ 250 สตริง
- **ผู้ตรวจ 1 ตัวถือ 3–6 ก้อนพร้อมกัน** — จับ "คำล็อกหลุดครั้งเดียวจาก 26 ครั้ง" และ
  "ก้อนพี่น้องตั้งคำไม่ตรงกัน" ซึ่งผู้ตรวจที่ถือก้อนเดียวมองไม่เห็น
- lead เคาะกติกาของชั้นนั้นให้จบ **ก่อน** แจก แล้วใส่ลง glossary

**ขั้นบังคับของ lead ทุกคลื่น**:
```
python scripts/make_prior_hints.py MSG_005 MSG_006 ...   # ก่อนแจก
python scripts/mark_dnt.py MSG_005 ...                   # คัดสตริงชนิดข้อมูล
# สร้าง batch_MSG_00N.todo.json (คิวที่ตัด DNT ออก) แล้วส่งไฟล์นั้นให้นักแปล
python scripts/check_cross_batch.py                      # ก่อนส่งตรวจ
python scripts/check_term_consistency.py                 # ต้องได้ 0/0
python scripts/check_speech_layer.py                     # ต้องตก 0
```

⚠ **สิ่งที่พบในก้อน MSG แรก ๆ**: มีคีย์ที่เป็น **สตริงของนักพัฒนา** (`yes。` `no。` `おしまい。`)
และบรรทัดที่ทีมอังกฤษไม่ได้แปล (ยังเป็นญี่ปุ่นล้วน) ปนอยู่ — สตริงระบบให้คงต้นฉบับ
ส่วนบทพูดญี่ปุ่นจริงให้แปลจากญี่ปุ่นตรง ๆ · ชั้นนี้ใช้ `

` จริง ต้องคงรูปแบบเป๊ะ

### 3. บิลด์ใหม่แล้วให้ผู้ใช้ทดสอบในเกม (ค้างมาตั้งแต่ sprint 8)

**ยังไม่เคยทดสอบในเกมเลยสักครั้ง** และ pak ที่ติดตั้งอยู่ยังเป็นของ master 10,128 คู่
ตอนนี้ master โตเป็น 20,651 คู่แล้ว (รวมชั้น `tips` ที่เพิ่งปลดล็อกได้)

```
python scripts/check_msg_roundtrip.py     # ต้องได้ "ต่าง 0"
python scripts/check_pak_roundtrip.py     # ต้องได้ "ต่าง 0 หาย 0 เกิน 0"
python scripts/check_armp_rebuild.py      # ตอนนี้ 116/122
python scripts/build_text.py --install
python scripts/check_layout_all.py        # ต้องต่าง 0
```
สิ่งที่ต้องดูบนจอ (สะสมมาจากสามคลื่น):
- บทคัตซีนบทแรกเป็นไทย · สระ/วรรณยุกต์วางถูก · กล่องข้อความไม่ล้น
- **ชื่อไอเทม/ทักษะ/ถ้วยรางวัลไม่ล้นกรอบ** — คลื่นนี้มีชื่อที่ยาวเกิน 1.5× ของ EN อยู่หลายจุด
  (หัวข้อสมุดบันทึก `Shopping` 2.75× · `Cocksure` 2.1× · ชื่อขั้นนักสู้ในสังเวียน)
- **หน้าจอตั้งค่า · ป้ายสถานที่บนแผนที่ · เมนูการ์ดไทชิ** (851 ใบ ชื่อยาวทั้งนั้น)
- คำถามที่ปิดไม่ได้ถ้าไม่เห็นจอ: `Sp` (เมนูโชกิ) · `Field of Vision` vs `Field of View` ·
  `Font Style` vs `Set Font` · `Handicap` (手加減) เป็นตัวเลือกความยากหรือการต่อแต้ม ·
  `Koi!` ในโออิโช-คาบุ แปลว่า "เรียกไพ่" ใช่ไหม

### 4. หนี้ที่ยังไม่ได้ปิด

- **เนื้อเพลง batch_021** 34 คีย์ยังเป็นอังกฤษ (Asura Komachi · Baka Mitai) — เป็นเพลงที่มีอยู่จริง
  นอกเกม ต่างจากเพลงที่เกมแต่งเอง (ซามูไรออนโดะ · รำนิจิบุ) ซึ่งแปลไปแล้วตามปกติ
- **6 ตาราง ARMP ที่ยังประกอบกลับไม่ได้** (`battle_cmdset` · `font2_tag` · `tips_layout` ฯลฯ)
  — ตัวอ่านมองทุกคอลัมน์เป็นชนิด -1 ไม่ใช่อาการเดียวกับ `tips` · **ทั้งหกไม่มีข้อความให้แปล**
- **ไฟล์บริบทเลื่อนหนึ่งช่อง** — ผู้ตรวจก้อน 046 พบคีย์ที่ช่อง `ja` ซ้ำกับบรรทัดก่อนหน้า
  ควรไล่ตรวจ `make_batch_context_ishin.py` สักรอบ (คำแปลไม่ได้รับผลกระทบเพราะยึด EN)

## คำสั่งที่ใช้บ่อย

```bash
python scripts/paths.py                     # เช็ค path ทั้งหมด
python scripts/sprint_status.py             # สถานะการแจกงาน (batch ไหนถึงไหน)
python scripts/scope_report.py --write      # ขนาดงาน -> docs/scope.md

# --- วงจรของ batch หนึ่งก้อน ---
python scripts/merge_qc.py --dry-run --only NNN   # ตรวจอย่างเดียว ต้อง "ตก 0"
python scripts/merge_qc.py --only NNN             # ตรวจ + merge เข้า master_th
python scripts/merge_qc.py --status               # ไฟล์ done ไหนยังไม่ตรง master (ต้อง merge ซ้ำ)

# --- ตัวตรวจคุณภาพ ---
python scripts/check_glossary_locks.py            # คำล็อก glossary §1 + name_locks.json + place_locks.json
python scripts/check_speech_layer.py              # บทพูดที่ซ่อนในชั้น priority 3 ใช้ภาษาปัจจุบัน (ต้องตก 0)
python scripts/check_pronoun_pairs.py             # สรรพนามข้ามระดับ
python scripts/check_translit_drift.py            # คำทับศัพท์ที่สะกดเพี้ยนกันเอง
python scripts/check_particle_density.py --only NNN --min-lines 3
python scripts/check_alignment.py --only NNN      # คำแปลเลื่อนคีย์
python scripts/check_cross_batch.py               # คำที่ทีมขนานกันตั้งชนกันเอง (บังคับก่อนส่งผู้ตรวจทุกคลื่น)
python scripts/check_term_consistency.py           # ตระกูลชื่อ (ชื่อท่า/สกิล) ตั้งชนกันข้ามก้อน
python scripts/test_qc_tools.py                   # เทสต์ตัวตรวจทั้งชุด (70 เคส)

# --- ด่านบังคับก่อนบิลด์ (กติกาเหล็กข้อ 6) ---
python scripts/check_msg_roundtrip.py             # ต้องได้ "ต่าง 0"
python scripts/check_pak_roundtrip.py             # ต้องได้ "ต่าง 0 หาย 0 เกิน 0"
python scripts/check_armp_rebuild.py              # ตาราง ARMP ที่ประกอบกลับไม่ได้ -> build/armp_deny.json

# --- บิลด์และติดตั้ง ---
python scripts/build_text.py                      # บิลด์ทั้งสามชั้น -> build/LikeADragonIshinThai_P.pak
python scripts/build_text.py --dry-run            # นับอย่างเดียว
python scripts/build_text.py --install            # ติดตั้งเข้า Content/Paks/~mods/
python scripts/check_layout_all.py                # เทียบไบต์ในแถวของ bin ที่บิลด์กับ vanilla
python scripts/make_title_poc.py --uninstall      # ถอน pak PoC ตัวเก่า

tools/repak/repak.exe info <pak>                  # ตรวจ pak ด้วยเครื่องมือของชุมชน
```

---

## ธรรมเนียมทีม (ย้ำจาก sprint 5)

- นักแปล/ผู้ตรวจ **อ่าน `docs/reference/BATCH_CHEATSHEET_ishin.md` ไฟล์เดียว** ก่อนเริ่ม
  (ลดการเปิดบรีฟ+matrix+glossary+name_locks ทีละไฟล์ → ประหยัด token ต่อ batch ราว 25%)
- **ห้าม `cat` ไฟล์บริบททั้งไฟล์** — query ด้วย python เป็นชุดเล็ก (ไฟล์ละ ~90KB)
- subagent **ห้าม spawn ต่อ** · ห้ามเปิดเกม · ห้ามแก้ `master_th.json` เอง
- ไฟล์ชั่วคราวใน scratchpad ต้องขึ้นต้นด้วยเลข batch (`b006_`, `b006_rev_`) — scratchpad ใช้ร่วมกัน
- lead **ต้องเขียน `translations/review/batch_NNN.review.md`** ทุกก้อน ไม่งั้น `sprint_status.py`
  จะนับว่ายังไม่ได้ตรวจ
